# Handoff — Planner Redesign PR2

**Date:** 2026-08-17
**Branch:** `redesign/planner-shell` (pushed, tracking `origin/redesign/planner-shell`)
**Plan to execute:** `docs/superpowers/plans/2026-08-17-pr2-redesign-plan.md`
**Spec (authoritative):** `docs/superpowers/specs/2026-08-17-planner-redesign-design.md`

You are taking over execution of PR2. The plan is written and good — read it in full before starting. This document covers only what the plan does *not* say: where execution actually stopped, which of the plan's stated premises have since gone stale, and what happened in the tree around it.

---

## 1. Where execution stopped

**PR1 is complete and merged into this branch.** All four handoff deliverables exist and are importable:

| Plan calls it | Actual path |
|---|---|
| accent fills/tints | `lib/accent.ts` |
| country profile seams | `lib/countryProfile.ts` |
| ISO tables / country records | `lib/countries.ts` |
| the trip-payload accessor | `lib/useTripPayload.ts` |

**Task 1 (nav source of truth) is committed.** `31dd792` landed `lib/nav.ts` (`TRIP_NAV`, `TripTabId`, `toTripTabId()`), `lib/nav.test.ts` (7 tests), and this document.

Verified at handoff time: `npx vitest run lib/nav.test.ts` → 7 passed; `npx tsc --noEmit` → clean.

*(This section originally described Task 1 as uncommitted — it was written in the same commit that landed it. Corrected 2026-08-17.)*

**One deviation from the plan, now ratified (see §5.2):** the plan specifies only `TRIP_NAV` and `TripTabId`. `toTripTabId(value)` also ships, narrowing an untrusted `?tab=` string with a `"plan"` fallback. Rationale: Task 12 (J2) puts tab state in the `?tab=` search param, so *something* has to narrow it, and putting it beside the nav array keeps the tab vocabulary in one module. **Stays in `lib/nav.ts`** — Task 12 imports it rather than redefining it.

**Everything from Task 2 onward is untouched.** `lib/contracts.test.ts` does not exist; no shell components exist; `components/TripView.tsx` still has its 7 tabs.

---

## 2. Plan premises that have gone stale — read before Task 3

### 2.1 Component testing now exists (affects Tasks 3, 4, 10, 11, 16, 24)

The plan's §0 ground rules state:

> `vitest.config.ts` runs `lib/**/*.test.ts` in a **node** environment — there is no jsdom or component-testing infrastructure and this plan does not add any.

**This is no longer true.** PR1 landed `3d42a88 test: add a jsdom vitest project so components can be tested`. Two component tests run in it today: `components/harness.test.tsx` and `components/shell/PrefsProvider.test.tsx`.

Consequence: every task that declines a test *on the grounds that component testing is impossible* is resting on a false premise. Those tasks are Task 3 (AppShell), 4 (TripSwitcher/ThemeToggle), 10 (CrewMenu), 11 (ShareMenu), 16 (PlaceSearch), 24 (DayBuilder UI).

My judgement, which the user has **not yet ruled on** — see §5: keep skipping tests for genuinely visual work (rail layout, hero scrim, drag feel, which a unit test can only fake), but write real component tests where behaviour is assertable and the plan itself already names an acceptance criterion:

- Task 16 `PlaceSearch` — the plan demands "↓↑ moves active option, Enter adds, Esc clears, input keeps focus" and `aria-activedescendant` listbox semantics. That is testable and it is the spec's a11y path.
- Task 10 `CrewMenu` — the plan demands "trigger focusable, Esc closes, focus returns". Testable.
- Task 24 `DayBuilder` — the keyboard add/move path (not the drag feel).

Note this also affects the plan's repeated claim that testable logic must live as a pure `lib/` module "with the React hook or component as a thin binding". That architecture is still *good* and I'd keep it — but it is now a design preference, not a constraint imposed by the harness. Don't let it push logic into `lib/` where a component test would be the honest check.

### 2.2 J12 is resolved

The plan says the PR1 accessor's import path was unknown at planning time and must be substituted at execution. It is `lib/useTripPayload.ts`. No guesswork needed.

### 2.3 J9 still needs checking (Task 20)

The plan says Task 20 (server-side season derivation) should be dropped "if the PR1 agent claimed it". **I did not verify this.** Before starting Task 20, check whether PR1 already moved `seasonOfMonth` derivation server-side — `lib/months.ts:29` was the hardcoded-northern line the spec objects to.

---

## 3. What changed in the tree today, outside the plan

A security fix landed on `main` and was merged into this branch. It is unrelated to the redesign but touches files the plan's contract tests will scan.

**Commit `0d35d20` — "refuse every request when a deployment has no usable secret"**, merged here as `316b80c`. It modifies `proxy.ts`, `lib/wall.ts`, `lib/wall.test.ts`:

- `wallDecision` gained a third outcome, `"refuse"`, which short-circuits before every other rule.
- `proxy.ts` returns a 503 on `"refuse"`.
- **The proxy matcher no longer excludes `api/`, `b/`, `login`, `signup`.** They are still exempt from the wall — `wallDecision` passes them by name — but the proxy now *runs* on them.

Why it matters to you: if any PR2 task adds a route or asset that must bypass the shell/wall, the matcher is no longer where exemptions live. Put them in `lib/wall.ts` with the others. Don't "restore" the matcher exclusions — they were removed deliberately, because a path the proxy never sees is a path the 503 cannot take down.

Background, in case you touch auth: `instrumentation.ts`'s boot guard cannot protect a Vercel deployment. `VERCEL=1 next build` with no secret exits 0 and prerenders `/`, `/plan`, `/login`, `/signup` as static, so the CDN answers them without ever booting a server and `register()` never runs. Verified by experiment, not inferred.

**Production state:** `BETTER_AUTH_SECRET` is now set and the wall is live (`/` → 307 `/login`). But `main` has not been redeployed since the push, so the fail-closed behaviour is **not yet live**. The user owes one redeploy, which will also carry a rotated `ACCESS_CODE`.

---

## 4. Environment gotchas

These have each cost real time in this repo:

- **`npm install` must use `--ignore-scripts` on this machine.** Plain install fails: npm auto-runs `node-gyp rebuild` for `better-sqlite3` and there's no MSVC toolchain. Safe — every native dep ships prebuilts.
- **Port 3000 is taken by Docker.** Use another port for any dev/preview server.
- **Stale `.next` artifacts produce phantom tsc errors.** After switching branches you may see `.next/types/validator.ts … Cannot find module '../../app/api/…'` for routes that exist only on the other branch. `rm -rf .next` and re-run; the source is fine.
- **The test count differs by branch.** 399 on `redesign/planner-shell`, 219 on `main` — `main` doesn't have the redesign's tests. Don't read the lower number as a regression.
- **In PowerShell, `curl` is an alias for `Invoke-WebRequest`** and rejects `-s -o -w`. Use `curl.exe` and `NUL` instead of `/dev/null`.

Verification loop the plan mandates after every task:

```bash
npx vitest run && npx tsc --noEmit && npm run build
```

---

## 5. Open questions — ALL RULED ON by the user, 2026-08-17

These are settled. Do not re-open them; if you disagree, say so rather than quietly deviating.

1. **Component tests (§2.1) — test where behaviour is assertable.** Write real component tests for Task 16 `PlaceSearch` (↓↑/Enter/Esc, focus retention, `aria-activedescendant` listbox semantics), Task 10 `CrewMenu` (trigger focusable, Esc closes, focus returns), and Task 24 `DayBuilder` (keyboard add/move path only). Keep skipping tests for genuinely visual work — rail layout, hero scrim, drag feel — where a unit test can only fake the check. Tasks 3 and 4 stay untested on those grounds.
2. **`toTripTabId` (§1) — stays in `lib/nav.ts`.** Task 12 imports it.
3. **J10 — the currency pivot gets its own PR4 plan, written now** while country-generalisation context is fresh, to execute after PR3. It stays out of PR2 and PR3 scope.
4. **The ⚠ cutovers land directly on this branch, no feature flag.** Task 5 deletes `components/shell/AppHeader.tsx`; Task 12 rewrites `components/TripView.tsx`. The branch isn't deployed, so nothing user-facing breaks before merge, and a flag would double the surface PR3 has to clean up.

---

## 5b. Progress log — Step 6 complete (2026-08-17)

Tasks 1–5 are committed and the tree is green at 421 tests, tsc and build clean.
The app shell is mounted and `AppHeader` is gone.

Findings from execution that the plan does not contain:

- **Task 2's C4 allowlist is not what the plan guessed.** `/join` is *not* an
  exception — `joinTrip` and `loadClaimable` call it from inside the accessor.
  The real exceptions are `/briefing` and `/photos`, documented in the accessor's
  own docblock. The scan also had to key on *fetch-and-path co-occurring* rather
  than a `fetch(\`/api/trips/…\`)` shape: three compliant files name a trip path
  without fetching (TripView hands URLs to `mutate()`, two carry it in comments),
  and the looser form also catches `const url = …; fetch(url)`.
- **`ShellTripContext` had to become a store.** The plan's "published by the trip
  page" cannot work with a plain context: the shell wraps the page, so the header
  is an ancestor and the read is always null. Provider above the shell, page
  publishes, header reads.
- **⚠ The token names invite a palette inversion.** `--surf-1` is `#f1f5fa`,
  byte-identical to the `bg-mist` on `body`; `--paper` is the white the old header
  used. Assigning them the way the names suggest flips every page's backdrop in
  one commit. Shell root is `--surf-1`; header and rail are `--paper`.
- **The shell must not render `<main>`** — all six page files already do.
- Task 3 and 4 got no visual tests, per the ruling, but AppShell's *route gating*
  is assertable and is tested (7 cases). Layout is not asserted anywhere.

**For verification without auth:** locally a missing `BETTER_AUTH_SECRET` is
non-fatal (`checkAuthSecret`'s `fatal: deployed`) — the documented no-accounts
mode. The wall is off and all five routes are browsable. `/api/auth/get-session`
503s, which is expected, not a regression.

Still open: **J-C5** in the PR4 plan, and `/plan`'s fixed footer remains pinned
until Task 19 (C2).

---

## 6. Suggested first moves

1. Read the plan and the spec end to end.
2. Continue at **Task 6** — extract `PackingSection` — the start of Step 7. Tasks 1–5 are done; see §5b.
3. Before Task 20, verify J9 per §2.3 — nobody has checked whether PR1 moved `seasonOfMonth` derivation server-side.
4. Task 12 is the 644-line `TripView` rewrite and the largest single risk left in PR2. It imports `toTripTabId` rather than redefining it (§5.2), deletes the local `Shell` eyebrow (J5), and must keep handing URLs to `mutate()` rather than fetching — `lib/contracts.test.ts` pins that distinction deliberately.
