# dependencies
/node_modules

# next.js
/.next/
/out/
# Regenerated by `next dev` on every run (agentRules in next.config.ts).
/AGENTS.md
/CLAUDE.md

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
# ...except the committed template, which holds no real values
!.env.example

# typescript
*.tsbuildinfo

# vercel
.vercel

# local data (SQLite db, ingest artifacts)
/data/app.db*
/data/ingest.log
/data/.ingest.lock

# agent scratch: SDD ledgers, task briefs, review packages, generated schema dumps
/.superpowers/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/e2e/.auth/
