# Travel super app: function catalogue and decision register

**Status:** generated 2026-09-24, updated 2026-09-25 for the native mobile app and the website. Sources: the catalogue workflow (four code readers, each checked by a completeness critic, a decision-register reader and a new-app designer), the places design and the native/web design. Companion to [`2026-09-24-travel-super-app-design.md`](2026-09-24-travel-super-app-design.md). Today's functions were read from the code at `main f3c5788` and cite `path:line` there.

| | Count |
|---|---|
| Functions in today's app | 280 (77 keep, 177 change, 26 drop) |
| New functions | 158 (119 first release, 37 later, 2 retired) |
| Past decisions | 181 (127 still apply, 40 replaced, 14 retired) |

## Part 1: functions by app

### Platform

#### SHL · Launcher & shell

_Home page, header, group / trip switcher, Now strip, app grid_

Where its data lives: user/device. It reads every space the person syncs from the local replica. The chosen group and trip are device-local. It has no rows of its own in MVP.

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Group / trip switcher | Pick a group, then a trip or 'No trip'. Group-wide apps (Money, Polls, Dates) ignore the trip when none is picked. The choice is remembered per device, and the context lives in the page, not the URL path, so the offline cold start works. | New · first release | offline read |  | `read-only (device-local state)` |  |
| App grid | Tiles come from registry manifests (name and three-letter code) and appear only if the resolver allows '<app>.view' in this context. A trip-scoped app with no trip picked asks you to choose one. | New · first release | offline read |  | `read-only` |  |
| Now strip | Collects each app's now(ctx) cards from local data. Order: needs attention, sign-in needed, due within 2 h, due today, money owed, open asks, info. Up to 6 cards plus 'See all'. Stable card ids are used to remove duplicates. | New · first release | offline read |  | `read-only` |  |
| Empty states | With no group: 'Create a group' or 'Have an invite link?'. With a group but no trips: 'Plan a trip' or 'Find dates'. | New · first release | offline read |  | `read-only` |  |
| Other-group badges | A dot on the switcher when another group has attention items, such as a debt or a closing poll. | New · first release | offline read |  | `read-only` |  |
| Public landing page | Anyone can visit the website's front page at / without signing in; the launcher moves to /app. | New · first release | online only |  | `web (public)` |  |
| Public place and guide pages | Shared places with tips, photos or ratings get public pages that search engines can index. Contributors appear by a public name, and dates show the month only. | New · first release | online only |  | `web (public) · web_public role` |  |
| Support, privacy and terms pages | Support contact and the legal pages, on the website and inside the app. | New · first release | offline read |  | `/support · /legal/*` |  |
| "Get the app" banner | Narrow browser windows show the phone app order and suggest the native app. | New · first release | offline read |  | `web` |  |
| Open website links in the app | Share, invite and place links open the right screen in the app when it's installed (universal links and app links). | New · first release | offline read |  | `platform/links · +native-intent` |  |
| Push notifications | Get notified when a poll is about to close, you're invited, or a change needs your attention. Blocked people never trigger one. | New · first release | online only | `self` | `device.register@1 · notification_outbox` |  |
| Phone and desktop app orders | Phones list on-trip apps first; desktops list planning apps first. Each app declares its order and any per-client exceptions. | New · first release | offline read |  | `manifest order: {phone, desktop}` |  |
| Open a trip (member view) | Opens /trip/<id> and sees the trip header, the section tabs and live data for every section. | Change | offline read |  | `components/TripView.tsx:31-377; app/trip/[id]/page.tsx:9-11; app/api/trips/[id]/route.ts:22-42; lib/useTripPayload.ts:67-137; lib/server/authz.ts:17-30` | Today one GET returns the whole TripPayload (every feature's rows) to a caller whose account is linked to a member NAME. Access is resolved in the order member > guest (valid ?code=) > none. New: the trip context is chosen in the launcher's group/trip switcher. Each mini-app reads its own slice from the synced local store (the trip sync space) instead of one aggregate payload. Access comes from the member row plus role (group role or per-trip override). |
| Switch between trip sections | Taps Plan / Today / Money / Kit (mobile pill strip or desktop rail). The choice lives in ?tab= so Back and refresh keep it. | Change | offline read |  | `lib/nav.ts:10-43; components/TripView.tsx:52-59,282-297; components/shell/RailNav.tsx:90-127` | The four fixed tabs (contract C1) become the launcher app grid plus one route per mini-app, and tiles follow permissions. Kit splits into Tickets and Packing. Journal (today inside Today) and Briefing (today inside the Share menu) become their own tiles. |
| Trip header / hero | Sees the trip name, the stops joined by arrows, season, day count and start date on a country photo band (with a photo credit) and a country 'chop' mark. The shell header above repeats the trip name with 'N days · from <date>'. | Change | offline read |  | `components/TripView.tsx:245-275,401-429; components/shell/CountryHero.tsx; components/shell/AppShell.tsx:60-76,122-143; lib/countries.ts:27-28,43` | Moves into the shell's trip-context header. The chop becomes an English passport-style stamp generated the same way for every country, with no per-country glyph. The guest header variant has no photo. Two day counts can disagree on the same screen today: the hero reads plan length (TripView.tsx:270), but the shell header strip reads data.input.days (AppShell.tsx:72-74), which addDay never updates. New: one day count, derived from the Planner's day rows or the trip's dates. **China-only part:** Only CN has a mark: '同行' (lib/countries.ts:43), rendered as a Chinese-character seal (components/TripView.tsx:256-260,405-409). |
| Trip not found | Opens a dead or mistyped trip link. The code has a 'Trip not found' card (with 'Plan a new trip' and dropping the trip from this device's remembered list), but in practice the visitor sees 'This trip is private' instead, and any code they type gets 'Wrong join code'. | Change | offline read |  | `components/TripView.tsx:66-68,184-198; lib/myTrips.ts:174-176; lib/server/authz.ts:22-29; lib/server/pgStore.ts:340-345; app/api/trips/[id]/route.ts:27-37; lib/useTripPayload.ts:231` | GET returns 403 whenever access is 'none', and a trip that does not exist always resolves to 'none': it has no member_accounts row, and joinCodeMatches returns false for an unknown trip. A 404 needs access to resolve while getTrip returns null, which cannot happen because nothing deletes trips. So the not-found card and forgetMyTrip effectively never run, and signed-out visitors without ?code= are sent to /login before any of this. The localStorage 'my trips' list is legacy (the server already has tripsForUser). The launcher lists trips from the group sync space. New: distinct 'removed', 'no access' and 'private' states. |
| Switch to another of my trips | On a trip page, opens the header's trip menu, sees the trips their account belongs to with the open one marked, and taps one to go there, or taps 'Plan a new trip'. | Change | offline read |  | `components/shell/TripSwitcher.tsx:28-129; components/shell/AppShell.tsx:122-125; lib/server/pgStore.ts:719-745` | The list is fetched from /api/me/trips on first open, not on mount. Offline or on a server error it shows only 'Couldn't load your trips.', with no cached list. It lists trips linked to the account (tripsForUser), not groups. New: this becomes the launcher's group/trip switcher, read from the group sync space so it works offline, grouped by group, with trip-only guest trips listed separately. |
| Trip pages take the country's colour | Opens a trip and the whole app's accent colour switches to that country's hue (unless Display settings pins a fixed hue). | Change | offline read |  | `components/shell/TripAccentProvider.tsx:34-38; components/shell/PrefsProvider.tsx; components/shell/ThemeToggle.tsx:90-125; lib/countries.ts:20-24` | The open trip's country is read from the payload that TripView publishes into ShellTripContext. New: the accent comes from the shell's current trip context, with a neutral non-trip default for the launcher. The per-country hue comes from reference/ country data. **China-only part:** With no trip open the accent falls back to China's hue: TripAccentProvider passes 'CN' (TripAccentProvider.tsx:37), and PrefsProvider's own default is country='CN'. |
| Account menu in the header | Taps their initial to see their email and links for My trips, Account and Sign out; a signed-out visitor sees a Sign in link instead. | Change | offline read |  | `components/auth/AccountChip.tsx:9-87 (My trips→'/' :64-67); components/shell/AppShell.tsx:145-148` | Remove 'My trips' (→ '/'): '/' becomes the launcher. Add People/Groups, Preferences, Needs attention and Install app. The chip calls authClient.useSession() (:11), which needs the network. The blueprint says the shell must take identity from the local user space, so offline the chip still shows who you are rather than dropping to 'Sign in'. |
| Home page ('Where are we going?' + Plan a new trip) | Lands on '/' and sees 'Your trips', a 'Plan a new trip' button and their trip cards. | Change | offline read |  | `app/page.tsx:4-25; components/home/TripsDashboard.tsx:158-284` | '/' becomes the launcher. It has a group/trip switcher, a Now strip of cards contributed by each app's client.tsx, an app grid filtered by the permission result, a platform card for unsent changes or Needs attention, and an iPhone install prompt. 'Plan a new trip' becomes 'New trip' in the switcher (a trip is a platform row) and Planner opens its wizard for it. Contract C1 (lib/contracts.test.ts:356-363) fails any file that writes two or more of 'Plan', 'Today', 'Money', 'Kit' as literals or JSX text without importing lib/nav, so a hand-written tile list would trip it; rewrite C1 against the registry. The not-found state's 'Plan a new trip' button (TripView.tsx:192) already links to '/'; it is its label that is wrong (see the added 'Trip not found page' row). |
| Next-trip highlight card | Sees their ongoing trip, or failing that the soonest upcoming one, as a big card ('In 12 days', 'Day 3 of 8 — happening now') with a 'Next' chop. | Change | offline read |  | `components/home/TripsDashboard.tsx:46-57,66-101 (游 chop :79); lib/myTrips.ts:115-150 (tripPhase, pickNextTrip, tripEndDate)` | pickNextTrip decides the switcher's default trip, and the phase becomes a platform Now card built from the local trip rows. Move the pure helpers (tripPhase, pickNextTrip, tripEndDate, localTodayIso) into platform/groups trip domain; the rest of lib/myTrips.ts is dead (see the device trip list). Replace the 游 chop with an English passport stamp. **China-only part:** 游 Chinese-character chop at components/home/TripsDashboard.tsx:79 |
| List of all your trips with phase labels | Sees a grid of their other trips labelled 'Tomorrow!', 'In N days', 'Past trip' or 'No date set', with date ranges. | Change | offline read |  | `components/home/TripsDashboard.tsx:59-63,103-123,166-190; app/api/me/trips/route.ts:5-14; lib/server/pgStore.ts:719-744 (tripsForUser via member_accounts)` | List trips grouped by group, taken from synced platform trip and member rows rather than the member_accounts join. Past trips stay listed because Money balances carry across trips. /api/me/trips is replaced by the sync pull of the user's group and trip spaces; at most a thin endpoint remains for a first online bootstrap. |
| Home empty, error and signed-out states | Sees 'No trips yet — Plan a trip', 'Signed out — sign in', 'Plan locally' or 'Couldn't load your trips — retry'. | Change | offline read |  | `components/home/TripsDashboard.tsx:192-256` | Drop 'Plan locally' along with no-accounts mode. 'Signed out' (a revoked cookie that still passes the optimistic wall) becomes the paused / sign-in-again state. The comment at :199-203 lists an admin password reset as one cause, but set-user-password revokes nothing today (see the admin-reset row). Load errors mostly disappear because data comes from the local replica, but a first-sync failure needs its own state. The empty state becomes onboarding: create a group, accept an invite, or plan a first trip. |
| Group and trip switcher in the header | On a trip page, opens the list icon to see their trips (the current one marked), jump to another, or 'Plan a new trip'. | Change | offline read |  | `components/shell/TripSwitcher.tsx:24-129; components/shell/AppShell.tsx:60,122-125` | Today it appears only on /trip/* routes, lists trips only, fetches /api/me/trips when opened, and links to /trip/<id>. Its empty text says 'No other trips yet' (:96-99) although the list includes the open trip. New: always visible as 'Group ▾ / Trip ▾'. A 'no trip' choice serves group-wide apps (Money, Polls, Dates). The chosen context is stored in the user space and page state, not the URL path, because an offline cold start needs one cached page per app (blueprint). Add New group and New trip actions, and read from the local replica. |
| Trip name and dates in the header | On a trip page, sees the trip name plus 'N days · from YYYY-MM-DD' on desktop. | Change | offline read |  | `components/shell/AppShell.tsx:61-79,126-138` | Read the name and dates from the platform trip row. Do not carry over today's bug: the day count uses data.input.days (:73), while the plan's real length is plan.days.length, and the two diverge after a day is added (lib/redactTrip.ts:16-21 documents this). The platform trip should own one canonical start and end date, which Dates and Planner update through the platform trip API. |
| Accent colour: follow the trip's country or pin one hue | Picks 'Per country' (colour follows the open trip's country) or 'Fixed' with a hue slider. | Change | offline edits |  | `components/shell/ThemeToggle.tsx:98-140; lib/prefs.ts:20,175-187; components/shell/TripAccentProvider.tsx:34-38; components/shell/PrefsProvider.tsx:64,120-127` | 'Per country' reads the current trip's country from the platform trip row (for a multi-country trip, the first). With no trip open, use a neutral brand hue, not China's. Country hue data (lib/accent, plus the curated hues in lib/countries.ts:36-65) moves to reference/. **China-only part:** 'CN' fallback accent: TripAccentProvider.tsx:37, PrefsProvider.tsx:64 (country='CN'), lib/tripCountry.ts:37-39 (absent country → 'CN') |
| App name and brand mark (link to home) | Sees the 游 chop plus 'Itinerary Planner' in the header and tab title, and taps it to go home. | Change | — |  | `components/shell/AppShell.tsx:105-118; app/layout.tsx:87-91; app/trip/[id]/page.tsx:4-6; app/globals.css:90-132` | The app name is still to be decided; 'Itinerary Planner' appears in three places, and the description still talks about a three-step planner. The 游 chop becomes an English passport stamp. The brand links to the launcher. **China-only part:** 游 Chinese-character chop at components/shell/AppShell.tsx:110-112; the Kaiti (Chinese brush) font stacks that exist to draw the chops, .font-kai/.stamp/.seal-round at app/globals.css:90-132 (font-kai is also applied to every country's localName, e.g. components/plan/PlaceSearch.tsx:513) |
| Move between trip sections (Plan / Today / Money / Kit) | Uses the desktop rail or the mobile pill strip to switch trip tabs, kept in ?tab=. | Change | offline read |  | `lib/nav.ts:33-43; components/shell/RailNav.tsx:66-131; components/TripView.tsx:52-59 (setTab), 282-297 (mobile pill strip); components/shell/AppShell.tsx:152` | The tabs become separate mini-apps opened from the grid (app/(apps)/<name>, each a one-line re-export). Kit splits into Tickets and Packing. Retire ?tab= and TRIP_NAV. A desktop rail, if kept, lists the registry's tiles; a phone app bar lives in #shell-bottom and replaces the in-page pill strip. |
| Pages without app chrome (sign-in, sign-up, shared briefing) | Sees /login, /signup and /b/* with no header or rail. | Change | — |  | `components/shell/AppShell.tsx:40-43,55` | Derive this from the same public-route list the wall uses (registry plus platform), and add the invite landing and offline pages. Stop maintaining two separate lists by hand. |
| Header knows which trip is open (shell trip store) | Nothing visible; lets the header show the trip name, crew and Share without fetching again. | Change | offline read |  | `components/shell/ShellTripContext.tsx:37-64; components/TripView.tsx:70-79; app/layout.tsx:137-141` | Replace it with a platform 'current context' store (group id, trip id, my member id, effective role), read from the local user space and the replica, not a TripPayload published by a page. The root provider tree shrinks accordingly. |
| Trip not found page | Opens a trip link that does not exist and sees 'Trip not found — This trip may have been created on another machine or the link is wrong' with a 'Plan a new trip' button that actually goes to '/'. | Change | offline read |  | `components/TripView.tsx:184-198 (button → '/' :192), 66-68 (forgetMyTrip on not-found); app/api/trips/[id]/route.ts:35-37 (404)` | Missing from the inventory. The new shell needs generic context states read from the local replica: 'not found', 'you no longer have access' (removed from the group, with the local copy deleted), and 'not synced to this device yet'. Drop the pre-database wording ('created on another machine') and the forgetMyTrip call. The button should say what it does (back to the launcher). |
| Install the app to the Home Screen (new) | Nothing today; in the new app, an iPhone user is prompted on the launcher to add it to the Home Screen so offline edits are kept safely. | Change | — |  | `none (no web manifest, service worker or icons in app/ or public/; public/ holds only geo data)` | NEW. Add the manifest, icons and service worker (all exempt from the login wall), and an install prompt that explains why on iPhone: Safari can wipe a site's storage after 7 days without a visit, and only the installed app keeps the outbox durable (blueprint). |
| Offline fallback page (new) | Nothing today; in the new app, opening a page that is not cached while offline shows an offline page instead of a browser error. | Change | offline read |  | `none (blueprint lists the offline page among the wall-exempt service-worker files)` | NEW. Served by the service worker, exempt from the wall, chrome-free like /login. It should link to the apps whose pages are cached so the person can carry on. |
| Switch between globe and flat map | Taps "Show the globe" or "Show a flat map". The choice is remembered, and turning on reduced motion always gives the flat map and hides the button. | Keep | offline edits |  | `components/map/WorldPane.tsx:66,80,113-124; lib/prefs.ts:23,41,80,125-130; components/shell/PrefsProvider.tsx:73-82 (cookie + PUT /api/me/prefs); app/api/me/prefs/route.ts:9,20 (GET, PUT)` | This is a per-user preference (worldView) that belongs in the user sync space. The map widget reads it from the platform prefs held by the shell. The approved platform module list has no separate prefs module, so prefs sit under platform/shell. The setting should become a named user-scoped command so a change made offline syncs later. Keep the rule that the prefs schema lists every field: Zod strips unlisted keys, and that has already clobbered one pref (MapExplorer.tsx:95-104). |
| Choose light, dark or system theme | Opens Display settings and picks Light, Dark or System; the page repaints (and follows the OS live on System) and the choice sticks on reload with no flash. | Keep | offline edits |  | `components/shell/ThemeToggle.tsx:59-80; components/shell/PrefsProvider.tsx:62-127; app/layout.tsx:105-112,123; lib/prefs.ts:90,103-134` | Keep the first-paint cookie and inline script, which also keep pages statically prerenderable. The stored value becomes a synced row in the user space instead of a fire-and-forget PUT. |
| Mobile bottom bar area and safe-area padding | Nothing visible yet: the reserved bottom region is empty. Notch and home-indicator insets are respected. | Keep | — |  | `components/shell/AppShell.tsx:96-103,163-175` | Keep it as the only fixed bottom element (contract C2). It is the natural home for the app bar or sync status on an installed iPhone app. |
| Print a page without the app chrome | Prints a trip page (e.g. from the planner's Print button or the browser) and the header, rail, menus and bottom bar are left out. | Keep | offline read |  | `components/shell/AppShell.tsx:96,172 (print:hidden); components/shell/RailNav.tsx:80; components/shell/TripSwitcher.tsx:58; components/shell/CrewMenu.tsx:75; components/shell/ShareMenu.tsx:89; components/shell/ThemeToggle.tsx:31; trigger lives in components/PlanStep.tsx:140 (window.print)` | Missing from the inventory. Keep print:hidden on all shell chrome, including the new launcher bar, switcher and app bar in #shell-bottom. The Print buttons themselves belong to the apps (Planner, Briefing). |
| Inline card actions | Act from the card itself, for example vote or mark repaid. MVP cards only navigate into their app. | New · later | offline edits |  | `the owning app's command` |  |
| Dismiss / snooze a card | Stored in the user space so it follows you across devices. | New · later | offline edits | `self` | `launcher.dismissCard@1` |  |
| Customise grid | Reorder or hide tiles, and the 'More' toolbox for later tools. | New · later | offline edits | `self` | `launcher.setGridPrefs@1` |  |
| Per-country accent overrides | Nothing: no UI sets them. | Drop | — |  | `lib/prefs.ts:21-22,68-74,113-117,159-162; lib/server/schemas.ts:351; components/shell/ThemeToggle.tsx:82-97 (comment admits there is no UI)` | accentHues is read (e.g. components/map/worldLevelShared.tsx:242) but never written by any UI, so in practice it is always {}. Drop the field and the cookie's 'hues=' segment; add it back as a real feature only if wanted. |

#### IDN · Identity

_Accounts, sign-in, sessions, devices, the login wall_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Delete my account (in the app) | Delete your account from the app. Your content is removed and your name anonymised; shared place facts stay but lose the link to you; groups you own pass to someone else. Asks you to sign in again first. | New · first release | online only | `self` | `account.delete@1` |  |
| Request deletion on the web | People who can't sign in can ask for their account to be deleted from a web page, as Google requires. The page says how long backups keep data. | New · first release | online only |  | `POST /api/account/deletion-request` |  |
| Sign up inside the phone app | Create an account on native screens, with the invite code or a group invite. Apple rejects apps that send people to a website to register. | New · first release | online only |  | `Better Auth (native screens)` |  |
| Accept terms and set consent | Accept the current terms and choose consents; the version you accepted is recorded. | New · first release | offline edits | `self` | `terms.accept@1 / consent.set@1` |  |
| Demo account for store reviewers | A permanent demo account with sample data, so App Store and Play reviewers can use the app behind the invite wall. | New · first release | offline read |  | `seeded account` |  |
| Mark a place as already visited / restore it | Taps 'Been here already? Mark visited'. The place drops out of selection and appears under 'Already been' with a Restore link. | Change | offline edits |  | `app/plan/page.tsx:17,100-130; components/DestinationStep.tsx:115-123,420-450,515-522; components/map/explorerPlaces.ts:115-127` | Today it is stored per browser in localStorage 'cip-visited-v1' and only curated China cards can be marked or hidden. Move it to the user sync space, keyed on reference place ids and markable from search or the map for any country. Planner and Explore read it. Replace the 去过 stamp with an English stamp. **China-only part:** only curated (China) places; 去过 stamp :434 |
| Prompt to sign in when creating | Signed out: sees 'Sign in to share this trip — sign in' after tapping create. | Change | — |  | `components/PlanStep.tsx:349-353,409-418; app/api/trips/route.ts:24-27; lib/wall.ts:36-44; proxy.ts:30; lib/server/session.ts:10-22` | The login wall checks only that a session cookie exists (proxy.ts:30; lib/wall.ts:36-44), not that it is valid, so an expired or invalid cookie reaches /plan and then gets this 401. With accounts off, getSessionUser always returns null (lib/server/session.ts:11), so every create returns 401 and a shared trip can never be created without accounts. Offline creation needs a local member identity instead of a 401 path. |
| Create an account | Enters a name, email and password, plus the invite code when one is configured, to create an account. | Change | online only |  | `app/signup/page.tsx:3-9; components/auth/AuthForm.tsx:36-73,86-93; lib/server/auth.ts:54-84 (emailAndPassword :66)` | Keep Better Auth email+password, but on the single platform DB client. Today auth builds its own pg Pool (auth.ts:24-33), separate from the store's postgres.js client, so no transaction can span auth and domain tables. Signup creates only a user row. A member row appears when the person creates a group or accepts an invite, keyed by member id. Today the member name is a frozen copy of user.name (app/api/trips/route.ts:80, join/route.ts:83). When signup starts from an invite link, carry the invite token through ?next= and accept the invite right after the account exists. Email ownership is never verified (no emailVerification config, auth.ts:66), so a targeted invite must be claimed by holding its token, never by matching the email alone. Signing up with a taken email returns an error, which reveals that the address has an account; Better Auth returns a generic response only with requireEmailVerification or autoSignIn:false (node_modules/better-auth/dist/api/routes/sign-up.mjs:163). The label 'Your name (shown to trip members)' is pinned by e2e/auth.setup.ts:23; update both together. |
| Invite code needed to sign up (ACCESS_CODE) | Types the shared family code at signup; a wrong code is refused with 'Wrong invite code — ask the family for it', and signup is open when no code is set. | Change | online only |  | `lib/server/auth.ts:68-82; components/auth/AuthForm.tsx:46-63,94-105; .env.example:37-40; README.md:201` | This gates account creation for the whole deployment and grants no access to any trip. The comparison is case-insensitive and not constant-time (auth.ts:73-76). Rate limiting does exist, but only Better Auth's built-in limiter: in production it caps /sign-up/*, /sign-in/* and /change-password at 3 requests per 10 s per IP (node_modules/better-auth/dist/context/create-context.mjs:169-174; api/rate-limiter/index.mjs:302-308). Its storage is in memory per serverless instance, so on Vercel it is neither shared nor durable; move it to database or secondary storage. New rule: accept /sign-up/email when ACCESS_CODE matches (an optional instance-wide gate) OR the request carries a valid, unexpired group or trip invite token, so invited people never need the family code. Use a constant-time compare. Hide the field when arriving from an invite. Neutralise the family wording ('Family invite code', 'Ask the family', AuthForm.tsx:96,102; auth.ts:78). The code still travels by smuggling it through the fetch-options body (AuthForm.tsx:46-63); keep that workaround or move it to a header. |
| Sign in | Enters email and password to sign in; on success lands on the page they were going to. | Change | online only |  | `app/login/page.tsx:3-9; components/auth/AuthForm.tsx:64-72,124-130 (login/signup cross-link keeps ?next); lib/server/auth.ts:54-84` | auth.ts sets no session options (:56-83), so Better Auth's defaults apply: expiresIn 7 days, extended at most once a day when the session is used (updateAge 1 day; node_modules/better-auth/dist/context/create-context.mjs:145-147). A session therefore dies after 7 days without an online request, for example a phone kept offline on a long trip. Raise expiresIn to 60-90 days. There is no session.cookieCache either, so every session lookup reads the database. After a 401 the outbox is 'paused'; signing in again must be as the SAME person before queued commands resend (blueprint), so the sign-in page needs a 'continue as X' mode. Brute-force protection is only the in-memory built-in limiter (see the invite-code row). The login/signup cross-link is kept. |
| Forgotten password help | Reads 'Forgot your password? Ask the trip admin to reset it' on the sign-in form. | Change | online only |  | `components/auth/AuthForm.tsx:131-135; README.md:166-167` | There is no self-service reset and no email sender. Better Auth's /request-password-reset exists but refuses to run without emailAndPassword.sendResetPassword (node_modules/better-auth/dist/api/routes/password.mjs:52-53). 'Trip admin' means nothing in the super app; point to the app admin (instance admin). Email-based reset is an open decision for the owner. |
| Sign out | Chooses Sign out from the account menu and is sent to the home page. | Change | online only |  | `components/auth/AccountChip.tsx:72-83` | Before signing out, check the outbox and warn if changes are unsent or waiting in Needs attention. On sign-out, wipe the local replica and outbox for the user, group and trip spaces from IndexedDB so the next person on a shared phone sees nothing. Today sign-out already leaves device data behind: localStorage cip-guest-code-<id> (lib/useTripPayload.ts:125), cip-member-<id> (components/TripView.tsx:42), cip-my-trips-v1 (lib/myTrips.ts:23), cip-wallet-code (lib/walletSync.ts:9) and the cip-prefs cookie. Redirect to /login instead of '/' (:78). |
| See your account details | Opens /account to see their name, email and raw account id. | Change | offline read |  | `app/account/page.tsx:29-36,83-89` | The raw id is shown only because ADMIN_USER_IDS is filled in by hand from it (.env.example:42-46). Once admin is a role in the database, move the id to the admin screen. Add 'groups you belong to' and your installed devices (user space). |
| Change your display name (no UI today) | Nothing today: there is no rename screen, and a member's name is frozen at the moment they join. | Change | online only |  | `no UI; Better Auth /update-user is reachable (node_modules/better-auth/dist/api/routes/update-user.mjs:13); frozen names at app/api/trips/route.ts:80 and app/api/trips/[id]/join/route.ts:83` | NEW UI. Every feature row will reference member_id, so renaming becomes safe; today names are the keys, so a rename would break balances and checks. Owner decision: one account name everywhere, or a per-group member display name (e.g. 'Dad' in Family). |
| Admin: list all users | An admin opens /account, taps 'Load members' and sees up to 100 accounts; a non-admin sees the same section and is told 'You're not an admin on this deployment'. | Change | online only | `instance.admin` | `app/account/page.tsx:54-66,112-122; lib/server/auth.ts:16-22,67; node_modules/better-auth/dist/plugins/admin/has-permission.mjs:4-8` | Move this to its own admin screen that non-admins never see; every user sees it today. Any listUsers error, including a network failure, is shown as 'not an admin' (:58-60), and the hint names only ADMIN_USER_IDS (:114-116) although user.role='admin' also qualifies (has-permission.mjs:4-8). Recommendation: make admin a database role, seeded from ADMIN_USER_IDS at deploy, and keep the env var only as a bootstrap. Add paging past the hard limit of 100 (:57) and show each user's groups. This is an instance-level admin, separate from group owner/organiser roles. |
| Admin endpoints with no UI (remove, ban, impersonate, set role, create, update user, revoke sessions) | Nothing through the app; an admin could call them directly under /api/auth/admin/*. | Change | online only | `instance.admin` | `node_modules/better-auth/dist/plugins/admin/routes.mjs (15 endpoints, :44-871); app/api/auth/[...all]/route.ts:12-22; lib/server/pgStore.ts:201-208 (member_accounts.user_id has no FK)` | Decide which endpoints to expose. remove-user today leaves orphan member_accounts rows, and that name then stays 'claimed' forever. New schema: member.user_id FK→user ON DELETE SET NULL, so the member row and its history survive and the member becomes account-less or left, never deleted (blueprint). Disable impersonate-user: an admin's device could otherwise queue commands as someone else, which breaks 'a queued change is only ever sent as the person who made it'. Ban and revoke-user-sessions are worth exposing for a leaked account. |
| Login wall (signed-out visitors go to sign-in) | Opens any page while signed out and is redirected (307, no-store) to /login?next=…; a session cookie, valid or not, lets them through. | Change | — |  | `proxy.ts:13-62, matcher and its comment :64-80; lib/wall.ts:23-40 (exemptions :35-38)` | Keep the wall optimistic. The blueprint says mini-app pages are static shells that never read the session on the server, so real checks happen in routes and commands. Derive exemptions from registry manifests (publicRoutes) plus the platform's own routes: /login, /signup, the invite landing page, /b/*, the service worker, web manifest, icons and offline page. Browsers fetch the manifest without cookies, so today's wall would redirect it. The matcher exempts only favicon.ico by name (proxy.ts:78-80), and no favicon or icon exists in app/ or public/. The reference artifacts in public/ (cities/, climate/, provinces/, world-countries.json, world-globe.json, country-projections.json, china-provinces.json) sit behind the wall today (proxy.ts:64-70); decide whether reference data is public, which lets the service worker cache it without credentials, or stays walled. Delete the hand-kept list (wall.ts:35-38) and its second copy (AppShell.tsx:41-43). Drop the /trip/*?code= exemption (wall.ts:38) together with join codes. Keep no-store on redirects (proxy.ts:52-61). |
| See and sign out your other devices (no UI today) | Nothing today: there is no list of signed-in devices; the only way to end other sessions is to change the password. | Change | online only |  | `no UI; Better Auth endpoints are live: node_modules/better-auth/dist/api/routes/session.mjs:340 (/list-sessions), :370 (/revoke-session), :413 (/revoke-sessions), :443 (/revoke-other-sessions); app/account/page.tsx:41-45 (revokeOtherSessions only via password change)` | NEW UI on the account screen, paired with the 'installed devices' list the account-details row proposes. Signing a device out pauses its outbox; the server cannot know whether that device holds unsent changes, so the confirmation should say so. Useful for a lost phone mid-trip. |
| Delete your account or change your email (not available) | Nothing: neither exists; the Better Auth endpoints are present but switched off. | Change | online only |  | `node_modules/better-auth/dist/api/routes/update-user.mjs:235,294,381 (/delete-user refuses unless user.deleteUser.enabled), :406,440 (/change-email refuses unless user.changeEmail.enabled); lib/server/auth.ts:56-83 (neither enabled)` | Owner decision. If account deletion is offered, it must follow the member rule: member.user_id becomes NULL and the member row, its history and money FKs survive as an account-less or left member. Changing email needs an email sender for verification, the same dependency as self-service reset. |
| Operator: reset a password from the command line (break-glass) | The owner, locked out with no working admin account, runs a local script that prints SQL for the database console or writes the new hash directly. | Change | — |  | `.vercel/reset-password.mjs:1-5 (gitignored by .gitignore:32; modes --sql for the Supabase SQL editor, --prod via DATABASE_URL, --local for data/app.db)` | Missing from the inventory. It is the recovery path when no instance admin can sign in, but it lives outside the repo and targets Supabase and SQLite. Move a secret-free version into scripts/, target Neon, drop --local, and have it also revoke the user's sessions. Alternatively make it seed the instance.admin role. |
| Return to where you were after signing in | Is sent to /login?next=… by the wall or a Sign-in link and returns to that page afterwards. | Keep | — |  | `proxy.ts:48-50; components/auth/AuthForm.tsx:10-18,28-34,70-72; components/auth/AccountChip.tsx:15-23; app/account/page.tsx:13-21; components/TripView.tsx:157; components/home/TripsDashboard.tsx:221` | Keep the same-origin safeNext check. The return URL is built in five places: AccountChip and the account page each run the same nextSuffix effect, AuthForm copies the whole query string into its cross-link, and TripView.tsx:157 and TripsDashboard.tsx:221 hardcode next=. Make it one platform helper. The blueprint keeps group/trip context in page state, not the URL, so next= only has to carry an app route plus, where present, an invite token. |
| Change your password | Enters the current and new password (8+ characters) and changes it, which signs out their other devices. | Keep | online only |  | `app/account/page.tsx:38-52,91-110` | revokeOtherSessions:true (:41-45) will pause every other device's outbox with a 401 until the same person signs in again, so the UI should say so. The 8-character minimum is checked client-side here and again in AuthForm.tsx:40 (Better Auth also enforces its own minPasswordLength); make it one shared constant. |
| Admin: reset another user's password | An admin types a new password next to a user and taps Reset. | Keep | online only | `instance.admin` | `app/account/page.tsx:68-78,123-138; node_modules/better-auth/dist/plugins/admin/routes.mjs:804-848` | Keep; this is the only in-app recovery path while there is no email. Record an audit entry (who reset whom, when). set-user-password only rewrites the password hash and does NOT revoke the target's sessions (routes.mjs:804-848), so a stolen session survives a reset. Call /admin/revoke-user-sessions (routes.mjs:712) in the same action. The target's devices will then pause their outboxes until that same person signs in. The comment at components/home/TripsDashboard.tsx:199-203, which says an admin reset invalidates the cookie, is wrong today. |
| Refuse to run with a missing or weak auth secret | A misconfigured deployment serves a 503 'misconfigured' page and fails to boot rather than serving pages publicly. | Keep | — |  | `instrumentation.ts:14-19; lib/authSecret.ts:56-74; proxy.ts:25-46; lib/wall.ts:24-30; lib/server/auth.ts:47-52` | Keep as is. Check Preview as well as Production after the Vercel project rename and the move to sin1. |
| Sign-in works on preview and alias URLs (trusted origins) | Signs in on a preview deployment or secondary alias without an 'Invalid origin' error. | Keep | — |  | `lib/server/trustedOrigins.ts:38-49; lib/server/auth.ts:64; .env.example:48-58` | Keep. Origins are derived from Vercel's system variables, so the renamed project's URLs follow automatically. TRUSTED_ORIGINS is still needed for the bare <project>-<team>.vercel.app alias and any custom domain. |
| Shared saved passwords | The website and the app share saved passwords through the system password manager. | New · later | offline read |  | `webcredentials` |  |
| No-accounts local planning mode | With no BETTER_AUTH_SECRET, the site runs open and the home page says 'Accounts are not set up on this deployment — plan a trip locally'. | Drop | — |  | `lib/wall.ts:32; lib/server/session.ts:11; app/api/auth/[...all]/route.ts:6-8,13,19; lib/server/authz.ts:49-51; components/home/TripsDashboard.tsx:205-214` | Retire it. With Postgres only, rows keyed to member ids and every write a per-person command, there is nothing to do without an identity. Today it produces three inconsistent behaviours: 503, 401, or the check skipped entirely. Local development runs with a secret against local Postgres or PGlite. |
| Warning when ACCESS_CODE is set without an auth secret | Nothing visible; logs a warning once. | Drop | — |  | `proxy.ts:14-23; README.md:208-213` | This is an upgrade note from the pre-accounts era and means nothing on a clean slate. The fatal-secret check already covers deployments. |

#### GRP · Groups & people

_Persistent groups, members, guests, invites, ownership_

Where its data lives: group. platform_group, platform_member and invite METADATA (hash, role, expiry, uses) live in the GROUP space, which syncs to group members only; trip-only guests never see it. The plaintext invite link lives only in its creator's USER space. Redemption runs server-side.

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Create group | Creates the group and the creator's owner member row in one transaction. The display name defaults to the account name. The client-generated UUIDv7 means a retry comes back as 'duplicate', never as a second group. | New · first release | online only | `fixed: any signed-in user` | `group.create@1` |  |
| Rename group | Changes the group name shown in the switcher and on stamps. | New · first release | offline edits | `group.editDetails (default owner, organiser)` | `group.rename@1` |  |
| Add a person without an account | Adds a placeholder member (a child, a grandparent) who can be a payer or split participant. Every reference goes to the member id, so they can later claim the row when they sign up. | New · first release | offline edits | `group.invite (default owner, organiser)` | `group.addPerson@1` |  |
| Create invite link | The server mints a random token of at least 128 bits and stores only its hash. The token grants member or viewer (organiser only when the owner creates the link) and expires after 14 days by default, with an optional use limit. The plaintext link is returned once and kept in the creator's user space so they can copy it again, the way the Crew menu does today. It must never be stored in the group space: every member, viewers included, syncs that space, so a viewer could read the link and mint members. | New · first release | online only | `group.invite (default owner, organiser)` | `group.createInvite@1` |  |
| Revoke invite link | Sets revoked_at, and later redemptions fail with 'link revoked'. The list shows the role, expiry and use count for each link. | New · first release | offline edits | `invite creator, or owner` | `group.revokeInvite@1` |  |
| Open and redeem invite | The invite page shows the group name, the inviter and the role offered. Signing up from this page is allowed. If unclaimed placeholder people exist, the person explicitly picks 'I'm Mum' or 'I'm new', the same claim choice JoinClaimDialog offers today. Redeeming as an existing member does nothing. Someone who left and redeems with the same account gets their old row back, so their history reconnects. Redemption is rate-limited per user and per IP. | New · first release | online only | `holder of a valid link, signed in` | `group.redeemInvite@1` |  |
| Change a person's group role | Sets owner-excluded roles: organiser, member or viewer. Organiser needs an account. Ownership changes only through Transfer ownership. | New · first release | offline edits | `fixed: owner (per the blueprint's Group row)` | `group.setRole@1` |  |
| Rename a person | Changes a display name. This is safe now because expenses, votes and checks reference the member id, not the name as they do today (lib/server/schemas.ts:51,266-271). | New · first release | offline edits | `self, or owner/organiser` | `group.renameMember@1` |  |
| Remove a person | Sets left_at and never deletes the row. The confirmation lists the person's open items, which apps contribute through the registry (for example Money's open balances per currency). It is not blocked the way Splitwise blocks a non-zero balance, because a person who left stays in the ledger and can still settle. Their queued commands come back rejected with reason 'removed'. | New · first release | offline edits | `fixed: owner` | `group.removeMember@1` |  |
| Leave group | You remove yourself and see your open balances first. The owner must transfer ownership, or delete the group, before leaving. | New · first release | offline edits | `self` | `group.leave@1` |  |
| Transfer ownership | Hands ownership to an active member who has an account. The old owner becomes an organiser, and one transaction keeps exactly one owner. | New · first release | online only | `fixed: owner` | `group.transferOwnership@1` |  |
| Delete group | Asks you to type the group name to confirm. Then it soft-deletes the group, and the command cascades deleted_at to its trips and to every feature's rows through registry hooks. Purging happens only after the retention horizon. | New · first release | online only | `fixed: owner` | `group.delete@1` |  |
| Group and people list | The switcher's group list and a people screen showing roles, 'no account yet' placeholders and people who left. It reads the local replica. | New · first release | offline read |  | `read-only` |  |
| Count travellers (adults / kids) | Uses +/- counters for adults (1-12) and kids (0-12). | Change | offline edits | `trip.manageMembers (platform)` | `components/DetailsStep.tsx:115-121,156-192; app/plan/page.tsx:394-397; lib/server/schemas.ts:36-37` | People come from group members and trip-only guests (platform member rows), not typed counts. Add an optional child flag per member so activity scoring (kids>0) and packing still work, and derive the counts from it. |
| Make the creator a trip member | Nothing. The creator is added as a member named from their account name or email (30 chars) and linked to their account. | Change | offline edits |  | `app/api/trips/route.ts:80-100; lib/server/store.ts:267; lib/server/tripStore.ts:87-90` | The creator is a member row (owner or organiser in the group). trip_member references member ids, not name strings. |
| Join a trip as a member | Signed in and on the guest view, taps 'Join this trip', confirms in the join dialog and becomes a member under their account name. Signed-out visitors see a 'Sign in to join' link instead. | Change | online only |  | `app/api/trips/[id]/join/route.ts:18-120; components/TripView.tsx:143-162; components/trip/JoinClaimDialog.tsx:13-58; lib/useTripPayload.ts:203-218` | Needs sign-in plus the trip's join code. The name is the account name (or the email local part), trimmed to 30 characters and deduplicated with ' 2', ' 3' suffixes. A user who is already linked just gets their existing name back. Races are guarded by a unique (trip, user) pair. All members are equal and there are no roles. New: joining creates a platform member row keyed by id in the GROUP (or on one trip, as a trip-only guest) with a role, default member. An account is optional for a member row and needed only to sign in. The joiner needs no switch; group.invite gates whoever issued the invite. |
| Invite people (copy invite link / read the code) | Opens the Crew or Share menu and copies /trip/<id>?code=<code>, or reads out the 6-character code. | Change | offline read | `group.invite` | `components/shell/CrewMenu.tsx:61-70,146-164; components/shell/ShareMenu.tsx:75-84,129-146; lib/server/pgStore.ts:313; lib/server/ids.ts:15` | Any member can see and share the code. The code never rotates or expires. There is no way anywhere to remove a member, leave a trip or delete a trip. New: group-level invites and trip-only guest invites, with the role set at invite time; rotate/revoke; remove people; change roles. Gated by the switch 'Group · invite and remove people' (owner yes, organiser can invite, others switchable). |
| See the crew | Opens the crew avatars and sees every member's name, join date and a YOU badge. | Change | offline read |  | `components/shell/CrewMenu.tsx:25-169` | Becomes the group's members plus this trip's overrides and trip-only guests, with role badges. Identity is the member id, not the name. |
| Create a new trip (creator becomes first member) | Finishes the /plan wizard; the trip is created and they become its first member. | Change | offline edits | `trip.create` | `app/api/trips/route.ts:19-102 (creator name :80, createTrip :81, currency stamp :88-92, link :93-100); lib/server/pgStore.ts:222-236` | The trip row is created by the platform inside a group, in ONE transaction. Today it is three separate writes, and a failed link leaves an orphan trip nobody can reach (:93-100; the error tells the user to join with a code it does not return). There is no owner today, only a 'first member'; in the new model group roles apply. Planner writes the itinerary into its own tables and Money owns the currency stamp. Open decision: a trip must have a group (FK), so a solo trip needs an automatic personal group. |
| Create a group (new) | Nothing yet; today the only 'crew' is the member list created implicitly with each trip. | Change | offline edits |  | `none; nearest is app/api/trips/route.ts:80-81 (creator inserted as the trip's first member)` | NEW. The person names the group and becomes its owner. The group persists across trips, and group-wide apps (Money ledger, Polls, Dates) attach to it. Group, member and roles are rows in the group sync space. |
| See who's on the trip (crew popover) | Sees overlapping initials in the header and opens a list of members with 'YOU' and 'joined' dates. | Change | offline read |  | `components/shell/CrewMenu.tsx:25-145 (keyed and 'YOU'-matched by name :99,129,134)` | Show group members with role badges, plus this trip's role overrides and trip-only guests. Account-less members (e.g. a child) are listed; people who left are marked as left. Key by member id, not display name. This becomes a People screen reachable from the switcher, not a trip-only header widget. |
| Invite people (copy invite link / show join code) | Copies a link /trip/<id>?code=<CODE>, or reads out the 6-character code, so others can view or join. | Change | online only | `groups.invite` | `components/shell/CrewMenu.tsx:61-71,146-164; components/shell/ShareMenu.tsx:75-85,129-147 (duplicate code); lib/server/ids.ts:15-17; lib/server/pgStore.ts:229-233` | Replace the per-trip join code (6 characters, about 30 bits, never rotated or revocable) with invites. A group invite grants a role (default member); a trip-guest invite grants a trip_member row only. Tokens are high-entropy and stored hashed, with expiry, a use limit and revocation, and there is a list of pending invites. Links go to /invite/<token>, a public landing page, instead of a code in the trip URL. An invite can target an existing account-less member ('you are Mei') so accepting it claims that member row. Delete the duplicated copy-invite code. |
| Join a trip with its code (become an editing member) | A signed-in guest taps 'Join this trip' and becomes a full editing member under their account name; a signed-out guest sees 'Sign in to join and edit this trip'. | Change | online only |  | `app/api/trips/[id]/join/route.ts:18-120; lib/useTripPayload.ts:203-218; components/TripView.tsx:139-162` | Becomes 'accept invite'. It creates the member row, or the trip_member row for a guest, with the INVITE's role, in one transaction. Today any signed-in holder of the view code becomes a full editor (:43-45). Join and link are two writes, and a lost race leaves an orphan member row (:97-117). Rate-limit token attempts; there is no app-level rate limiting today. |
| Change roles, remove someone, leave a group (new) | Nothing today: nobody can be removed, leave, or have a code rotated. | Change | online only | `groups.manageMembers` | `none (no DELETE of members/member_accounts and no rotate or leave helpers anywhere, per understand/auth-identity.md §1)` | NEW. The owner or an organiser changes roles. Removing someone marks them left, never deletes them, so their history and FKs stay intact. People can leave; ownership can be transferred; the last owner cannot leave. A member removed while offline has their queued commands for that group rejected, and their phone deletes its local copy of the group (blueprint). Recommended online-only because it is security-sensitive, but the resulting rows sync, so offline permission checks remain correct. |
| Invite by email | A pending invite tied to an email address, shown as a Now card when that address signs in. It needs a mail provider; auth today is email and password only, with no mailer (lib/server/auth.ts:66). | New · later | online only | `group.invite` | `group.inviteByEmail@1` |  |
| Promote a trip guest to group member | Gives a trip-only guest a group role while keeping the same member id. | New · later | offline edits | `fixed: owner` | `group.promoteGuest@1` |  |
| Merge duplicate people | Merges a placeholder with the account holder who joined separately. It needs a registry hook so each feature re-points its own FKs inside the same transaction. The explicit claim at redeem time is what makes this rare. | New · later | online only | `fixed: owner` | `group.mergeMembers@1` |  |
| Claim a legacy pre-accounts name | While joining, picks 'I am <old name>' to inherit ticks, expenses and journal entries recorded under a name from before accounts existed. A banner reminds a device that used to edit under that name. | Drop | — |  | `app/api/trips/[id]/join/route.ts:47-70,122-145; components/trip/JoinClaimDialog.tsx:13-58; components/TripView.tsx:39-42,163-168` | This only exists for pre-accounts data, and the clean-slate Postgres has none. The general need survives as 'claim an account-less member row' in platform/groups: linking an account to a guest who was added by name. |
| Claim your pre-accounts member name | When joining, picks 'I am <old name>' to inherit what that name ticked, spent and wrote; a banner reminds devices that used to edit as that name. | Drop | online only |  | `app/api/trips/[id]/join/route.ts:47-70,122-145; components/trip/JoinClaimDialog.tsx:13-59; components/TripView.tsx:40-42,163-168 (localStorage cip-member-<id>)` | This only migrates pre-accounts data, which the clean slate discards. Its successor is the targeted invite that claims an account-less member (see Invite people). |
| This-device trip list and wallet sync codes | Nothing: no screen writes the device list or calls the wallet. | Drop | — |  | `lib/myTrips.ts:3-15,152-181 (localStorage cip-my-trips-v1); lib/walletSync.ts; app/api/wallet/route.ts:8-35; app/api/wallet/fetch/route.ts:6-27; app/api/wallet/put/route.ts:6-37; components/TripView.tsx:66-68` | Wallets are already decided as dead. saveMyTrip has no caller at all; loadMyTrips, replaceMyTrips, mergeTripLists and removeMyTrip are used only by walletSync (lib/walletSync.ts:1-7), which nothing imports, plus forgetMyTrip. forgetMyTrip (TripView.tsx:67) prunes a list nothing writes. wallet/fetch and wallet/put have no session check at all. Keep only the pure date helpers (tripPhase, pickNextTrip, tripEndDate, localTodayIso) and move them into platform/groups; the MyTrip type survives only because TripsDashboard maps API rows into it (TripsDashboard.tsx:33-44). Delete the wallet schemas in lib/server/schemas.ts:232-241. |

#### TRP · Trips

_The trip row every app hangs off: dates, countries, travellers_

Where its data lives: trip. platform_trip and platform_trip_member live in that TRIP's space, which syncs to all group members plus that trip's guests. Trip creation is a platform API that features such as Planner and Dates call. They never write trip rows directly.

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Create trip | Creates the trip in a group, with an optional name, dates and countries, and makes the creator a traveller. Planner's wizard calls this first and then writes its own itinerary rows. Dates calls it through 'Create trip from this window'. | New · first release | online only | `group.createTrip (default owner, organiser, member)` | `trip.create@1` |  |
| Rename trip | Changes the trip name shown in the switcher, the header and the Briefing. | New · first release | offline edits | `trip.editDetails (default owner, organiser; member ⇄)` | `trip.rename@1` |  |
| Set trip dates | Takes {start,end} as inclusive calendar dates, or {lengthDays} for an undated trip, or clear. This is the one path by which dates change: Dates' decide handler calls the same service inside its own transaction. Planner reacts to the new range by reading it and never loses days silently (see open questions). | New · first release | offline edits | `trip.editDetails` | `trip.setDates@1` |  |
| Set trip countries | An ordered list of ISO alpha-2 codes, validated against the reference/ country list. There is no default country: the list is empty until chosen. It feeds currency quick-picks, packing and climate through reference data, identically for every country. | New · first release | offline edits | `trip.editDetails` | `trip.setCountries@1` |  |
| Choose who is travelling | Adds or removes group members as travellers on this trip. This follows TripIt's split between 'is traveling' and permission. Money uses travellers as the default split, and Packing and Today use them too. Group members who are not travelling can still see the trip. | New · first release | offline edits | `trip.editDetails` | `trip.setTravellers@1` |  |
| Add a trip-only guest | Adds a person who exists only on this trip: a member row with group_role NULL, one trip_member row and a role of member or viewer. They sync only this trip's space. | New · first release | offline edits | `trip.inviteGuest (default owner, organiser)` | `trip.addGuest@1` |  |
| Guest invite link | An invite scoped to one trip that grants guest member or viewer. It is redeemed through the same flow as group.redeemInvite@1, with the same hashing and the same user-space storage of the plaintext. | New · first release | online only | `trip.inviteGuest` | `trip.createGuestInvite@1` |  |
| Remove a guest | Sets left_at on the guest. Their trip history stays, and their queued commands come back rejected. | New · first release | offline edits | `trip.inviteGuest` | `trip.removeGuest@1` |  |
| Per-trip role override | Raises or lowers one person's role on this trip only, for example Grandpa as a viewer on the ski trip. Only the owner can grant an organiser override. Guests always carry an override because they have no group role. | New · first release | offline edits | `fixed: owner, organiser (organiser up to member)` | `trip.setRoleOverride@1` |  |
| Archive / unarchive trip | Hides the trip from the switcher and from Now cards but keeps it editable, so late settlements and journal entries still work. An 'Archived' banner is shown. | New · first release | offline edits | `trip.editDetails` | `trip.archive@1 / trip.unarchive@1` |  |
| Delete trip | Soft-deletes the trip and cascades deleted_at to every row in its space. It is refused when a registry delete-guard reports live money rows, because deleting trip-tagged expenses would silently change group balances; the suggestion is to archive instead. | New · first release | online only | `fixed: owner` | `trip.delete@1` |  |
| Trip list and details | Switcher list, the countdown from the dates, and travellers. Reuses the ongoing-first, then soonest-upcoming rule in pickNextTrip (lib/myTrips.ts:129). | New · first release | offline read |  | `read-only` |  |
| Set trip length | Picks 3/5/7/10/14 days or types any value from 1 to 21. | Change | offline edits | `trip.editDates (platform)` | `components/DetailsStep.tsx:34,85-113; app/plan/page.tsx:18,392; lib/server/schemas.ts:34` | Trip length becomes the platform trip's date window (start plus length). The Dates app can set that window later through the platform trip API. Planner reads the day count and never owns it. |
| Work out which country the trip is for | Nothing directly. The trip's country follows the first picked place that knows its country, or the browsed country when nothing is picked. | Change | — |  | `lib/wizard.ts:57-73; app/plan/page.tsx:72,155-164,263-276,313` | The platform trip holds a set of countries. Derive it from every pick's reference country instead of first-pick-wins for a single country. Drop the fallback to the picker's 'CN'. The pickedIn bookkeeping goes away once each pick carries a reference id with its country. **China-only part:** fallback to open country, which defaults to CN (wizard.ts:46-50) |
| Set an optional start date | Picks a start date, or leaves it empty. | Change | offline edits | `trip.editDates` | `components/PlanStep.tsx:325,390-398; lib/server/schemas.ts:55` | Becomes the platform trip's date window (start plus length). The Dates app can set it later through the platform trip API, never by writing Planner tables. Today the start date and the month picked on the timeline are separate and never checked against each other. A June timeline month with a December start date saves a season derived from June next to a December date. With dates on the platform trip, the season and month follow from the dates. |
| Start shared trip (create) | Taps 'Start shared trip →'. The trip is saved and they land on /trip/<id>?code=<joinCode>. | Change | offline edits | `group.createTrip` | `components/PlanStep.tsx:330-370; app/api/trips/route.ts:19-102; lib/server/store.ts:52-58; lib/server/tripStore.ts:78-90` | Becomes a platform trip.create@1 in a group plus planner.generate@1 via the outbox, applied in one transaction. Today it is three separate writes: createTrip :81, setCurrencySettings :89 and linkMemberAccount :93. A failed link returns 500 after the trip already exists. The join-code redirect goes, because group membership replaces codes. |
| Rename the trip | Changes the trip's name. | Change | offline edits | `trip.editDetails` | `app/api/trips/[id]/route.ts:44-116; lib/server/schemas.ts:72-76` | No UI calls PATCH /api/trips/[id] (I searched components/ and app/). The route also ALWAYS runs buildTripData and then clearScheduleChecks (route.ts:87-107). So a request that sends only a new name regenerates the plan, with fresh item ids, and wipes every member edit and item tick. New: the name is platform trip metadata, changed by a small non-destructive command (for example trip.rename@1), 1-60 characters. |
| Set or change the start date | Gives the trip a start date, or changes or clears it, so days get real dates, tickets pin to days and Today starts counting down. | Change | offline edits | `trip.editDetails` | `app/api/trips/[id]/route.ts:87-90; components/trip/PlanTab.tsx:202-206; components/trip/TicketsTab.tsx:71-75; components/trip/TrackerTab.tsx:91-98; lib/tickets.ts:8-14` | The start date can only be set at creation. Three screens tell the user to set one (TrackerTab even says 'when creating or editing the trip'), but no UI exists, and the only API path is the destructive rebuild. New: a trip.setDates@1 command on the platform trip row (start date, possibly an end date) that changes nothing else. Every app derives its day dates from it. |
| Name the trip | Edits the trip name, which is pre-filled as '<first city> trip', then '<country> trip', then 'Untitled trip'. A cleared box falls back to the same default. | Keep | offline edits | `group.createTrip` | `components/PlanStep.tsx:299-302,323-324,380-389; lib/server/schemas.ts:54` | The fallback ladder is kept. It moves to the platform trip.create command (60 chars max). |
| Restore a deleted trip | Undeletes the trip within the retention horizon. | New · later | online only | `fixed: owner` | `trip.restore@1` |  |
| Duplicate trip | Copies the trip shell and asks each app, through the registry, whether to copy its own rows (for example the plan) to new dates. | New · later | online only | `group.createTrip` | `trip.duplicate@1` |  |
| Move trip to another group | Rewrites group_id across every feature's rows through registry hooks. | New · later | online only | `fixed: owner of both groups` | `trip.moveToGroup@1` |  |
| Traveller age band | An adult or child flag on travellers, so Planner and Packing derive the counts they keep today as bare numbers (adults and kids in lib/server/schemas.ts:36-37) from real people. | New · later | offline edits | `trip.editDetails` | `trip.setTravellerDetails@1` |  |

#### ACC · Permissions

_Roles, per-trip overrides, each app's switches, the editor_

Where its data lives: group + trip. Group-level default switches live in the GROUP space. Per-trip switch overrides and role overrides live in that TRIP's space. The resolver is one pure TypeScript module that runs on the phone (tiles, disabled buttons, offline) and on the server (every command when it arrives).

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Resolve a permission (can) | Effective role is the trip override, otherwise the group role (guests have only the trip role). The switch value is the trip's value, otherwise the group default, otherwise the manifest default. The owner is always allowed, and manifest 'never' bounds are applied. Returns allowed plus a human reason. The same code runs on the phone and on the server. | New · first release | offline read |  | `read-only` |  |
| Trip permission editor | One screen generated from every app's manifest switches: rows are switches grouped by app, columns are roles, and each cell shows the inherited default or the changed value with a reset. A new app's switches appear here without editing the platform. | New · first release | offline edits | `fixed: owner, organiser (blueprint 'Trip · change this trip's switches')` | `access.setTripSwitch@1 / access.resetTripSwitch@1` |  |
| Group default switches | The same editor at group level. It sets the defaults every trip inherits, and also governs group-wide rows that have no trip, such as untagged expenses and group polls. | New · first release | offline edits | `fixed: owner` | `access.setGroupSwitch@1 / access.resetGroupSwitch@1` |  |
| App visibility switch | The platform declares '<app>.view' for every app, default all roles. It hides the launcher tile AND skips that app's pull for the roles it excludes. This is the only read control that can actually be enforced. | New · first release | offline edits | `fixed: owner, organiser` | `access.setTripSwitch@1 (key <app>.view)` |  |
| 'Why can't I?' explanations | A disabled control shows the resolver's reason, for example 'Only organisers edit the itinerary on Japan ’26'. | New · first release | offline read |  | `read-only` |  |
| Enter a join code at the private gate | Signed in but not a member, opens a trip link without a valid code, sees 'This trip is private', types the join code and gets the read-only view. | Change | online only |  | `components/trip/PrivateGate.tsx:1-40; lib/useTripPayload.ts:50-57,120-126,226-239; app/api/trips/[id]/route.ts:27-33; lib/wall.ts:33-39` | Signed-out visitors without ?code= never reach the gate, because the wall redirects them to /login first (lib/wall.ts:38-39). Signed-in non-members and anyone holding a wrong ?code= see it, and so does anyone opening a DEAD link, because a nonexistent trip also resolves to 'none', which returns 403. The code is compared case-insensitively. It is then kept in localStorage (cip-guest-code-<id>) and sent as ?code= on every poll. A wrong code shows 'Wrong join code'. New: the code becomes an invite into the group, or into the trip as a trip-only guest, creating a member row with a role. It stops being a standing bearer token for a guest view. Outsiders who only need to read use the Briefing link. group.invite gates ISSUING invites; the person redeeming one needs no switch. |
| Read-only guest view of a trip | With a valid ?code= but not joined, sees the day list (slot and title only), the packing list and the member count. No names, ticks, money, tickets or journal are shown. | Change | offline read |  | `components/trip/GuestTripView.tsx:1-41; lib/redactTrip.ts:8-51; components/TripView.tsx:139-182; app/api/trips/[id]/route.ts:38-40; lib/wall.ts:38` | The guest view is built by explicit copy from a whitelist, and the FIELD_CLASSIFICATION canary fails the build if a new payload field is not classified. But planDays copies whole day objects (redactTrip.ts:24), so the API response carries item notes, free-text times and time blocks that the UI never renders. The guest view keeps polling with the code. It is a second read-only surface next to the Briefing link, with different redaction rules. New: anyone with read access is a viewer-role member (in the group, or a trip-only guest) and sees each app read-only according to its switches. Everyone else gets the Briefing link. Decide whether an anonymous code-only view survives at all. |
| API routes open unless each checks itself | Nothing visible; any /api/* route without its own check is public. | Change | — |  | `lib/wall.ts:37; e.g. app/api/trips/[id]/briefing/route.ts:34-42 (body parsed before the gate)` | Make APIs deny-by-default with one route wrapper (requireUser / requireSpaceRole + zod + error envelope) in place of the storeMode/getSessionUser boilerplate each route repeats today. Most writes move to /api/sync/push, which checks permission per command when it arrives. Reference APIs that should stay public (destinations, map cities/airports, airport search) are declared public explicitly. Authenticate before parsing the body. |
| Guest read-only trip view | A code-holding guest sees the trip header, day-by-day plan, packing list and member count, but no names, money, tickets or ticks. | Change | offline read |  | `components/trip/GuestTripView.tsx:4-41; components/TripView.tsx:139-182,401-429; lib/redactTrip.ts:8-28,36-52; app/api/trips/[id]/route.ts:38-40` | Split in two. Signed-in viewers see each app read-only according to the permission result, and each app hides its own edit controls. Anonymous viewers get only the Briefing's public sections. Keep the allowlist plus compile-time canary (redactTrip.ts:36-52) as the platform's share-page canary: every Briefing or Today section must declare public true or false. The guest header's country chop becomes an English stamp. **China-only part:** country 'mark' chop (only CN has one: 同行, lib/countries.ts:27-28,43) rendered at components/TripView.tsx:405-409 |
| Who counts as a trip member (access classification) | Nothing visible; decides whether a request gets the full trip, the guest view, or 'This trip is private'. | Change | offline read |  | `lib/server/authz.ts:6-39; app/api/trips/[id]/route.ts:27-41` | Today: member (linked account) > guest (valid code) > none, returning a display name. New: find the user's member row in the group. The effective role is the trip override if one is set, otherwise the group role; trip-only guests exist only on their trip. The app switch is the trip's setting if set, otherwise the manifest default. Return the member id and role. The same pure function runs on the phone (for the app grid and UI) and on the server (for commands). |
| Only members can change a trip (write gate) | A non-member's edit gets 'Sign in to make changes' (401) or 'Only trip members can make changes — join the trip first' (403). | Change | offline edits |  | `lib/server/authz.ts:45-62; used by every trip sub-route, e.g. app/api/trips/[id]/briefing/route.ts:41-42` | Check once per command when it reaches /api/sync/push: the session user must be the command's author, still a member, and allowed by the switch. A failure is 'rejected' with a reason and goes to Needs attention; a 401 is 'paused'. The session lookup done twice on the deny path (:36,:54) and the ?code= read on writes both go away. |
| Who may edit or delete other people's items | Journal entries can be edited only by their author; any member can edit or delete anyone's tickets, expenses and settlements, rebuild the plan, or revoke the briefing link. | Change | offline edits | `money.editAny, tickets.editAny, planner.edit, briefing.share` | `app/api/trips/[id]/journal/[entryId]/route.ts:61-64,100-103 (author-only); app/api/trips/[id]/expenses/[expenseId]/route.ts:27,73, app/api/trips/[id]/tickets/[ticketId]/route.ts:23,64, app/api/trips/[id]/settlements/[settlementId]/route.ts:12 (requireMember only)` | These rules become switches each app declares in its manifest with defaults (e.g. money.editAny owner+organiser, journal own-only, planner.edit, briefing.share). They are enforced at command arrival and edited per trip in the generic permission editor. Ownership compares member ids, not names. |
| Change who can do what on this trip (permission editor, new) | Nothing today: no roles exist beyond member, guest and none. | Change | online only | `trip.editSwitches` | `none (lib/server/authz.ts:6-9 has only member/guest/none)` | NEW. One generic screen built from every manifest's permissions. The owner or an organiser flips per-trip switches and sets per-trip role overrides and trip-only guests; group-level defaults are set on the group. A new app's switches appear here automatically. |
| Create a public read-only briefing link | Taps 'Create share link' to get /b/<code> for people not joining the trip. | Change | online only | `briefing.share` | `components/trip/BriefingShare.tsx:12-86; app/api/trips/[id]/briefing/route.ts:17-65; lib/server/ids.ts:24-30; lib/server/pgStore.ts:485-513` | Add a platform share_link table: space-scoped, with created_by member id, a code of 60+ bits, revoked_at, optional expiry and label, and several links per trip allowed (today one per trip). Briefing, and later other apps, consume it. Today any member can create or revoke; that becomes a switch. Fix the order so the gate runs before the body is parsed (route.ts:34-42). |
| Copy or revoke the public link | Copies the /b/ URL, or taps 'Revoke link' to kill it; sharing again produces a new code. | Change | online only | `briefing.share` | `components/trip/BriefingShare.tsx:93-108,122-129; lib/server/pgStore.ts:515-519 (hard DELETE)` | Revoking sets revoked_at instead of deleting the row. That keeps an audit trail and follows the rule that synced rows are never hard-deleted. |
| Preview as role | Renders any app as a viewer, member or guest would see it. | New · later | offline read |  | `read-only` |  |
| Permission change history | Who changed which switch or role, and when, taken from the command log. | New · later | online only |  | `read-only` |  |
| Presets | One-tap bundles such as 'Organisers only' or 'Everyone edits'. | New · later | offline edits | `fixed: owner, organiser` | `access.applyPreset@1` |  |
| View a private trip by typing its join code | Opens /trip/<id>, sees 'This trip is private', enters the code and gets a read-only view. | Drop | online only |  | `components/TripView.tsx:131-137; components/trip/PrivateGate.tsx:7-40; lib/useTripPayload.ts:50-57,98,125,226-239` | Anonymous viewing of the live trip goes away. Outsiders get a Briefing share link; signed-in read-only people get the viewer role or a trip-guest invite. This also removes the code sent in the URL query (useTripPayload.ts:98) and the localStorage key cip-guest-code-<id> (:53,125), and there are no /trip/[id] URLs in the new model anyway. |

#### PLC · Places

_Place catalogue, pins, sharing, merges, moderation, recommendation signals_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Pin a place on the map | Long-press, or centre the crosshair, to drop a pin, then give it a name, category and short note. It stays private to your group. | New · first release | offline edits | `places.contribute` | `place.contribute@1` |  |
| Type coordinates or use my location | Enter decimal or DMS coordinates, or use the phone's location. This is the accessible alternative to dragging, and the fallback without WebGL. | New · first release | offline edits | `places.contribute` | `part of place.contribute@1` |  |
| Move a pin | Drag or nudge a pin to fix its location. On a shared place this proposes a correction instead of moving it. | New · first release | offline edits | `places.contribute` | `place.tag@1` |  |
| Edit a place | Change its name or category. On a shared place you edit only your own contribution. | New · first release | offline edits | `places.contribute` | `place.edit@1` |  |
| "Is this the same as…?" | When you add a place, the app suggests up to 3 likely duplicates nearby. Your answer links them. | New · first release | offline edits | `places.contribute` | `place.link@1` |  |
| Share with everyone / stop sharing | Publish a private place, with the fields you choose, to all users, or withdraw it. Plans that use it keep working. | New · first release | offline edits | `places.share` | `place.share@1 / place.unshare@1` |  |
| Add a tip or photo | Add a tip, or a photo (stored in R2, location data stripped). It follows the place: private to your group, or public if the place is shared. | New · first release | offline edits | `places.contribute` | `contribution.add@1 / contribution.withdraw@1` |  |
| Rate a place | Give a rating; your latest rating replaces the earlier one. | New · first release | offline edits | `places.contribute` | `place.rate@1` |  |
| Report content | Report a shared place, tip or photo. After reports from 2 different groups it is hidden until an admin reviews it. | New · first release | offline edits | `signed in` | `content.report@1` |  |
| Hide, remove or restore (admin) | Admins review reports and hide, remove (keeping a tombstone) or restore content. | New · first release | online only | `places.moderate` | `moderation.hide@1 / moderation.remove@1 / moderation.restore@1` |  |
| Merge and undo a merge | Combine two records of the same place, or undo it exactly. A group can merge its own private places; shared places are merged by admins or the nightly job. | New · first release | offline edits | `members (private) · moderators (public)` | `place.merge@1 / place.unmerge@1` |  |
| Admin review queue | Side-by-side review of uncertain duplicates and reported content, with merge, not-the-same and undo. | New · first release | online only | `places.moderate` | `read-only + moderation commands` |  |
| Places resolve everywhere | Any app that stores a place id shows the current place, following merges, with your group's own copy if a shared one is hidden. | New · first release | offline read |  | `resolve(ids, viewer)` |  |
| Search places and addresses | Search the app's own city and place files first, which works offline, then an address search (Photon, falling back to Geoapify) through one cached server route. | New · first release | offline read |  | `read-only` |  |
| Wikidata attractions for every country | A nightly job adds tourist attractions from Wikidata (CC0), ranked by fame, to the catalogue files. It is the answer to the thin plans outside China. | New · first release | offline read |  | `nightly job` |  |
| Licences and attribution page | Lists every data source and its licence: GeoNames, Wikipedia, Overture, Foursquare's notice, the OpenStreetMap map, and the contributor terms. | New · first release | offline read |  | `read-only` |  |
| Contributor terms | When you first share, you accept the terms: facts as CC0, tips and photos as CC BY 4.0. The version you accepted is recorded. | New · first release | offline edits | `places.share` | `part of place.share@1` |  |
| Signals for future recommendations | Viewed, saved to a plan, visited, rated, voted and dismissed are recorded, with the travel month, in the same transaction as the action. No models are built yet. | New · first release | offline edits |  | `signal_event (in-transaction)` |  |
| Overture places | Adds Overture Places (CDLA-P-2.0 / Apache-2.0) after a trial run checks its tourism quality, China included. | New · later | offline read |  | `nightly job` |  |
| Dismiss a recommendation | Say why a suggested place isn't for you. This is used once recommendations exist. | New · later | offline edits | `self` | `rec.dismiss@1` |  |

#### SAF · Safety

_Content filter, reports, blocking: what the app stores require_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Content filter before posting | Tips, places and photos are checked before they go public; photos from new accounts wait for review. Apple 1.2 requires filtering before posting. | New · first release | offline edits |  | `inside place.contribute@1, place.share@1, contribution.add@1` |  |
| Report a person or content | Report a person, a shared place, tip or photo, or an item in your group, with a reason. Works signed out on public pages too. | New · first release | offline edits | `signed in · public form` | `content.report@1 · POST /api/report` |  |
| Block / unblock a person | Hide everything a person writes and stop their notifications reaching you. Stored privately in your own space. | New · first release | offline edits | `self` | `user.block@1 / user.unblock@1` |  |
| Report handling | Reporters get an acknowledgement; admins work a queue with a response-time target and can hide, remove or restore. | New · first release | online only | `places.moderate` | `admin queue` |  |

#### SYN · Offline & sync

_Outbox, Needs attention, install, re-login_

Where its data lives: user. The outbox and local replica live on the device (IndexedDB). Needs attention lives in the USER space, synced across that person's devices. The command log and results are server-side.

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Save while offline | Every feature write updates the local replica immediately and queues a named command (name@version) in the outbox, with a stable command id. | New · first release | offline edits |  | `platform (wraps every feature command)` |  |
| Pending badges | A small 'not sent' clock on any row touched by an unsent command. It clears when that command's result is 'applied'. | New · first release | offline read |  | `read-only` |  |
| Sync status chip | Shows Online, Sending 3, Offline · 3 waiting, Paused · sign in, or 'Updated 5 min ago' for each space. | New · first release | offline read |  | `read-only` |  |
| Unsent changes list | The outbox in plain language, for example 'Add expense “Dinner” ¥4,000 · Japan ’26'. | New · first release | offline read |  | `read-only` |  |
| Send now and automatic retry | Sends from the foreground: on app open, the online event, visibilitychange and a timer, with backoff, resending the same command id. iOS has no Background Sync. | New · first release | online only |  | `platform push` |  |
| Needs attention list | Each rejected change shows its reason in plain words: no permission now, removed from the group, changed by Mei meanwhile, poll closed, invalid. The actions are 'Edit & resend', which opens the app's form prefilled and sends a NEW command id, and 'Discard'. An item survives even after the group's data was removed from the device. | New · first release | offline edits | `self` | `sync.resolveAttention@1` |  |
| Paused session and re-login | On a 401 the queue is kept and you are asked to sign in. The queue is resent only if the new session is the same user. If someone else signs in, the queue is held, and discarding it requires an explicit 'Discard Darren's unsent changes'. | New · first release | online only |  | `platform push` |  |
| Sign-out guard | Warns when unsent changes exist and keeps them on this device for that user. | New · first release | offline read |  | `read-only` |  |
| Signed-in devices | Lists sessions and lets you sign out the others. This is needed once sessions are raised to 60–90 days, so a lost phone can be cut off. | New · first release | online only | `self` | `auth (Better Auth revokeSession)` |  |
| Removed-from-group notice | 'You were removed from Family; 2 changes couldn't be saved.' The local copy of that group is then deleted, and the attention items keep their text. | New · first release | offline read |  | `read-only` |  |
| Update ready | A new service worker leads to 'Reload to update'. Queued commands stay valid because shipped name@version shapes are frozen. | New · first release | offline read |  | `read-only` |  |
| See other members' changes live | Keeps a trip open and sees other members' edits appear within about 4 s, and at once when the tab regains focus. | Change | offline read |  | `lib/useTripPayload.ts:139-180; lib/tripPayloadCore.ts:12,21-28,83-93` | Today the client polls every 4 s while the tab is visible. A payload is applied only if its version is strictly newer. That version is one trip-wide counter that every write in every feature bumps. A failed mutation forces a refetch. New: the group, trip and user sync spaces pull rows into IndexedDB, with a version per row, so adding an expense no longer forces a concurrent plan edit to retry. |
| Edits show instantly and roll back if refused | Sees builder edits at once. If the server refuses one, the day snaps back and an error banner explains why. | Change | offline edits |  | `components/plan/useDayBuilder.ts:82-133; lib/dayBuilder.ts:259-312,452-541` | Today: one op in flight at a time. Ops that name an item which no longer exists are dropped. On failure the optimistic change and everything queued after it are undone. During a drag at most one newest payload is held back. This is a hand-built outbox for one feature; generalise it into the platform outbox (applied / duplicate / rejected / retry / paused, plus the Needs attention list). |
| Trip page when offline or the server fails | Opens or keeps a trip open while offline or while the API returns an error (500/503). On first load they see 'Loading trip…' indefinitely; once loaded, the last good copy stays on screen with no stale or offline indicator; a save shows 'Couldn't reach the server — try again.' | Change | offline read |  | `lib/useTripPayload.ts:95-133,160-180; lib/tripPayloadCore.ts:59-70; components/TripView.tsx:123-129` | A network failure returns silently, and any non-2xx status other than 403 or 404 is classified 'error' and ignored, so the page keeps retrying every 4 s with no message. New: mini-apps render from the IndexedDB store, show sync status (online, offline, pending count, Needs attention), and queue edits in the outbox instead of failing them. |
| Preferences follow you to another device | Expects their theme and accent on a new device; today they only stay on the device where they were set. | Change | offline edits |  | `app/api/me/prefs/route.ts:9-50; components/shell/PrefsProvider.tsx:72,74-87; lib/server/pgStore.ts:196-200,747-764` | Server storage is effectively write-only today. PrefsProvider reads only the cookie (:72) and only ever PUTs (:80); nothing calls GET /api/me/prefs, so a new device never receives saved prefs. user_prefs has no FK to user (pgStore.ts:196-200). New: prefs are rows in the user sync space, pulled at sign-in, and they refresh the first-paint cookie. |
| Needs attention: fix or discard a rejected offline change (new) | Nothing today; in the new app, opens a list of queued changes the server rejected (e.g. no permission, removed from the group, conflicting edit) and fixes or discards each. | Change | offline edits |  | `none (blueprint: 'a rejected change lands in a Needs attention list, where you can fix it or discard it')` | NEW. Rejected commands keep their name@version, payload, space and the server's reason. Discard removes the entry; Fix reopens the owning app's form with the payload and queues a new command. Reachable from the account menu and a platform Now card, and checked before sign-out. |
| See unsent changes and sync status (new) | Nothing today; in the new app, sees that N changes are waiting to send, that sending is paused until they sign in again, or that everything is saved. | Change | offline read |  | `none (blueprint: 'The platform adds its own card while anything is waiting to sync or needs attention'; the installed app 'always shows unsent changes')` | NEW. A platform Now card on the launcher plus a small indicator in #shell-bottom or the header. It covers the outbox states the sync layer returns: pending, paused (401) and needs attention. Today writes fail silently offline, e.g. the prefs PUT is fire-and-forget (PrefsProvider.tsx:80-86). |
| Download trip reference data for offline | Cities, map and climate shards for the trip's countries, used by Explore and Planner reading offline. | New · later | online only |  | `read-only` |  |
| Discard one unsent change, reset device copy, conflict diff | Cancel a single queued command (later commands may depend on it), resync this device's replica while keeping the outbox, and a side-by-side 'yours vs theirs' view. | New · later | offline edits | `self` | `sync.discardQueued@1 / read-only` |  |
| Android Background Sync | A Chromium-only extra that gives up after about 3 attempts. Foreground sending stays the real path. | New · later | offline edits |  | `platform push` |  |
| Install prompt | In an iPhone or iPad Safari tab (not standalone), shows Share → Add to Home Screen steps, because Safari has no beforeinstallprompt. On Chromium it shows an Install button. Before installing on iOS, it asks you to send unsent changes first: the installed app has its own storage and sign-in. | Drop | offline read |  | `read-only` | Retired 25 Sep: phones use the native app, and the website has no install flow. |
| Storage persistence | Calls navigator.storage.persist() after install and after sign-in. Shows persisted yes/no and the usage from navigator.storage.estimate() under Settings › Offline. | Drop | offline read |  | `read-only` | Retired 25 Sep: phones keep data in SQLite; the website no longer asks for persistent storage. |

#### KIT · UI kit, files, database

_Shared components, photo storage, migrations_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Photo upload from the phone | Photos taken or picked on the phone upload straight to R2 through a presigned link, separately from the command outbox. | New · first release | offline edits |  | `blob.reserve@1 / blob.finalize@1` |  |
| View an uploaded photo | Sees the image thumbnails inside entries. | Change | offline read |  | `app/api/trips/[id]/photos/[photoId]/route.ts:7-25; lib/server/photoStore.ts:102-116` | Members only. Non-members get the same 404 as a missing file, so the response cannot be used to probe which files exist. Cached privately as immutable. New: served through the platform/files access check (decide whether the viewer role can see photos) and cached for offline. |
| Close header menus with Escape or a tap outside | Presses Escape or taps elsewhere to close the account, crew and share menus; the display-settings and trip-switcher menus stay open until their own button is tapped again. | Change | — |  | `components/auth/AccountChip.tsx:25-40 (Esc and outside click, no focus return); components/shell/CrewMenu.tsx:32-50,76-81 and components/shell/ShareMenu.tsx:53-67,90-95 (Esc returns focus); components/shell/ThemeToggle.tsx:31 and components/shell/TripSwitcher.tsx:57-62 (native <details>; CrewMenu.tsx:17-19 notes native disclosure lacks the Esc/focus behaviour)` | Missing from the inventory. Five header menus use three different behaviours built by hand. Build one menu/popover primitive in the UI kit (Esc closes and returns focus, outside tap closes) and use it for the account menu, switcher, People and preferences. |
| Running without a database (shared features answer 503) | On a deployment without DATABASE_URL, sees 'Shared trips need a database…' errors while the planner still works; locally the app falls back to SQLite. | Drop | — |  | `lib/server/store.ts:26-33 (DB_UNAVAILABLE; storeMode postgres \| unavailable on Vercel \| sqlite locally); repeated in every route, e.g. app/api/me/trips/route.ts:6-8, app/api/me/prefs/route.ts:10-12,21-23; app/b/[code]/page.tsx:19; lib/server/auth.ts:32 (SQLite auth path); README.md:175-181` | Missing from the inventory (sibling of the no-accounts mode). Postgres only: a deployment without a database should refuse to boot like the missing-secret check, and local development uses PGlite or local Postgres. The 29 storeMode checks disappear into the route wrapper. May overlap with the data-layer slice. |

### Mini-apps

#### PLN · Planner

_Wizard, generated itinerary, day builder, gateways_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Open the trip wizard | Starts a new trip plan from the home page, the trip switcher or the dashboard button. | Change | offline read | `group.createTrip (platform, owner/organiser on by default)` | `app/page.tsx:17; components/shell/TripSwitcher.tsx:120; components/home/TripsDashboard.tsx:132; app/plan/page.tsx:20` | Entry becomes the Planner tile / 'New trip' in the launcher, opened inside the selected group. The trip row itself is created by platform/trips; Planner only generates its itinerary. |
| Plan another trip (restart) | Clears the wizard to start a fresh plan. | Change | — |  | `app/plan/page.tsx:321-333,495-501` | Drop the reset to country 'CN'. Restart clears picks, interests, month, arrival and hand-typed places, but not days, adults, kids or season. Decide whether it should. **China-only part:** setCountry("CN") at page.tsx:330 |
| Choose a season | Taps Spring, Summer, Autumn or Winter. Each chip is captioned with that hemisphere's months. | Change | — |  | `components/DetailsStep.tsx:60-83; lib/meta.ts:38-43,65-73; app/plan/page.tsx:30 (default 'autumn')` | Derive the season from the platform trip's dates or month through the reference hemisphere. Keep the chips only as a fallback when there are no dates, and remove the hard 'autumn' default. On a first pass the captions read the picker country (CN), so they show northern months. **China-only part:** first-pass captions use CN because the picker defaults to CN (page.tsx:379-390) |
| Add a hand-typed place with no map pin | Picks the last row, 'Add "X" as its own place', for somewhere no dataset knows. | Change | offline edits | `planner.edit` | `lib/placeSearch.ts:146-161; components/plan/PlaceSearch.tsx:509-521; app/plan/page.tsx:196-225; components/DestinationStep.tsx:226-229` | BUG TODAY: create rebuilds from ids through resolveDestinations (lib/server/catalog.ts:429-443), which returns nothing for 'offmap:' ids. Hand-typed places show in the preview but are silently dropped from the saved trip, and an all-hand-typed trip fails with 400 'No plannable destinations'. The new planner_item must allow a free-text place with no reference id. |
| Get a suggested visiting order and apply it | With 2 or more mapped picks, sees 'Suggested route · N km' with rail, flight or overland legs and notes, and taps 'Apply this order' to reorder the stops. | Change | offline read |  | `lib/route.ts:122,146-232,314-421; components/map/MapExplorer.tsx:257-280,305-310; components/map/RoutePanel.tsx:27-150` | Keep the route domain in Planner, with transport speeds from reference. The default TRANSPORT is China's profile (route.ts:122), so a missing country silently assumes Chinese high-speed rail. Require the country's profile. Only CN has a researched railKmh, so under global-only every country gets overland/flight legs. Apply-order is the only way to reorder stops in the wizard. Only picks in the country currently open on the map are routed. Picks from another country and hand-typed places are counted as 'couldn't be placed on the map' and moved to the end on Apply (MapExplorer.tsx:264-280,305-310; RoutePanel.tsx:142-147), so a multi-country trip cannot be ordered as a whole. The rail glyph for non-flight legs is hard-coded (RoutePanel.tsx:101). **China-only part:** TRANSPORT default = CN profile route.ts:122; CN booking copy (12306) |
| Choose the arrival airport | In the route panel, types an airport name or code under 'Flying into' and picks a suggestion. The suggested route then starts near it. | Change | offline read | `planner.editGateways` | `components/map/RoutePanel.tsx:40-63; components/trip/AirportPicker.tsx:59-128; components/trip/AirportInput.tsx:152 (GET /api/airports/search); app/api/airports/search/route.ts; app/plan/page.tsx:29,314,443-444; components/DestinationStep.tsx:34-36` | Keep in Planner's gateways. The picker renders only when a route exists (at least 2 mapped picks), so a single-city trip cannot choose an arrival airport in the wizard, and departure cannot be chosen at all (the server stamps it). Show gateways for every trip. Clearing the box sets arrival to null, which the page sends as no arrivalAirport (page.tsx:314), so the server stamps its default. The wizard has no way to say 'no arrival airport'. Airport search is a server route over the worldwide airports artifact. Cache it or ship a slim index for offline. |
| Add arrival, city-hop and departure items | Sees 'Arrive in X' on day 1, a hop item when changing city, and an evening or afternoon departure on the last day. | Change | — |  | `lib/itinerary.ts:185-186,209-249; lib/countryData/cn.ts:92-101; lib/countryData/neutral.ts:93-108` | Keep the logic and take the copy from the reference transport profile. China's hand-written copy ('High-speed rail or flight to {city}', passport-to-board note) retires with the China profile. **China-only part:** CN_HOP_TITLE/NOTE/DEPARTURE cn.ts:92-101 |
| Evening item or dinner suggestion | Sees an evening activity or 'Dinner at a local spot — try <dish>' every night except the last. | Change | — |  | `lib/itinerary.ts:164-172,271-282` | Dishes come only from curated China foods. Everywhere else it says 'the local speciality'. Keep the evening slot and drop the dish unless a global food source is added. **China-only part:** dish list from curated foods |
| Boarding-pass header | Sees 'Your <country> itinerary', the route in capitals, season, day and traveller chips, and a country chop. | Change | offline read |  | `components/PlanStep.tsx:69-97,107-129; lib/countries.ts:27-28,43` | The headline must handle multi-country trips. The chop is shown only for CN (mark 同行, the only mark in countries.ts) and becomes an English passport stamp per the decision. Traveller chips come from members. **China-only part:** CN mark 同行 countries.ts:43 |
| Validate the trip on the server | Gets 'Invalid trip' or 'No plannable destinations in the selection' when the input is rejected. | Change | offline edits |  | `lib/server/schemas.ts:32-65; app/api/trips/route.ts:36-42,58-63; app/plan/page.tsx:199` | BUG TODAY: destinationIds are capped at 8 (schemas.ts:33) but the wizard never caps picks, so a 9+ place preview fails at create. Align the limits. Each id is also capped at 60 characters (schemas.ts:33), and a hand-typed place's id is 'offmap:' plus its slug (page.tsx:199) while the search box has no maxLength, so a name longer than 53 characters also fails the whole create with 'Invalid trip'. country defaults to 'CN' (schemas.ts:42) and must be required and derived. Validation moves into the command handlers. **China-only part:** country default "CN" schemas.ts:42 |
| Build the stored trip snapshot | Nothing. The server resolves destinations and stores plan, packing, foods and destination names as one JSON blob. | Change | offline edits | `planner.edit` | `lib/server/planService.ts:7-25` | Split it. Planner writes planner_day and planner_item rows (place → reference id, fractional order). Name and dates go to the platform trip, packing to Packing, and foods are dropped. Destination names come from reference. |
| Rebuild a trip's plan from new inputs | Nothing today. PATCH /api/trips/[id] can regenerate the plan (and rename or change the start date), but no UI calls it. | Change | offline edits | `planner.rebuild (owner/organiser)` | `app/api/trips/[id]/route.ts:18,44-116; lib/server/planService.ts:7` | Expose it as an explicit, confirmed 'Regenerate plan' command (planner.rebuild@1). It is destructive to planner items only; tickets and other apps' data survive. Rename and dates go to the platform trip API. LATENT BUG: the handler always calls buildTripData, falling back to the stored input (route.ts:87-94), so a rename-only or date-only PATCH also regenerates the whole plan, packing and foods, discards members' plan edits, and clears schedule checks (clearScheduleChecks, :104). This is why rename and dates must move out. |
| Activity shelf for the day builder | When editing a day, picks from unscheduled activities of that day's destination, or types a custom item. | Change | offline edits | `planner.edit` | `components/trip/PlanTab.tsx:131-145; lib/dayBuilder.ts:170-205` | The shelf offers activities only for curated (China) destinations. Every other stop gets just the custom row. It needs reference activities for any place id. **China-only part:** activities only from DESTINATIONS PlanTab.tsx:141 |
| Chinese copy and typography in the wizard | Sees 'Travelling together? 一起走', Kai-font local names, and 去过/已选 stamps. | Change | — |  | `components/PlanStep.tsx:245,374; components/DestinationStep.tsx:434,472,486; components/plan/PlaceSearch.tsx:513; components/map/CountryPlaceList.tsx:119; components/map/PlacePopup.tsx:85; components/map/SelectedPlaceCard.tsx:215` | English copy and English passport stamps. Endonyms render in the normal font (GeoNames rows carry no localName anyway). The font-kai local-name spans are also in the three shared map components, so the reference map widget needs the same change. The ※ tip bullet (PlanStep.tsx:245) is a CJK reference mark and can go too. **China-only part:** 一起走, 去过, 已选, font-kai, ※ |
| Rebuild the plan from new choices | Changes destinations, day count, season, party or interests and regenerates the itinerary and packing list. | Change | online only | `planner.rebuild` | `app/api/trips/[id]/route.ts:44-116; lib/server/planService.ts:7-25; lib/tripGateways.ts:67-76; lib/server/gatewayGuard.ts:19-28; lib/server/pgStore.ts:413-418` | Current rules: members only. Explicit and destructive: it replaces the plan (fresh item ids), packing list, foods and destination names, and deletes every item:/day: tick. Tickets, expenses, settlements, journal, currency settings and the briefing link survive because they live in other tables. Gateways missing from the new input carry over; unknown IATA codes are refused. A result with zero plannable days returns 400. Writes are version-guarded with 3 retries, then 409. No UI calls it today. New: a Planner command (planner.rebuild@1) behind a confirm step. It must stop regenerating Packing's list or touching ticks other apps own, and it runs on the worldwide reference/ pipeline. **China-only part:** The generator reads curated lib/data destinations (only Chinese cities). Tips come from the country profile, and only China's profile carries the hand-written CN_GENERAL_TIPS (lib/countryProfile.ts:174); lib/itinerary.ts:1 only re-exports them as GENERAL_TIPS. |
| See bookings on their day | Sees ticket chips (emoji, time, title) on the day a booking covers. A multi-day booking shows on every day it spans. | Change | offline read |  | `components/trip/PlanTab.tsx:207-211; lib/tickets.ts:8-31; components/trip/DayCard.tsx:81-94` | Only works when the trip has a start date. Spans are inclusive, sorted by date, then time, then title. Today PlanTab reads Tickets' rows directly. New: Tickets publishes a day-section contribution through the registry and Planner renders it, because features never import each other. |
| Move an item up or down within a day | Taps ↑ or ↓ on an item row. | Change | offline edits | `planner.edit` | `components/trip/DayCard.tsx:142-159; lib/planOps.ts:164-172` | Swaps the item with its neighbour; moving past either end does nothing, silently. There are no ops to move an item to another day, remove a day, reorder days or change a day's destination (planOps.ts:5-38). Proposed: planner.moveItem@1 taking a target day and index, which would also replace the chain of single-step swaps a drag sends today. |
| Add a day | Taps '+ Add day', optionally picking which of the trip's destinations it is in (default: same as the last day). | Change | offline edits | `planner.edit` | `components/trip/PlanTab.tsx:119-125,147-154,226-260; lib/planOps.ts:68-90; lib/server/schemas.ts:85` | Appends day N+1 with no items. The UI offers only destinations already on the plan, but the API accepts any catalog id and refuses unknown ones with 'Unknown destination'. input.days is not updated, so the shell header's day count goes stale (AppShell.tsx:72-74); the hero and guest views read the plan length instead. addDay has no ceiling, but every per-day op is capped at day 60 (DayNumberSchema, schemas.ts:85), so a day added past 60 cannot be edited. New: day rows. Adding a day at any position, removing days and reordering days are all missing today. |
| Tick an itinerary item done | Checks an activity or custom item. It is struck through with 'done by <name>'. Any member can untick it. | Change | offline edits | `planner.tick` | `components/trip/DayCard.tsx:352-372; lib/useTripPayload.ts:182-201; lib/tripPayloadCore.ts:35-44; app/api/trips/[id]/checks/route.ts:8-34; lib/tripShared.ts:351-354` | The tick shows immediately, before the server confirms. It is stored in the shared `checks` table under key 'item:<id>' with checked_by as a NAME. The server accepts any key of 1-200 characters without checking it names a real item. The last person to tick wins, and unticking deletes the row. New: a Planner-owned item_done table with FKs to the item id and member id, written by command planner.tick@1. |
| Read tips and the coverage note | Reads the 'Good to know' block: trip tips saved at creation plus a live note on how complete our data is for this country. | Change | offline read |  | `components/trip/PlanTab.tsx:82-94,265-287; components/plan/GapNote.tsx; lib/itinerary.ts:298-315; lib/countryProfile.ts:174,194,209` | plan.tips is frozen at generation, while the gap note is resolved live from the country profile. Tips are the profile's tips (neutral tips plus Wikidata fact templates for most countries), plus one line per curated destination's seasonNote, plus a kids tip. New: tips come from reference/ country facts through the one global pipeline, and the gap note keeps reading live coverage. **China-only part:** Only China's profile has hand-written tips (CN_GENERAL_TIPS, lib/countryProfile.ts:174). The per-city season lines come from curated destination seasonNotes (lib/itinerary.ts:308-311), which only the Chinese curated cities have. |
| Add a suggested place from the shelf | Taps + next to an unscheduled activity for that day's destination, and it is appended to the day. | Change | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:450-514; lib/dayBuilder.ts:106-108,170-205,322-341; components/trip/PlanTab.tsx:131-145` | The shelf holds the target day's destination's curated activities, minus titles already on any day at that destination (matched by folded title), minus adds still in flight. The slot comes from the activity's time of day ('day' and 'any' map to morning). The server does not prevent duplicates. New: suggestions from reference/ worldwide POI data, the same way for every country. **China-only part:** Activities come only from the curated lib/data DESTINATIONS (PlanTab.tsx:141), which are Chinese cities, so every other trip's shelf holds only the custom row. |
| Give an item a time | Taps 'Set a time' on an untimed item, and it becomes a 09:00, 1-hour block. | Change | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:41-43,657-672; lib/dayBuilder.ts:343-365; lib/planOps.ts:141-151; lib/server/schemas.ts:93-94` | Generated plans have no timing (buildItinerary never sets startMinutes), so this is how every block starts. The server accepts start 0-1439 and duration 1-1440 (schemas.ts:93-94); the UI floor is 15 minutes (lib/timeline.ts:23). Sent as setTiming with both halves. There is no control for choosing a start time: only the 09:00 default, then ±15 min on the length. A start-time picker is probably needed. |
| Drag a block to reorder (desktop) | Drags a block's header with the mouse to a new position. Esc or leaving the window cancels. | Change | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:187-202,210-262,291-308; lib/dragLayer.ts:26-72` | Mouse only: pointerType is checked, so there is no touch drag. The drop is sent as N single-step moveItem ops because there is no insert-at-index op, so a rejection partway leaves a half-finished reorder. Polling pauses during the drag. Dragging a block to ANOTHER DAY does not exist. New: a single move-to-index command, including across days, and touch drag for the iPhone PWA. |
| Drag a shelf place onto a day | Drags a shelf row with the mouse onto the day's list, or onto another day's chip, to add it there. | Change | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:160-193,210-230,402-447,493` | Mouse only, like block drag (same handleProps, DayBuilder.tsx:217). Chips for days at a different destination are dimmed and refuse the drop, because the shelf row would not exist there. A drop always appends at the end. Uses the same path as the + button. New: touch drag for the iPhone PWA and insert at the dropped position. |
| View the route map | Switches to 'Route' and sees the trip's stops in day order on the country map, coloured by season fit for the travel month. | Change | offline read |  | `components/trip/RouteMap.tsx:95-204,214-436; components/trip/PlanTab.tsx:195-199` | Read-only (markers keep hover only). Stops that are not curated are resolved through /api/destinations/resolve, which answers at most 12; any beyond that are silently left off the map. The month comes from the start date, otherwise the first month of the season in the country's hemisphere. Province geometry and climate are fetched per country, and failures fall back to the stop list. New: Planner embeds the reference/ map widget and draws stops from Planner rows that store coordinates. **China-only part:** Stops are looked up in curated DESTINATIONS (RouteMap.tsx:120-125,151-156). For CN the climate shard is not fetched (RouteMap.tsx:370); season fit comes from curated bestSeasons or the hand-made REGION_MONTHS table via regionFit (components/map/mapTypes.ts:70-73,234). The ChinaLevel branch still receives noop callbacks (RouteMap.tsx:36-42,78-83). |
| Print the itinerary | Uses the browser's own print. Controls hide, while the gateways line and every day card print. | Change | offline read |  | `components/trip/DayCard.tsx:141,186,202; components/trip/GatewaysStrip.tsx:105-109,193; components/trip/PlanTab.tsx:162` | There is no print button, only print:hidden styling. The Briefing is the better print surface; decide whether Planner keeps its own print styles. |
| Get a suggested visiting order and travel times | With 2 or more places picked, sees "Suggested route · N km", optionally starting near the arrival airport. It shows the numbered order, each leg as ✈️/🚄 with hours, "km overland" or "transfer", notes (legs worth flying, no airport within 150 km, booking advice) and a count of picks that couldn't be placed. Taps "Apply this order". | Change | offline edits | `planner.edit` | `components/map/RoutePanel.tsx:27-149; lib/route.ts:1,122,146-260,314-422; components/map/MapExplorer.tsx:257-310; lib/countryData/transportDefaults.ts:253-283; lib/countryData/cn.ts:22,82-85` | China is the only country with a rail speed (CN_RAIL_KMH=230, cn.ts:22, used in countryBaseProfile.ts:217). So only China ever shows 🚄 legs and the "High-speed rail seats open about 15 days ahead on 12306 or Trip.com" copy (cn.ts:83). Also, route.ts:122 exports TRANSPORT built from DEFAULT_COUNTRY, which is China. Use the neutral transport profile (flight/overland) for every country. "Apply this order" becomes a Planner reorder command. The route maths stays a pure function in the Planner domain, and the flight constants stay country-neutral. Picks made in another country count as "couldn't be placed" and go to the end of the order (MapExplorer.tsx:264-280). A multi-country trip needs a real answer here, not that fallback. **China-only part:** Rail legs, the 🚄 glyph and the 12306 booking copy exist only for CN; the default TRANSPORT is China's. |
| Set the arrival airport from the route panel | Types an airport name or code in "Flying into", and the suggested route then starts from the place nearest that airport. | Change | offline edits | `planner.edit` | `components/map/RoutePanel.tsx:47-56; components/trip/AirportPicker.tsx; components/map/MapExplorer.tsx:41-48,272; lib/route.ts:295-345` | This is the Planner gateways feature reached from inside the map. The map widget imports components/trip/AirportPicker, which is a cross-feature import. Setting the arrival gateway should be a Planner command, and the map should get the gateway through a Planner contribution. Picking from the trip country's cached airports can work offline; free-text search across the world needs the server. |
| See the trip's route on the Plan tab map | On a saved trip's Plan tab, sees a read-only country map of the stops in order, coloured for the trip's month, or "None of this trip's stops can be drawn on a map yet" when none resolve. | Change | offline read |  | `components/trip/RouteMap.tsx:5-18,81,121,153,194-216,251,289,361-434; components/trip/PlanTab.tsx:199; components/map/CountryMap.tsx:79-89 (readOnly); components/map/CountryLevel.tsx:698-707` | Planner mounts the reference map widget in read-only mode. The month comes from the trip's start date or season (routeMonth). China-only parts: CN skips climate (RouteMap.tsx:370), and stops resolve against DESTINATIONS (RouteMap.tsx:18,121,153). It also fetches /api/destinations/resolve, which should read the reference city table instead. Two defects to leave behind. (1) Hover is wired to noop, so this map has no tooltip, although the read-only mode supports one. (2) The place list under the map still renders pressable chips wired to noop. Today can reuse this map through the registry. **China-only part:** Skips the CHELSA climate for CN; resolves curated China destinations. |
| Open the country picker from the wizard | On the wizard's "Where to this time?" step, taps the 🌍 country button ("Change country — planning in X") to jump back to the world picker from either the map or the card view. | Change | offline read |  | `components/DestinationStep.tsx:57-111,262-289,371-387; app/plan/page.tsx:50-57,276,425` | Planner keeps a way to change which country it is browsing, but mounts the reference world widget for it. The browsing country is separate from the trip's country, which is the first pick's country (tripCountryFromPicks). The new model should make that explicit: a required trip country, or a trip spanning several countries, rather than an inferred one. Explore gets its own country switcher on the same widget. Both need a neutral starting country, not "CN". **China-only part:** The browsing country starts and resets to "CN" (app/plan/page.tsx:57,330). |
| See and remove picked places under the search | Sees the picked places as chips under the search box (with a short description once fetched, and ○ for hand-typed ones) and taps × to remove one. | Change | offline edits | `planner.edit` | `components/plan/PlaceSearch.tsx:529-577; components/DestinationStep.tsx:126-203,252-255` | This is the Planner's stop list in the wizard. Remove becomes planner.removeStop@1. The chip model has China-only branches (curated DESTINATIONS lookup, catalog localName) that go with the curated list. May overlap the planner slice, so dedupe by name. |
| Move between wizard steps | Steps through Trip details, Destinations and Your plan, and can jump back to any finished step from the progress bar. | Keep | — |  | `app/plan/page.tsx:338-368,375,402,447; lib/wizard.ts:11` | Keep as planner UI. Wizard state is React-only (page.tsx:21-98) and is lost on refresh. Save it as an offline draft in IndexedDB. Each step is mounted only while it is current, so going back to Destinations remounts it: the map returns to the globe (DestinationStep.tsx:74), the view to Map (:57), and the month timeline to October (MapExplorer.tsx:51,72), even when the user already opened a country or picked a month. The draft should keep the map level, open country and month. |
| Back / Next / Build my plan footer | Uses Back and Next (or 'Build my plan' on step 2) and sees a summary such as '5 days · 2 travellers' or '3 destinations selected'. | Keep | — |  | `app/plan/page.tsx:459-504; lib/wizard.ts:75-87 (canAdvance: days>=1, step 1 needs at least 1 pick)` | Keep. The gate logic is pure and already tested (lib/wizard.test.ts). |
| Pick interests | Toggles any of 11 interests (food, history, nature, beach, theme parks, arcades, shopping, nightlife, museums, hiking, family), or none to get must-sees only. | Keep | — |  | `components/DetailsStep.tsx:123-151; lib/meta.ts:6-18; lib/types.ts:3-14; app/plan/page.tsx:132-135` | Stays a planner-owned per-trip setting and generation input. Packing's interest-based gear can no longer import it (see the packing row). |
| See and remove picked places | Sees picks as chips (with a short description when known, and a no-pin marker for hand-typed ones) and taps x to remove one. | Keep | — |  | `components/plan/PlaceSearch.tsx:530-580; components/DestinationStep.tsx:126-203,252-255; app/plan/page.tsx:121-124,186-189` | Keep as the planner draft. Catalog chips are re-stamped with whatever country is open now (DestinationStep.tsx:172-198). Storing reference ids that carry their own country removes that. |
| Live 'does it fit' counter | While picking, sees e.g. '5 cities · 12 nights needed · 7 days set — 5 over' or 'N spare'. | Keep | offline read |  | `components/plan/FeasibilityCounter.tsx:18-52; lib/feasibility.ts:17,29,58-93; components/DestinationStep.tsx:210-219,362` | Keep as a planner domain function. Once the researched curated ranges retire, every place uses the 2-night catalog floor, and hand-typed places use 1-2 nights. The counter and the generator already disagree: the counter gives every catalog or GeoNames pick at least 2 nights (DestinationStep.tsx:216 sends [1,3], raised to [2,3]), but the generator gives a GeoNames city 1-2 days (lib/server/catalog.ts:414). So the counter can say 'over' for a plan that generates fine. Use one nights source for both. **China-only part:** researched suggestedDays exist only for curated China cards |
| Generate the day-by-day plan (preview) | On 'Your plan' sees each day's morning, afternoon and evening items built in the browser. | Keep | offline read |  | `components/PlanStep.tsx:29-41,160-185; lib/itinerary.ts:174-295` | A pure planner domain function that runs on both client and server. Item ids come from newId() (itinerary.ts:288), so the preview and the saved plan differ. For an offline create command, send the generated items or make generation deterministic. |
| Score activities for the group | Nothing. Activities matching interests rank higher (+3 each). Must-sees get +2.5 and best-season +1. With kids, family/theme park get +2 and nightlife -1. Avoided seasons are excluded. | Keep | — |  | `lib/itinerary.ts:69-70,95-107` | Keep. mustSee, bestSeasons and avoidSeasons exist only in curated China data and catalog attractions, so outside China only interest overlap matters. Kids>0 should come from members' child flag. **China-only part:** season/must-see signals only in China data |
| Split days across destinations | Nothing. Days are shared out in proportion to each place's suggested stay, at least one day each. Places beyond the day count are dropped. | Keep | — |  | `lib/itinerary.ts:114-137,188-190` | Keep. The preview header still lists dropped places in its route line (PlanStep.tsx:118). Show a warning instead. |
| Fill slots with activities or free time | Sees full-day or half-day activities in open slots, or 'Free time — wander X at your own pace'. | Keep | — |  | `lib/itinerary.ts:139-162,251-267` | Keep as is. |
| Print / save the plan as PDF | Taps 'Print / save as PDF' to print the plan with the data credit. | Keep | — |  | `components/PlanStep.tsx:138-144; app/plan/page.tsx:506-524` | Keep in Planner (Briefing may also offer it). contracts.test.ts C7 pins that the credit is never inside print:hidden. |
| Refuse unknown airport codes | Gets 'Unknown airport code XXX' if a gateway is not a known airport. | Keep | offline edits | `planner.editGateways` | `lib/server/gatewayGuard.ts:19-28; app/api/trips/route.ts:47-48` | Becomes validation inside Planner's gateway and generate commands. Rejected commands go to 'Needs attention'. |
| Auto-fill arrival and departure airports | Nothing. Gateways they did not choose are set to the main airport nearest the plan's first and last stops. | Keep | offline edits |  | `app/api/trips/route.ts:64-78; lib/gatewayDefaults.ts:36-61; lib/mainAirport.ts:104-109` | Keep in Planner. It uses worldwide airports from reference. Hand-typed stops have no coordinates, so they give null. |
| View the day-by-day itinerary | Reads each day card (Day 01, destination, items with slot or 'All day', time, note, kind emoji). Today's card is highlighted with a TODAY badge. | Keep | offline read |  | `components/trip/PlanTab.tsx:207-225; components/trip/DayCard.tsx:67-228,337-394; components/TripView.tsx:219,431-438` | DayCard is also the renderer for print. The travel-hop glyph comes from the country's rail speed (PlanTab.tsx:102). Day cards show no calendar date even when the trip has a start date (DayCard.tsx:72-78); only Today's plan and the briefing print dates. Today the plan is one JSON blob in trips.data. New: Planner-owned day and item rows with ids, and each day shows its date. |
| Add an item to a day | Taps '+ Add item' on a day card, enters a title, time of day, optional time text and note, and taps Add. | Keep | offline edits | `planner.edit` | `components/trip/DayCard.tsx:185-223,260-335; lib/planOps.ts:96-108; lib/server/schemas.ts:96-107; app/api/trips/[id]/plan/route.ts:16-72` | Rules: title 1-80; slot morning, afternoon or evening; time is free text up to 20; note up to 200; kind 'custom'. The item is appended at the end of the day and the server mints its id. A timing pair must be both set or both absent. Becomes planner.addItem@1 with a client-minted id, which offline follow-up ops need. |
| Edit an item | Taps ✎ on an item, changes title, slot, time or note, and saves. | Keep | offline edits | `planner.edit` | `components/trip/DayCard.tsx:103-130; lib/planOps.ts:110-139` | null clears time or note. Changing the slot drops fullDay. Setting one half of a timing pair on an item that lacks the other half is refused. Any member can edit any item, generated ones included. Becomes planner.updateItem@1. |
| Remove an item | Taps ✕ on an item and it disappears. There is no confirm step. | Keep | offline edits | `planner.edit` | `components/trip/DayCard.tsx:171-177; lib/planOps.ts:153-162; app/api/trips/[id]/plan/route.ts:61-63` | Removing an item that is already gone counts as success. The item's done-tick is deleted in a second write that is not atomic with the first. New: one transaction, with tick rows holding an FK to the item and cascading. |
| Open the day builder | Taps 'Build' (members only) to get a shelf of places beside the chosen day's time blocks. | Keep | offline read | `planner.edit` | `components/trip/PlanTab.tsx:62,107-117,127-129,164-192; components/plan/DayBuilder.tsx:102-387` | Once opened, it stays mounted but hidden so queued ops keep sending. It is a third view beside Days and Route. Items cannot be edited, removed or ticked from the builder, only from the day cards. |
| Choose the day new places go to | Taps a 'Day 03' chip under 'Adding to'. That day's blocks are then shown beside the shelf and new places are added to it. | Keep | — |  | `components/plan/DayBuilder.tsx:106,338-346,402-447; lib/dayBuilder.ts:152-156,316-317` | Local UI state only, clamped to 1..number of days (60 at most). The same chip picks both which day the builder shows and where adds go; the shelf follows that day's destination. |
| Add your own place from the shelf | Types into 'Something else' and presses Enter or +. | Keep | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:516-553; lib/dayBuilder.ts:196-203,322-341` | Trimmed and capped at 80 characters, slot morning. The draft clears after adding and focus stays in the input. Uses the same addItem command. |
| Lengthen or shorten a block | Taps −15m or +15m on a timed block. | Keep | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:627-644; lib/dayBuilder.ts:394-422; lib/timeline.ts:20-23; components/plan/useDayBuilder.ts:100-133` | Clamped to 15-1440 min and resends the existing start. Ops go one at a time because they carry absolute values computed from the on-screen state; the outbox's first-in-first-out order preserves that. |
| Remove a block's time | Taps 'Untime', and the item goes back to its morning, afternoon or evening band. | Keep | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:645-655; lib/dayBuilder.ts:367-392` | Both halves are cleared together; a half-cleared pair is never sent. |
| Reorder blocks with the arrow buttons | Taps ↑ or ↓ on a block in the builder. | Keep | offline edits | `planner.edit` | `components/plan/DayBuilder.tsx:674-702; lib/dayBuilder.ts:424-450` | Sends the same moveItem op as the day card. Moves past either end are refused locally, and focus stays on the control. |
| See the day's load and overlaps | Reads '9h 40m planned · 2 gaps', plus 'pushed' and 'past midnight' badges on blocks that an overlap moved later. | Keep | offline read |  | `components/plan/DayBuilder.tsx:105-110,343-352,611-623; lib/timeline.ts:60-87,122-155` | reflow runs only when reading and is never written back. Untimed items count as zero. |
| See the gateway airports | Reads '✈️ Fly in via LIM · out via CUZ', or 'not set', above the plan. The line also prints. | Keep | offline read |  | `components/trip/GatewaysStrip.tsx:35,169-199; lib/tripGateways.ts:27-32` | Stored in trips.data.input. Absent and null both read as not set. |
| Edit the gateway airports | Taps 'Edit gateways', searches for or types a 3-letter code for arrival and departure (or clears them), and saves. | Keep | offline edits | `planner.edit` | `components/trip/GatewaysStrip.tsx:36-168; components/trip/AirportPicker.tsx:59-130; components/trip/AirportInput.tsx:22-23,133-176; app/api/trips/[id]/gateways/route.ts:25-72; lib/server/gatewayGuard.ts:19-28; lib/tripGateways.ts:39-48` | Has its own route that writes only the input, never the plan or ticks. Both keys are required and null clears. An unknown code returns 400 'Unknown airport code'. Save is blocked while a field holds text that names no airport. Writes are version-guarded with retries. Search is server-side (/api/airports/search, at least 2 characters, 300 ms debounce). Offline: the code is checked when the command is applied (a rejection lands in Needs attention), or against cached reference airports. |
| Default gateway airports | Gets arrival and departure pre-filled with the main airport of the first and last stop when none were chosen. | Keep | — |  | `lib/gatewayDefaults.ts:36-61; lib/mainAirport.ts:104-109; app/api/trips/route.ts:64-77; lib/tripGateways.ts:67-76` | Only absent fields are filled; null means the traveller chose none. A rebuild keeps the old codes rather than stamping new ones, so a default can go stale when the first or last stop changes. Searches every airport worldwide, arrivable sizes only, within a radius cap. Already global. |
| Browse curated destination cards | Switches to Cards view with the Map/Cards toggle, filters by region chips, and taps a card (emoji, tagline, known-for, best seasons, day range) to select it. | Drop | — |  | `components/DestinationStep.tsx:84-97,290-336,395-418,455-526; lib/data/index.ts:7-16; lib/data/{east,north,south,west}.ts` | These are the hand-written China destinations (16 of them) with China region chips. The card label hard-codes '{region} China' (:489) and the selected card shows an 已选 stamp (:472). Retired by the global-only decision. The Map/Cards toggle (:290-317) and the 'No cards for X yet' empty message (:408-416) go with it, which leaves the map as the only browse view. **China-only part:** entire feature; '{dest.region} China' :489; 已选 :472; font-kai localName :486 |
| Eat your way through (foods) | Sees dish chips per destination beside the plan. The saved trip stores them for later tabs. | Drop | — |  | `components/PlanStep.tsx:218-236; lib/server/planService.ts:21-23` | Foods exist only on the curated China destinations. Catalog and GeoNames destinations have foods: [] (catalog.ts:379,413), so outside China the preview shows empty cards and the server stores nothing. **China-only part:** entire data source |
| Old trips upgraded when opened | Invisible to the user: opening an old trip adds item ids to its plan and moves index-based ticks to id-based ones. | Drop | — |  | `lib/server/store.ts:64-95; lib/server/migrate.ts:16-42` | The new Postgres starts empty, so there are no legacy shapes to upgrade. The tripCountry '?? CN' backfill and the 'absent pivot means CNY' rule go too. **China-only part:** tripCountry treats a missing country as 'CN' (lib/tripCountry.ts:38). |
| Curated China destinations | Sees 16 hand-written China places (Beijing, Xi'an, Shanghai and others) as bigger always-labelled pins, each with an emoji, tagline, activities, best seasons, season notes and monthly highlights. Once marked visited, they drop off the map. | Drop | — |  | `lib/data/{north,east,south,west}.ts (16 entries, all country "CN"), lib/data/index.ts; components/map/explorerPlaces.ts:41-81; lib/months.ts:255-340 (CURATED_HIGHLIGHTS, highlightFor); lib/curatedNames.ts; components/trip/RouteMap.tsx:18,121,153; lib/server/catalog.ts:432-433 (resolveDestinations curated branch)` | Retired by owner decision. Also remove the curated MapPlace kind, the marker and fit branches for it, the shard-twin suppression in curatedNames, the curated branch in resolveDestinations, and the visited filter that only applies to these places. **China-only part:** All of it. |
| Browse curated destination cards and mark places visited | Switches the destination step to "🎴 Cards", filters by China region, taps a card to pick it (stamped 已选), marks places as "been" so they drop out (stamped 去过), and restores them from "Already been". | Drop | — |  | `components/DestinationStep.tsx:57-123,290-336,395-450,455-520 ("{region} China" line at :489, stamps at :434,:472); app/plan/page.tsx:126-130 (toggleVisited)` | Only China's 16 curated places have cards, and other countries see "No cards for X yet". The cards carry Chinese-character stamps, the Kai font, a "{region} China" line and China region filters. Drop the view with the curated list. If a "been there" memory is wanted later, it belongs to Journal or the user's profile as a worldwide feature, not a wizard filter. **China-only part:** All of it: cards exist only for CN; 已选/去过 stamps; China region filter. |

#### MAP · Explore

_The world map as its own app_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Street map with place markers | Browse a street-level map (MapLibre with the Protomaps world file on R2) with clustered markers for catalogue, group and shared places. A list view mirrors the clusters. | New · first release | offline read |  | `read-only` |  |
| Place detail sheet | Open a place to see open-data facts, your group's notes, and shared tips, photos and ratings, with the sources credited. | New · first release | offline read |  | `read-only` |  |
| Download a trip map pack | Save the map for a trip's countries and cities to use offline. Tokyo plus Kyoto is about 28 MB at street level. | New · first release | online only |  | `read-only (downloads tiles)` |  |
| Filter the map | Filter by category, or show only your group's places or only shared ones. | New · first release | offline read |  | `read-only` |  |
| Add from the map | Every contribute, share, rate and report action starts here, using the Places platform commands. | New · first release | offline edits | `places.contribute` | `place.* commands` |  |
| Offline map packs on the phone | Download the map for a trip's countries and cities in the native app. The website doesn't offer offline packs. | New · first release | online only |  | `native only (tile Worker)` |  |
| Globe or flat world map | Toggles the world picker between a globe and a flat map; the choice is remembered. | Change | offline edits |  | `lib/prefs.ts:23-35,37-42; components/map/WorldPane.tsx:66,117; lib/server/schemas.ts:352-355` | Move it out of the platform UserPrefs into Explore's own user-space setting, since each feature owns its settings. Today every map-only pref must also be listed in PrefsSchema or the server silently strips it (components/map/MapExplorer.tsx:94-100). |

#### MNY · Money

_Group ledger across trips_

Where its data lives: group ledger. Expenses and settlements with no trip live in the GROUP space. Trip-tagged ones live in that TRIP's space, so a trip-only guest sees only their trip's rows. money_settings lives in the GROUP space. Balances are computed on the fly over every space the viewer syncs.

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Add expense | Title, date, category, amount in minor units, currency, the payer's member id, split, FX snapshot and notes. The trip tag defaults to the switcher's trip; 'No trip' makes it group-wide. The default split is the trip's travellers, or the active group members when there is no trip. The row lands in the local replica immediately and is sent from the outbox. | New · first release | offline edits | `money.add (default owner, organiser, member; ⇄ per trip)` | `money.addExpense@1` |  |
| Split modes: equal, shares, percent, exact | Equal among the people selected. Shares uses integer weights, for example 2 for a couple. Percent uses basis points summing to 10,000. Exact uses minor units summing to the total. The client sends only the inputs. The server recomputes share_minor with the shared function (largest remainder, ties broken in member-id order so every device agrees) and stores both. | New · first release | offline edits | `money.add` | `part of money.addExpense@1 / money.editExpense@1` |  |
| Multi-currency with FX snapshot | At entry the phone fills the rate from the expense currency to the group's ledger currency. It uses the live rate from /api/money/rates when online, reusing resolveRates with its last-good fallback (lib/rates.ts:184-188,243). Offline it uses the last cached rate (source 'cached'). Otherwise the user enters a rate or leaves it blank, and a blank row is shown 'unconverted', never dropped, as convertedTotals does today (lib/money.ts:63-64). The rate is frozen on the row. | New · first release | offline edits | `money.add (own rows) / money.editAny` | `part of money.addExpense@1; money.setRate@1 to fill or override later` |  |
| Edit expense | Carries baseVersion. If someone else changed the row since then, the command is rejected ('Mei changed this expense while you were offline') and goes to Needs attention, so money is never silently overwritten last-writer-wins, which today's routes do (expenses/[expenseId]/route.ts:38-63). | New · first release | offline edits | `author with money.add; anyone else needs money.editAny (default owner, organiser; member ⇄)` | `money.editExpense@1` |  |
| Delete expense | Soft delete carrying baseVersion. Its shares drop out of every balance. | New · first release | offline edits | `as Edit expense` | `money.deleteExpense@1` |  |
| Balances | Per currency and group-wide across all trips, with a trip filter. They are computed by the one shared function, never stored. People who left keep appearing until they reach zero. A trip-only guest sees only their trip's balances. | New · first release | offline read |  | `read-only` |  |
| Settle-up suggestions | Minimum transfers computed WITHIN each currency (today's greedy settleUp, lib/money.ts:380-405), labelled 'suggested'. They are never stored and never convert between currencies, which is Splitwise's rule too. | New · first release | offline read |  | `read-only` |  |
| Record a settlement (full or partial) | Prefilled from a suggestion with an editable amount, so a partial payment is just a smaller number. There is also free-form 'Record a payment': any payer, any payee, any amount. It carries a trip tag and must be trip-tagged when a trip-only guest is a party. | New · first release | offline edits | `money.settle (default owner, organiser, member)` | `money.settle@1` |  |
| Delete settlement | Undoes a recorded payment. It is a soft delete. | New · first release | offline edits | `recorder, or money.editAny` | `money.deleteSettlement@1` |  |
| Totals and category breakdown | Spending per trip or for the whole group in the ledger currency, using each row's own snapshot, plus the by-category chart that exists today (components/trip/MoneyTab.tsx:133-140). | New · first release | offline read |  | `read-only` |  |
| Ledger currency | The group's display and settlement-summary currency, like Settle Up's convertedToCurrency. Changing it never reinterprets old rows: each row keeps its fx_base, and totals list rows with a different base as unconverted until someone re-snapshots them. This is the lesson of the CNY pivot defect documented at lib/tripShared.ts:141-187. | New · first release | offline edits | `money.settings (default owner, organiser)` | `money.setLedgerCurrency@1` |  |
| Categories | A fixed global list: today's six are food, transport, lodging, tickets, shopping and other (lib/server/schemas.ts:255-262). Each shows an emoji. | New · first release | offline edits | `money.add` | `part of money.addExpense@1` |  |
| Export CSV | Generated on the phone from the local replica, so it also works offline. It contains expenses with a column per person's share, FX and converted amounts, and settlements, filtered to a trip or the whole group. It doubles as a personal backup. Today lib/csv.ts is only a parser. | New · first release | offline read | `money.view` | `read-only` |  |
| Stamp the trip's currency | Nothing. The trip's rate pivot is set from its country (CNY for China, the facts currency elsewhere, none if unknown). | Change | — |  | `app/api/trips/route.ts:82-92; lib/tripShared.ts:226-231; lib/countryProfile.ts:173,256-258` | Money owns this. It derives currencies from the platform trip's countries plus reference currency facts. Trip creation must not write Money data, and the CNY default goes. **China-only part:** currency 'CNY' via chinaProfile countryProfile.ts:173 |
| Add an expense | Taps '+ Add expense' and enters what it was, date (default today), amount, currency (trip or home quick pick, or any 3-letter code), who paid (default me), category, who to split among (default everyone) and notes. | Change | offline edits | `money.add` | `components/trip/ExpenseForm.tsx:65-229; components/trip/MoneyTab.tsx:80-83,246-261; app/api/trips/[id]/expenses/route.ts:10-61; lib/server/schemas.ts:248-281; lib/money.ts:278-287` | Rules: the amount is parsed using the currency's decimals (0, 2 or 3; extra decimals are refused) and must be 1..100,000,000 minor units. Currency is any 3 letters. paidBy and splitAmong must be current member NAMES, checked by reading first and writing after. At least one person in the split, equal split only, 6 fixed categories. New: money.addExpense@1 using member ids, written to the GROUP ledger with a trip tag, with the FX snapshot captured at entry and integer minor units plus currency. **China-only part:** The Transport category emoji is 🚄 (ExpenseForm.tsx:21). |
| Edit an expense | Taps Edit on an expense row, changes any field and saves. | Change | offline edits | `money.editAny` | `components/trip/MoneyTab.tsx:269-280,295-300; app/api/trips/[id]/expenses/[expenseId]/route.ts:15-66` | Any member can edit anyone's expense. The merged result is re-checked against member names; addedBy and createdAt are kept. No version guard, so the last write wins. New: the 'edit anyone's expense' switch (owner and organiser yes, member switchable), a version per row, and conflicts sent to Needs attention. |
| Delete an expense | Taps Delete on an expense row. There is no confirm step. | Change | offline edits | `money.editAny` | `components/trip/MoneyTab.tsx:147-151,301-304; app/api/trips/[id]/expenses/[expenseId]/route.ts:68-82` | Any member; hard delete. New: deleting someone else's row is gated by money.editAny. Consider undo or soft delete, since balances now carry across trips. |
| See converted totals | With a home currency set, reads 'Total <pivot>' and 'Total <home>'. Currencies without a rate are listed as left out. | Change | offline read |  | `components/trip/MoneyTab.tsx:89-104,175-198; lib/money.ts:56-141; lib/tripShared.ts:159-190` | Converts with hand-entered rates relative to a per-trip pivot (the trip country's currency). Balances are never converted. New: convert each expense with its own FX snapshot into a user or group home currency, and keep listing unconverted currencies honestly. **China-only part:** An absent pivot means legacy CNY (lib/tripShared.ts:187-190), and convertedTotals defaults its pivot to 'CNY' (lib/money.ts:107-111). |
| Set home currency and conversion rates | Opens 'Set up converted totals' or 'Edit conversion rates', enters a home currency and '1 XXX = n <pivot>' for each currency used, and saves. | Change | offline edits | `money.rates` | `components/trip/MoneyTab.tsx:205-214,319-440; app/api/trips/[id]/currency/route.ts:12-61; lib/tripShared.ts:140-157,226-275; lib/server/schemas.ts:320-329` | Stored per TRIP (trip_settings), with one home currency shared by every member. The pivot is stamped at creation from the country's researched currency and can never be edited by the client. The one exception: a null pivot adopts the first home currency anyone sets. Rates are positive floats, and saves are version-guarded. New: home currency per user (or per group), and an FX snapshot on each expense (rate, source, date) replaces the mutable rate table so old totals never reprice. **China-only part:** Placeholders 'SGD' and '5.20' (MoneyTab.tsx:400,417). |
| View today's exchange rate | Opens 'View today's exchange rate' and reads pivot↔home and pivot↔other-currency rates with an 'as of' time and a provider attribution. Sees a stale warning when the providers are down; with no home currency, members get a 'Set it now' link to the currency editor. | Change | online only |  | `components/trip/Rates.tsx:111-303; app/api/rates/route.ts:69-99; lib/rates.ts:181,243-291` | Display-only by design (J-C2: never used in totals), and only for signed-in users. Sources in order: er-api, then the jsDelivr CDN, then an in-memory last-good copy; revalidated hourly. New: can supply the default FX snapshot for a new expense, recording the date the rate was fetched. Still online-only. |
| See who owes whom | Reads balances per currency: '<name> is owed X' or '<name> owes X'. | Change | offline read |  | `components/trip/BalancesCard.tsx:94-132; lib/money.ts:312-372` | net = paid − share + repayments sent − repayments received. Equal split with largest remainder: the first shares get +1 minor unit. splitAmong [] means all CURRENT members. Unknown names are kept. Balances are per currency and NEVER converted; zero balances are hidden. New: one shared TS balance function over the group ledger, carried across trips, filterable by trip, keyed by member id. |
| Record a repayment (full or partial) | Taps 'Mark repaid' on a suggestion, optionally lowers the amount for a partial repayment, and confirms. | Change | offline edits | `money.settle` | `components/trip/BalancesCard.tsx:70-84,144-166; app/api/trips/[id]/settlements/route.ts:10-60; lib/server/schemas.ts:283-293` | Only possible from a suggested transfer; there is no free-form 'X paid Y' form. The amount is editable (partial, or even more than owed), the date is forced to today, from must differ from to, both must be member names, and recordedBy is stored. New: money.settle@1 using member ids, for any pair, with an editable date. At group level, a repayment can clear balances carried over from earlier trips. |
| Spend by category chart | Sees a bar chart of spend by category for each currency. | Change | offline read |  | `components/trip/MoneyTab.tsx:133-145,237-244; components/briefing/charts/BarChart.tsx` | Values are rounded to whole major units. The chart component belongs to Briefing, so this is a cross-feature import; move BarChart into the platform ui kit. |
| See the repayments log | Reads the list of recorded repayments: date, from → to and amount. | Change | offline read |  | `components/trip/BalancesCard.tsx:176-197` | Shown in insertion order, mixed across currencies, names only; recordedBy is stored but not shown. New: part of the group ledger, filterable by trip, keyed by member id, and showing who recorded each entry. |
| Browse expenses by day | Scrolls expenses grouped by date, newest first, each showing category emoji, title, the payer, 'split N-way' or 'split all', and the amount. | Keep | offline read |  | `components/trip/MoneyTab.tsx:122-131,263-314` | Expense notes are entered but never shown in the list, only inside the edit form (MoneyTab.tsx:282-307). In the group ledger this becomes a view filtered by trip tag. |
| See totals per currency | Reads 'Spend so far' with one line per currency. | Keep | offline read |  | `components/trip/MoneyTab.tsx:92-100,155-174; lib/money.ts:42-50,143-253` | Symbols are resolved per screen, so ¥ becomes CN¥ and JP¥ when both currencies appear. Keep this as generic symbol logic. |
| See suggested settle-up transfers | Reads suggestions like 'Bob → Ada: ¥120' for each currency. | Keep | offline read |  | `components/trip/BalancesCard.tsx:133-170; lib/money.ts:374-405` | Greedy per currency: the biggest debtor pays the biggest creditor first. Nothing nets across currencies. Keep, but compute over the group ledger. |
| Delete a repayment | Taps ✕ next to a logged repayment. | Keep | offline edits | `money.editAny` | `components/trip/BalancesCard.tsx:86-92,176-197; app/api/trips/[id]/settlements/[settlementId]/route.ts:7-21` | Any member; hard delete. There is no edit: delete and record again. |
| Receipt photo | Attaches a platform blob on R2. Offline, the photo is kept on the phone and uploaded before the command that references it is sent. Deferred until platform blobs land with Journal: Money moves before Journal in the build order. | New · later | offline edits | `money.add (own) / money.editAny` | `money.attachReceipt@1` |  |
| Change an expense's trip tag | Moves a row between the group space and a trip space, which is a delete plus a create across spaces in one command. | New · later | offline edits | `money.editAny` | `money.retagExpense@1` |  |
| Settle across currencies | Nets a JPY debt against an SGD debt at a stated rate, recorded as a pair of settlements so per-currency balances stay exact. | New · later | offline edits | `money.settle` | `money.convertBalance@1` |  |
| Multi-payer, adjustment and itemized splits | Several payers on one bill (Settle Up's whoPaid weights) and Splitwise's adjustment and itemized modes. | New · later | offline edits | `money.add` | `money.addExpense@2` |  |
| Default weight per person | Settle Up's defaultWeight: a couple counts 2, and a weight of 0 unticks a person by default. | New · later | offline edits | `money.settings` | `money.setDefaultWeight@1` |  |
| 'Who owes whom directly' view | A pairwise view instead of minimum transfers. Splitwise warns that switching after payments were made along simplified paths confuses people, so this should be a display option only. | New · later | offline read |  | `read-only` |  |
| Refunds / income and personal home-currency view | Negative expenses, and showing balances in each viewer's own currency. | New · later | offline edits | `money.add` | `money.addExpense@2` |  |

#### VOT · Polls

_Group decisions (new)_

Where its data lives: group. polls_poll, polls_option and polls_vote live in the GROUP space, as the blueprint places polls there. An optional trip_id is only a context tag, used for filtering and Now cards. Trip-only guests therefore cannot vote in MVP.

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Create poll | A question, 2–20 options, single or multiple choice (WhatsApp's 'allow multiple answers'), an optional deadline and an optional trip tag. Client ids let it be created offline. | New · first release | offline edits | `polls.create (default owner, organiser, member ⇄)` | `polls.create@1` |  |
| Place options | An option can be a city or country picked with the reference/ place-search widget. It stores the reference id (GeoNames id or ISO code) plus a snapshot of the label and coordinates, and results show a mini map. Places like restaurants are not in reference data, so those stay free text. | New · first release | offline edits | `polls.create` | `part of polls.create@1 / polls.edit@1` |  |
| Vote / change vote | Replaces the caller's ballot; an empty ballot retracts it. Votes can change until the poll closes, as in WhatsApp. Offline, the vote shows as pending, and server time decides whether it made the deadline. | New · first release | offline edits | `polls.vote (default owner, organiser, member; viewer ⇄)` | `polls.vote@1` |  |
| Results and who voted | Count and names per option, plus who hasn't voted yet: active members with an account and vote permission. Placeholder people are excluded. | New · first release | offline read |  | `read-only` |  |
| Deadline | An optional closes_at. After it the server rejects votes, as Doodle does, and a late offline vote lands in Needs attention with 'Poll closed at 18:00'. While offline the UI warns: 'counts only if it arrives before 18:00'. | New · first release | offline edits | `creator, or polls.manage` | `part of polls.create@1 / polls.edit@1` |  |
| Close / decide | Closes the poll and optionally pins the winning option; ties are shown and the creator picks. | New · first release | offline edits | `creator, or polls.manage (default owner, organiser)` | `polls.close@1` |  |
| Reopen | Reopens a closed poll. A new deadline is required if the old one has passed. | New · first release | offline edits | `creator, or polls.manage` | `polls.reopen@1` |  |
| Edit poll | Edit the question and deadline, or add options. An option can be removed only while it has no votes, so nobody's ballot changes under them. | New · first release | offline edits | `creator, or polls.manage` | `polls.edit@1` |  |
| Delete poll | Soft delete. | New · first release | offline edits | `creator, or polls.manage` | `polls.delete@1` |  |
| Date options | Deliberately not built in Polls: Dates owns finding dates. Revisit only if Dates' later candidate-windows mode is not enough. | New · later | offline edits | `polls.create` | `polls.create@2` |  |
| Hide results until you vote | Rallly's 'hide scores'. This is a UI courtesy only, because every phone holds every ballot. | New · later | offline edits | `creator` | `polls.edit@1 (setting)` |  |
| Anonymous polls | Doodle-style hidden voting needs each ballot stored in the voter's USER space plus server-computed tally rows in the group space, because rows sync unfiltered within a space. | New · later | offline edits | `polls.vote` | `polls.voteAnonymous@1` |  |
| Participants suggest options | A per-poll 'anyone can add options' setting. | New · later | offline edits | `polls.suggest` | `polls.addOption@1` |  |
| Ranked choice, limit per option, vote for a placeholder | Troupe's ranked choice, Doodle's cap on votes per option, and an organiser voting for someone without an account. | New · later | offline edits | `polls.vote / polls.voteForOthers` | `polls.vote@2` |  |
| Use the result in another app | For example, 'Add Kyoto to day 3'. Other apps contribute actions through the registry; Polls never imports Planner. | New · later | offline edits | `target app's switch` | `registry action` |  |
| Trip-space polls for guests and comments | Polls tagged into the trip space so trip-only guests can vote, and discussion on a poll (Rallly comments). | New · later | offline edits | `polls.create` | `polls.create@2 / polls.comment@1` |  |

#### DTS · Dates

_Availability and finding dates (new)_

Where its data lives: group. dates_search and dates_mark live in the GROUP space. An optional target trip_id points at platform_trip. Deciding calls the platform trip service inside the same transaction.

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Start a date search | Title, earliest start and latest end, a wanted length (minimum and maximum days), and an optional target trip. | New · first release | offline edits | `dates.create (default owner, organiser, member ⇄)` | `dates.createSearch@1` |  |
| Paint my availability | Tap or drag across days to set yes, if need be, no or clear. Each gesture sends one command that upserts your own marks, and it works offline. | New · first release | offline edits | `any member of the search's group (own marks only)` | `dates.mark@1` |  |
| Quick fill | 'Mark everything else as no' (or yes) in one tap. | New · first release | offline edits | `own marks only` | `dates.mark@1` |  |
| Heatmap calendar | Each day is shaded by how many are available, with if-need-be shown lighter. Tapping a day shows who is yes, if need be or no, like When2meet's hover. | New · first release | offline read |  | `read-only` |  |
| Best-window finder | A pure shared function. It tries every window of min–max days inside the range and ranks them by: the number of people available on every day, then the fewest if-need-be person-days, then longer, then earlier. People who haven't answered are never counted as available. It shows the top 5 with a breakdown, for example 'everyone except Sam (out Fri 14)'. | New · first release | offline read |  | `read-only` |  |
| Who hasn't answered | Lists members with accounts who have no marks. Placeholder people are excluded in MVP. | New · first release | offline read |  | `read-only` |  |
| Decide and apply | Records the chosen window. If there is a target trip, the handler calls the platform trips.setDates in the same transaction, so the decision and the trip dates change together or not at all. trip.editDetails is re-checked when the command arrives. | New · first release | offline edits | `dates.decide (default owner, organiser) + trip.editDetails on the target trip` | `dates.decide@1` |  |
| Create a trip from this window | Calls trip.create@1 with the dates, then links the search to the new trip. | New · first release | online only | `group.createTrip + dates.decide` | `trip.create@1 then dates.decide@1` |  |
| Edit search | Change the title, range or length. Marks outside a narrowed range are kept but ignored. | New · first release | offline edits | `creator, or dates.decide` | `dates.editSearch@1` |  |
| Reopen / delete search | Undo a decision (the trip dates are left alone), or soft-delete the search. | New · first release | offline edits | `creator, or dates.decide` | `dates.reopen@1 / dates.deleteSearch@1` |  |
| Required people | 'Must come' people. Windows without them are ranked out. | New · later | offline edits | `dates.decide` | `dates.editSearch@2` |  |
| Candidate-windows mode | The organiser proposes 2–5 windows and people answer yes / if need be / no to each, the Doodle/Rallly model. | New · later | offline edits | `dates.create / member` | `dates.proposeWindows@1 / dates.answerWindow@1` |  |
| Mark for a person without an account | Organisers answer for kids or grandparents. | New · later | offline edits | `dates.markForOthers (owner, organiser)` | `dates.markFor@1` |  |
| Guests, reusable availability, calendar import, holidays | Trip-space searches so trip-only guests can mark days; a personal availability reused across searches; importing busy days from ICS; and a public-holiday overlay that comes only from a worldwide reference dataset, never per-country code. | New · later | offline edits | `dates.create` | `dates.*@2` |  |

#### JNL · Journal

_Trip journal with photos_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Attach an uploaded photo | Taps '📷 Add photo' and picks a JPEG, PNG or WebP. A thumbnail appears on the draft, and ✕ removes it before posting. | Change | offline edits | `journal.write` | `components/trip/JournalSection.tsx:60-80,128-160; app/api/trips/[id]/photos/route.ts:15-59; lib/server/photoStore.ts:6-100` | Up to 8 MB. The declared type must match the file's leading bytes. Saved as a file on local disk (data/uploads/trips/<id>/) with no DB record. The button is hidden when the host cannot write, which is always the case on Vercel (photoStore.ts:54-58), so production has no photo uploads at all. New: a platform/files blob store with DB rows (owner member id, trip, type, size). Photos taken offline are queued as blobs in the outbox and uploaded when back online. |
| Read the journal | Reads entries grouped by day, newest day first, each with its author and photos. | Change | offline read |  | `components/trip/JournalSection.tsx:48-58,177-247; components/trip/TrackerTab.tsx:59-71` | Today it lives inside the Today tab. It becomes the Journal mini-app, with a contribution back into Today. |
| Edit my journal entry | Taps Edit on my own entry and changes the text. | Change | offline edits | `journal.write` | `components/trip/JournalSection.tsx:101-108,189-207,226-235; app/api/trips/[id]/journal/[entryId]/route.ts:33-85` | Author only: others get 403 and the UI hides the button. The UI edits text only, although the API also accepts date and photos. Uploads the edit removes are deleted best-effort. No version guard. New: author-only by default, plus a moderation switch for owner and organiser. |
| Delete my journal entry | Taps Delete on my own entry. There is no confirm step. | Change | offline edits | `journal.write` | `components/trip/JournalSection.tsx:236-239; app/api/trips/[id]/journal/[entryId]/route.ts:16-31,87-116` | Author only. Uploaded files that no other entry references are deleted best-effort. New: blob rows cascade. Deleting your own entry needs journal.write; a separate journal.moderate switch lets owner and organiser remove other people's entries. |
| Write a journal entry | Picks a day (default today), writes text, optionally attaches photos or links, and taps 'Add entry'. | Keep | offline edits | `journal.write` | `components/trip/JournalSection.tsx:39-46,90-99,114-175; app/api/trips/[id]/journal/route.ts:10-49; lib/server/schemas.ts:295-318` | Text 1-5000, up to 12 photos. The date is any ISO date, not limited to trip days. The author is my member name. Becomes journal.addEntry@1 with a client-minted id. |
| Attach a photo link | Pastes an https:// link and taps 'Attach link'. | Keep | offline edits | `journal.write` | `components/trip/JournalSection.tsx:82-88,161-167; lib/server/schemas.ts:298-304` | https only, up to 500 characters, shown as '🔗 photo link' that opens in a new tab. Keep as a fallback. |

#### TIX · Tickets

_Bookings_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Add a booking | Taps '+ Add ticket', picks a kind (flight, train, hotel, attraction, other) and fills in title, dates (check-in/check-out for hotels), time, from/to, confirmation, price and notes. Placeholder hints use the trip's own stops, gateways and currency. | Change | offline edits | `tickets.add` | `components/trip/TicketsTab.tsx:111-134,216-389; app/api/trips/[id]/tickets/route.ts:12-56; lib/server/schemas.ts:198-219; lib/tickets.ts:33-104; components/TripView.tsx:210-216` | Rules: title 1-80; dates ISO; time free text up to 20; from/to up to 60; confirmation up to 60; price is FREE TEXT up to 30, not a money amount; notes up to 300; empty fields become null. The default kind is 'train'. New: price stored as minor units plus currency under the strict money rules, with an optional 'add as expense' hand-off to Money through the registry (no FK between them). **China-only part:** The kind defaults to 'train' (TicketsTab.tsx:231), a China-first default; the notes placeholder is 'Seat 05A, carriage 3…' (TicketsTab.tsx:367). |
| Edit a booking | Taps '✎ Edit' on a card, changes fields and saves. | Change | offline edits | `tickets.editAny` | `components/trip/TicketsTab.tsx:84-106,244-258; app/api/trips/[id]/tickets/[ticketId]/route.ts:11-57` | Any member can edit any booking. The update is a partial patch where empty clears a field. It reads, merges and writes with no version guard, so the last write wins. New: a version per row, and separate switches for adding and for editing anyone's booking. |
| View bookings | Sees ticket-style cards: kind, title, dates or 'Date TBC', time, from → to, confirmation, price, notes and 'added by'. | Keep | offline read |  | `components/trip/KitTab.tsx:81-98; components/trip/TicketsTab.tsx:40-139,141-214; lib/tickets.ts:23-31` | Sorted by date, then time, then title, with undated bookings last. 'added by' is a name today and becomes a member id. |
| Airport suggestions in flight bookings | For a flight, types in From or To and picks a suggested airport, which fills in 'Name (IATA)'. | Keep | online only |  | `components/trip/TicketsTab.tsx:334-348; components/trip/AirportInput.tsx:63-176` | Suggests but never restricts: the field stays free text capped at 60. Uses the reference/ airport search widget. |
| Remove a booking | Taps '✕ Remove'. There is no confirm step. | Keep | offline edits | `tickets.editAny` | `components/trip/TicketsTab.tsx:106,200-207; app/api/trips/[id]/tickets/[ticketId]/route.ts:59-73` | Any member; hard delete. Bookings always survive a plan rebuild. |

#### PAK · Packing

_Checklists_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Generate a packing list | Sees grouped packing items: country documents and tech, clothing for the season, activity gear for interests or beach places, and a kids group. | Change | offline edits | `packing.edit` | `lib/packing.ts:12-95; lib/server/planService.ts:14; components/PlanStep.tsx:41,187-216; lib/countryProfile.ts:143-162` | Moves to the Packing app, seeded from platform trip facts (countries, dates→season, members' child flag) and reference country facts (plugs, voltage, cash). Planner may not import Packing, so interest-based gear needs interests exposed through a registry contribution, or it is dropped. Remove the Chinese BEACH_DESTINATIONS ids (:36) and the Harbin kit (:47-51), and reword 'long train rides' (:86). **China-only part:** BEACH_DESTINATIONS sanya/xiamen/qingdao/shenzhen :36; Harbin kit :47-51; CN_PACKING and CN winter note |
| View the packing list | Sees grouped checklists generated for the trip: country essentials, clothing for the season, activity gear and kids. | Change | offline read |  | `components/trip/KitTab.tsx:100-113; components/trip/PackingSection.tsx:21-56; lib/packing.ts:38-95; lib/server/planService.ts:14` | Generated once from TripInput, stored in trips.data.packing and regenerated by a rebuild. There is no way to add, rename, remove or reorder items or groups, and no split between personal and shared lists. New: Packing owns its list rows, seeded from a template built from the reference/ country profile, and they are editable. **China-only part:** BEACH_DESTINATIONS (sanya, xiamen, qingdao, shenzhen) and the Harbin extreme-cold line (lib/packing.ts:36,47-51). The kids group mentions 'long train rides' (lib/packing.ts:84). |
| Tick packed items | Ticks an item. It is struck through with the name of whoever packed it. Any member can untick it. | Change | offline edits | `packing.tick` | `components/trip/PackingSection.tsx:30-47; lib/tripShared.ts:356-358; app/api/trips/[id]/checks/route.ts:8-34` | Stored in the shared checks table under the key 'pack:<group title>:<item text>', so changing the text orphans the tick. Packing ticks survive a rebuild, which clears only item: and day: keys. New: Packing-owned tick rows with FKs to the item id and member id. Decide between shared and per-person packing. |
| Tick packing items in the preview | Checks off packing items on the plan preview (strike-through). | Drop | — |  | `components/PlanStep.tsx:67,99-105,196-209` | The ticks are local only and lost when the trip is created. Packing app checks replace them. |

#### BRF · Briefing

_Read-only share link_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| View the full briefing in-app | Opens Share → 'View briefing' and reads the unredacted briefing, including crew names, the ticked count and full booking details. | Change | offline read |  | `components/shell/ShareMenu.tsx:155-170; components/shell/ShareBriefing.tsx:30-58; lib/briefing.ts:201-240; components/trip/BriefingView.tsx:53-206` | Built in the browser from the whole payload. The 'N items ticked off' figure is payload.checks.length (lib/briefing.ts:237; BriefingView.tsx:95), so it mixes itinerary and packing ticks. New: the Briefing mini-app assembles its sections from registry contributions (days from Planner, bookings from Tickets, and so on) and never imports other features; each contributor reports its own counts. |
| Choose whether the link shows booking details | Ticks 'Include confirmation numbers, prices and ticket notes'. | Change | online only | `briefing.share` | `components/trip/BriefingShare.tsx:110-121; lib/briefing.ts:181-199` | Off by default. Names and ticks never appear on the public link. New: each contributing app declares its own include/exclude toggles for its sections. |
| Open a public briefing | Anyone with /b/<code> reads the trip: header (dates, party, route), a day-by-day journey with a day picker, charts (days per city, interest mix, daily pace) and logistics (bookings, tips, coverage note). Printing includes every day. | Change | online only |  | `app/b/[code]/page.tsx:8-50; components/trip/BriefingView.tsx:53-206; lib/briefing.ts:127-240; lib/wall.ts:36` | Rendered on the server, no sign-in, marked noindex, crew removed. New: the same page, built from the briefing contributions. It must stay outside the login wall and outside the PWA service worker's auth. **China-only part:** The route line shows a curated Chinese localName in a seal-coloured kai font (lib/briefing.ts:127-130; components/trip/BriefingView.tsx:83-89). |
| Share menu in the trip header | Taps Share to get the invite block, the public-link controls and a 'View briefing' disclosure. | Change | offline read |  | `components/shell/ShareMenu.tsx:21-30,45-175` | Split it up. Invites move to the People screen (platform/groups). The share-link controls live in the Briefing app on top of the platform share_link service. 'View briefing' is the Briefing tile itself. The lazy-load workaround (keeping the 70 KB facts artifact out of the root chunk, :8-30) disappears once Briefing is its own route bundle. |
| Include booking details on the public link | Ticks 'Include confirmation numbers, prices and ticket notes' for the shared link. | Change | online only | `briefing.share` | `components/trip/BriefingShare.tsx:110-120; lib/briefing.ts:186-199,229-231` | Becomes a per-link choice of which public sections to include, drawn from registry sections the apps mark public. Tickets declares its section and a 'with secrets' variant. Note: booking titles, dates and from/to are public today even with this box off; only confirmation, price and notes are gated. Free-text titles can contain names. |
| Open a shared briefing without an account (/b/<code>) | Anyone with the link sees a chrome-free, read-only briefing (plan, charts, tips, bookings) with no member names. | Change | online only |  | `app/b/[code]/page.tsx:8-50; lib/briefing.ts:219 (party size), 235-237 (crew hidden); lib/wall.ts:36; components/shell/AppShell.tsx:41-43` | Keep the route (app/b/[code]): public, noindex, dynamic. It stays online-only, the service worker never caches it, and it renders only sections that apps mark public through the registry. Today it calls buildBriefing directly, reading the store without API authz (:21-27); resolve the code through platform/access instead. The party size (adults, kids) is public today (briefing.ts:219); classify it like any other section. Add rate limiting on code lookups (none today). |
| Read the full briefing inside the app | A member expands 'View briefing' to see the unredacted briefing with bookings and crew. | Change | offline read |  | `components/shell/ShareMenu.tsx:155-170; components/shell/ShareBriefing.tsx:30-58` | Becomes the Briefing mini-app tile. Its content comes from registry sections each app contributes, not from buildBriefing reading a whole TripPayload. It keeps the GeoNames credit. |
| Create and copy a public briefing link | Taps 'Create share link', then Copy, to share /b/<code> with people who are not joining. | Keep | online only | `briefing.share` | `components/trip/BriefingShare.tsx:12-109; app/api/trips/[id]/briefing/route.ts:17-65; lib/server/pgStore.ts:485-512; components/shell/ShareMenu.tsx:149-152` | Any member can create it. One link per trip, with a 12-character bearer code; turning sharing on again keeps the same code. Only members can read the link's current state. |
| Revoke the briefing link | Taps 'Revoke link' and the URL stops working. Creating a link again mints a new code. | Keep | online only | `briefing.share` | `components/trip/BriefingShare.tsx:122-129; app/api/trips/[id]/briefing/route.ts:52-55; lib/server/pgStore.ts:514-518` | Any member can revoke. |

#### TDY · Today

_The trip day: countdown, now & next, recap_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Countdown before the trip | Before departure sees 'N days to go', packing ticked x/y and the number of tickets on file. Without a start date, sees a prompt to set one. | Change | offline read |  | `components/trip/TrackerTab.tsx:73-116; lib/tracker.ts:28-47` | The packing and ticket counts become registry contributions from Packing and Tickets, and the countdown is also a card in the launcher's Now strip. The 'set a start date' prompt needs a real control (see 'Set or change the start date'). |
| Day X of N with progress | During the trip sees today's destination, 'Day 3 of 10' and a bar showing activities ticked across the whole trip. | Change | offline read |  | `components/trip/TrackerTab.tsx:179-213; lib/tracker.ts:48-54,89-103` | progress counts every item, including travel, free and arrival, but DayCard only lets activity and custom items be ticked (DayCard.tsx:355). The day number comes from the device's local date compared with the start date, not the destination's timezone. Planner contributes this section. **China-only part:** todayIso's doc justifies device-local dates with 'the whole party lives in UTC+8 either way' (lib/tracker.ts:48). |
| Now & next | Sees the current and next unticked item for today, based on the time of day. | Change | offline read |  | `components/trip/TrackerTab.tsx:43-48,215-238; lib/tracker.ts:56-87` | Uses slot bands (morning before 12, afternoon before 18, then evening) and ignores time blocks. Re-renders every minute. New: use time blocks when an item has one. Planner contributes this section. |
| Tick today's items from Today | Ticks items in 'Today's plan' without opening the plan. | Change | offline edits | `planner.tick` | `components/trip/TrackerTab.tsx:240-268` | Every item kind can be ticked here, unlike on DayCard. It is the same tick command, owned by Planner and surfaced through a contribution. |
| Today's spend | Sees today's spend and whole-trip spend per currency, with a link to Money. | Change | offline read |  | `components/trip/TrackerTab.tsx:126-131,184-186,270-302; lib/money.ts:42-54` | Sums per currency, never converted. 'Today' means the expense date equals the device's local date string. Becomes a Money contribution to Today and to the Now strip. |
| Trip stats | Sees cities reached, km 'by rail so far', activities done and the number of journal entries. | Change | offline read |  | `components/trip/TrackerTab.tsx:118-125,133-154; lib/tracker.ts:105-132` | The km figure is the great-circle distance between consecutive distinct destinations. New: each stat is a contribution (cities and distance from Planner, entry count from Journal). Rename 'by rail' to distance travelled. **China-only part:** Coordinates come only from the curated getDestination (TrackerTab.tsx:120-125; lib/data/index.ts:14-16), so the km stat is always '—' on non-China trips. |
| End-of-trip wrap-up | After the last day sees 'That's a wrap' with day count, route and total spend per currency, followed by the stats and the journal. | Change | offline read |  | `components/trip/TrackerTab.tsx:156-177` | Becomes a trip recap card. Totals stay per currency, with no conversion. **China-only part:** 🏮 lantern emoji in the heading (TrackerTab.tsx:160). |

### Reference data

#### GEO · Map & globe widgets

_Globe, world and country maps, month timeline_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Choose the country to browse | Opens the globe or the A-Z country list under it (or taps the 'Change country' pill) to pick which country the search, map and cards show, and sees a 'Selected' card naming it. | Change | offline read |  | `components/DestinationStep.tsx:74,99-111,281-289; components/map/MapExplorer.tsx:317-324; components/map/worldLevelShared.tsx:300-356 (CountryPicker select, SelectedCountryCard); app/plan/page.tsx:57` | The globe/country picker becomes a shared reference widget used by Planner and Explore. The browse scope must start empty, not 'CN'. Today the pill reads 'China' before the user chooses anything. **China-only part:** useState("CN") page.tsx:57 |
| Pick a travel month on the timeline | Drags or taps the Jan-Dec scrubber under the country map, or focuses it and uses the arrow keys (wrapping Dec to Jan). It shows the country's holiday and crowd bands and sets the trip's month. | Change | offline read |  | `components/map/MapExplorer.tsx:51,72,312-315,549-551; components/map/MonthTimeline.tsx:28,46-55,79-114; lib/months.ts:19-32,45,65,107-111; lib/countryBaseProfile.ts:215,234` | Take the month from the trip's dates when they are set. The timeline is a shared reference widget. Holiday bands and the crowd curve exist only for CN (HOLIDAY_BANDS, NATIONAL_CROWD), so under global-only they disappear unless a worldwide source is added. The scrubber shows October (DEFAULT_MONTH, MapExplorer.tsx:51) before anything is picked and resets to October whenever the map remounts (Cards view, or leaving and re-entering the step), while the page keeps the month already picked, so the two can disagree. The picked month is not stored on the trip (TripData has no month field, lib/tripShared.ts:8-16); it is only used to derive the season. **China-only part:** HOLIDAY_BANDS (Chinese New Year, Golden Week) months.ts:65; NATIONAL_CROWD months.ts:45; countryBaseProfile.ts:215 |
| See how well a month suits a place | While picking, sees markers coloured by the timeline month and, on hover, a popup with the verdict (great/ok/poor/avoid), typical temperatures, a season note or month highlight, crowds and a 'Click to select' hint. | Change | offline read |  | `components/map/PlacePopup.tsx:27-148; components/map/MarkerLayer.tsx:57-59; components/map/mapTypes.ts:2,62-84,220; components/map/climateIndex.ts; lib/months.ts:118-236,241-251,255-341` | Keep the feature on worldwide CHELSA climate. Drop REGION_MONTHS (keyed by ChinaRegion), CURATED_HIGHLIGHTS and the northern-only seasonOfMonth used by monthFitForSeasons. It is drawn inside the wizard's own map today, and features never import each other, so it must live in the shared reference map/climate widget used by Planner and Explore, not inside the Explore app. **China-only part:** REGION_MONTHS/ChinaRegion months.ts:132; CURATED_HIGHLIGHTS :255; CLIMATE_COUNTRY = "CN" mapTypes.ts:84; popup crowd/holiday lines from the CN-only profile (PlacePopup.tsx:34-36) |
| Zoom the country map to a province | Picks a province from the 'Zoom to a province' dropdown (sorted A-Z). The map frames it and a caption says the list below still reaches every city. 'All of <country>' zooms back out. | Change | offline read |  | `components/map/MapExplorer.tsx:91,184-191,222-224,335-338,430-446; lib/regionScheme.ts:118-156,158-168; components/map/CountryLevel.tsx:547-565` | Provinces come from the Natural Earth admin-1 file for every country, China included (public/provinces/CN.json). Drop the CN-only label branch so China's names come from the same file. MapExplorer's comments (:170-177, :409-414) still describe a ChinaLevel renderer that CountryMap no longer dispatches to (CountryMap.tsx:270-304). Remove them in the move. **China-only part:** CN province labels from the curated PROVINCES table (curatedNameEn gated on code === 'CN', regionScheme.ts:162-168; imports REGION_META/provinceByAdcode :1) |
| Read the climate colour legend and its data note | Under the country map, sees what the marker colours mean and an 'About the climate colours' note when the colours come from derived climate data. | Change | offline read |  | `components/map/FitLegend.tsx:5-27; components/map/MapExplorer.tsx:521-531; lib/climateNote.ts:26,33-37; components/plan/GapNote.tsx` | Keep the legend. Once China goes through the worldwide climate pipeline it gets the same note as every other country, and the CN exception goes. **China-only part:** China gets no note because its colours come from curated REGION_MONTHS (CURATED_CLIMATE_COUNTRY = 'CN', climateNote.ts:26) |
| Load a country's map assets | Nothing visible. Opening a country loads its province geometry, the projection manifest, catalog cities (GET /api/map/cities), the GeoNames city file, its enrichment file and its climate file, plus its airports. | Change | offline read |  | `components/map/useCountryAssets.ts:176-265,270-285; app/api/map/cities/route.ts (GET); lib/server/catalog.ts:308; components/map/explorerPlaces.ts:97-109` | Keep the static geometry, city, enrichment and climate files and cache them in the installed PWA for opened and trip countries. Drop the /api/map/cities leg, which serves only the 695 China Wikidata cities (catalog.ts:101), and drop the CN climate exception, so every country loads the same files. **China-only part:** /api/map/cities catalog leg; CLIMATE_COUNTRY 'CN' skips the climate file (useCountryAssets.ts:222; mapTypes.ts:84) |
| Spin the globe and pick a country | Drags a 3D globe to rotate it, hovers a country to see its name, clicks a country shape or a small-country dot, and the globe spins to bring the chosen country to the front if it is facing away. | Change | offline read |  | `components/map/GlobeLevel.tsx:123,157,255,365-380,573,599-686 (<title> hover names at :647,:675); components/map/WorldPane.tsx:19-31,80,106; lib/globeTopology.ts:19; lib/globeRotation.ts; public/world-globe.json (110 KB, Natural Earth 110m); app/plan/page.tsx:50-57,276` | This is a shared widget that both Explore and Planner will mount. Countries behind the globe are pruned so they cannot be clicked (GlobeLevel.tsx:255). The widget has two China defaults to remove. (1) INITIAL_ROTATION is [-105,-35], which centres the globe on China (GlobeLevel.tsx:118-123). (2) The wizard's browsing-country state starts as "CN" (app/plan/page.tsx:57). WorldPane passes that state in as selectedCountry, so on first open the globe shows China as selected and the Selected card shows China. Fix: open on a neutral view with nothing selected, or on the active trip's country. Picking a country here only changes what the map is showing. It is not the trip's country, which the wizard takes from the first pick (app/plan/page.tsx:50-56,276, tripCountryFromPicks), so the pick itself needs no command. **China-only part:** Globe opens centred on China with China preselected (GlobeLevel.tsx:123 plus app/plan/page.tsx:57). |
| Choose a country from the A-Z list | Opens the "Or pick from the list" dropdown ("Every country, A–Z…") under the map and selects a country. | Change | offline read |  | `components/map/worldLevelShared.tsx:305-343; components/map/WorldMap.tsx:155-185 (entries built from topology features + smallCountries); components/map/WorldPane.tsx:88-93 (copy); lib/isoTopology.ts:50-82 (SEARCH_ONLY_REASONS)` | The list is built only from the countries the map topology draws, which is 235 on both the flat map and the globe. Territories in SEARCH_ONLY (15 codes) are in neither list, although public/provinces/ and public/cities/ have files for them (checked GF, GI, XK, TV, BQ, SJ, CX). The header copy says the list "reaches every country", and PlaceSearch cannot change country, so French Guiana, Gibraltar or Kosovo cannot be planned at all. Fix: build the list from the 246-country reference table, not from the topology. |
| Open a country's detail map | Opens a country and sees its outline and admin-1 regions framed to fit, with its cities on top. If the map geometry fails to load, a plain list of places is shown instead. | Change | offline read |  | `components/map/MapExplorer.tsx:137-151,370-548; components/map/CountryMap.tsx:128-178; components/map/CountryLevel.tsx:231-711; components/map/useCountryAssets.ts:160-264 (Promise.all fetch of provinces, projections, /api/map/cities, city shard, enrichment, climate); components/map/countryView.ts; lib/countryDetail.ts:102-142; lib/provinceTopology.ts:26-145; lib/countryProjection.ts:27-202; public/provinces/<CC>.json (246 files) + index.json; public/country-projections.json` | China's file is built differently from the other 245. public/provinces/CN.json is a re-envelope of the hand-made public/china-provinces.json (scripts/build-provinces.mjs:318-329,408). Its unit names are Chinese only (for example "北京市"), its ids are adcodes, it draws Taiwan, Hong Kong and Macau as non-selectable units inside China (710000/810000/820000), and it includes the nine-dash line as a unit (100000_JD). Rebuild CN from Natural Earth admin-1 like everyone else, and delete china-provinces.json. For offline use, cache each trip country's province, projection, city and climate files. **China-only part:** CN.json comes from curated china-provinces.json: Chinese names, adcode ids, Taiwan/HK/Macau as units of China, nine-dash line. |
| Zoom to a province and step back out | Picks a province from the "Zoom to a province" dropdown ("All of X" plus every region, sorted A-Z). The map animates in, shows only that province's cities, and a caption reads "Showing X — the list below still reaches every city". "← All X" and "← All countries" step back out, and hovering a region shows its name. | Change | offline read |  | `components/map/MapExplorer.tsx:91,184-224,335-338,392-446; lib/regionScheme.ts:42-160; components/map/CountryLevel.tsx:281-364,440-475 (card closes on re-frame); components/map/UnitsLayer.tsx:44; components/map/mapShared.ts (transformForFeatures)` | This already works for every country; countries with one selectable unit or none get no control (regionScheme.ts:124). Zooming filters the markers to the province's own cities and closes any open place card. The China-only part is the English province labels. They come from China's PROVINCES table through curatedNameEn, which checks code==="CN" (regionScheme.ts:96,166-176; lib/provinces.ts:14). A Natural Earth rebuild of CN supplies name_en, so that code goes away. **China-only part:** curatedNameEn is gated on "CN" and reads lib/provinces.ts PROVINCES (regionScheme.ts:170-176). |
| See cities as sized, labelled markers | Sees city dots sized by importance, with labels on the big ones, a selection ring and stop number on picked places, and a dashed suggested-route line between them. | Change | offline read |  | `components/map/MarkerLayer.tsx:16-138; components/map/markerGeometry.ts:223-239; components/map/markerLayout.ts; components/map/CountryLevel.tsx:379-436,582-618; components/map/useMarkerSelection.ts; lib/dragLayer.ts (nonOverlappingRadii); lib/cityShard.ts:205-222 (cityLevel)` | Curated places are always labelled and drawn larger (markerGeometry.ts:228,235), and only China has curated places, so that branch goes when the China list is retired. For every other city, size is a population tier named with China's administrative words: municipality (1M or more), prefecture (200k or more), county (cityShard.ts:205-222). Rename these to neutral size tiers. The map imports lib/dragLayer, which it shares with the planner DayBuilder (CountryLevel, WorldMap and GlobeLevel all use nonOverlappingRadii). Move that helper to the reference map layer or the UI kit so reference/ does not import a feature. **China-only part:** The kind==='curated' marker branch only ever fires for the 16 China destinations; tier names are China's administrative levels. |
| Hover a place for a quick-look tooltip | Hovers a marker and sees its name, local name, region line, a month verdict (Great/Decent/Off-season/Avoid/No data), typical low-high °C, notes, the month's highlight, a short description, crowd dots with a holiday emoji, a sights or population count, and "Click to select". | Change | offline read |  | `components/map/PlacePopup.tsx:43-147; components/map/mapTypes.ts:220-238,300-322 (originLineFor),376-390; lib/months.ts:255,339; lib/cityShard.ts:218-222; app/globals.css:90-92` | Keep the tooltip, but remove its China-only content: (1) the region line "North China" (mapTypes.ts:319); (2) curated month highlights such as "Fragrant Hills foliage" (months.ts:255-340); (3) seasonNotes; (4) the temperature notes from REGION_MONTHS; (5) the crowd dots and holiday emoji, whose data exists only for CN; (6) the local name set in the Chinese Kai font in seal red (PlacePopup.tsx:84-86; globals.css:90-92). (7) The origin line also adds "· municipality", "· prefecture" or "· county" for every city in every country (mapTypes.ts:320). These are China's administrative words used as population tiers, so drop that suffix or use neutral wording. "Click to select" (PlacePopup.tsx:144) should read "Tap to add" for Planner and not appear in read-only Explore. Keep the verdict, temperatures, description and population for every country. **China-only part:** Highlights, season notes, the 'X China' origin line, crowd dots and holiday emoji, Kai-font local names, China admin-level suffixes. |
| Tap a place to open its card and add or remove it | Taps a marker (or presses Enter on it). The tap adds or removes the place straight away and opens a card showing the verdict, size, typical temperature and main airport, with an "Add X to trip" or "Remove X from trip" button that undoes or redoes it. Esc, × or tapping outside closes the card and returns focus. | Change | offline edits | `planner.edit` | `components/map/CountryLevel.tsx:447-503 (activate = onTogglePlace + setCard),630-694; components/map/SelectedPlaceCard.tsx:104-278; components/map/MapExplorer.tsx:282-303 (togglePlace -> onToggleSelect/onAddCatalog/onRemoveCatalog); app/plan/page.tsx:121-124,155-189` | The card stays a reference widget, but the add/remove action should become a Planner command (for example planner.addStop@1 or planner.removeStop@1) that Planner contributes to the widget, instead of a wizard callback prop. That lets Explore show the card without importing Planner. The command is trip-scoped and queued offline. Note that the marker tap is itself a write, because activate toggles before the card opens (CountryLevel.tsx:490-493). Explore, and viewers or guests in Planner, need the card without the toggle-on-tap as well as without the button. read-only mode already removes marker activation. |
| Browse and filter the country's place list | Under the map, sees place chips grouped by region (12 per group, then "Show all N in X", with "Elsewhere in X" for places with no region), types to filter them, and taps a chip to add or remove it. Without map geometry, this list is the whole country view. | Change | offline edits | `planner.edit` | `components/map/CountryPlaceList.tsx:30-146; lib/placeGrouping.ts:1-63 (imports components/map/mapTypes); components/map/CountryMap.tsx:165-177; components/map/CountryLevel.tsx:698-707` | Tapping a chip writes the same Planner command as the place card. lib/placeGrouping.ts imports from components/ (the wrong direction), so it should move into reference/map. Local names on the chips use the Chinese Kai font (CountryPlaceList.tsx:118-120). Bug: CountryLevel renders this list even in read-only mode (CountryLevel.tsx:698-707), and RouteMap passes onTogglePlace={noop} (RouteMap.tsx:431). So the trip's Plan tab shows pressable stop chips and a filter box whose chips do nothing. In read-only mode the list should render plain items, or be hidden. |
| Pick the month you'd travel | Clicks, drags or arrow-keys along a 12-month strip. It shows the month, its season and season emoji for the country's hemisphere, and re-colours the markers. In the wizard, the chosen month also sets the trip season. | Change | offline read |  | `components/map/MonthTimeline.tsx:28-162; components/map/MapExplorer.tsx:51,72,312-315 (DEFAULT_MONTH = 10, onMonthPicked); app/plan/page.tsx:438-442; lib/countryBaseProfile.ts:179-181; components/trip/RouteMap.tsx:194-216 (routeMonth)` | Keep the slider and the hemisphere-aware season. The hard-coded October default (MapExplorer.tsx:51) should come from the trip's dates or season when there is a trip, as RouteMap's routeMonth already does; otherwise use the current month. Setting the trip season from the month is a Planner command, not the widget's. The crowd dots and holiday bands in this strip are China-only (see the crowd levels row). SEASON_EMOJI comes from lib/meta and should move with the widget. |
| Retry and loading states for map data | Sees skeletons ("Unrolling the world…") while maps load, and "Couldn't load the map data" or "Couldn't load the world map. You can still search for a country." with a "Try again" button on failure. | Change | offline read |  | `components/map/MapExplorer.tsx:355-368; components/map/worldLevelShared.tsx:376-415; components/map/WorldPane.tsx:14-31; components/map/useCountryAssets.ts:97-98,165,261,296; components/map/GlobeLevel.tsx:587` | With an installed offline PWA, the widget must tell "you are offline and this country is not cached" apart from a real failure, and say which trip countries are available offline. The world-map error copy "You can still search for a country" is false: no search can change the country. Point to the A-Z list instead, or fix it once the list is built from the reference table. |
| Map geometry builds | Nothing directly; they produce the world maps, the province maps and each country's framing. | Change | — |  | `scripts/build-provinces.mjs:318-329,352,397,408,600-610 (Natural Earth 10m admin-1; CN re-envelope); scripts/build-world-topology.mjs; scripts/build-globe-topology.mjs; scripts/build-projections.mjs; public/china-provinces.json (58 KB)` | These builds are manual and have no workflow. Remove CURATED_COUNTRY='CN', CN_NON_SELECTABLE and the china-provinces.json input, so CN is built from Natural Earth like the other 245. The CN idKey 'adcode' then goes away, leaving only 'adm1_code'. **China-only part:** The CN re-envelope from china-provinces.json (build-provinces.mjs:318-329,408). |
| Country photo band with photo credit (trip hero and Explore card) | Sees a Wikimedia Commons photo of the trip's country (or an accent gradient when none exists) behind the trip hero and the Explore country card, with a credit line whose artist and licence links open in a new tab. | Change | offline read |  | `components/shell/CountryHero.tsx:62-118 (band + always-on scrim), 126-149 (credit links), 161-169 (http/https scheme check); used at components/TripView.tsx:245-275 and components/map/worldLevelShared.tsx:365-372; lib/countryImagery.ts:211-222 (pickHero); data/country-images.json` | Missing from the inventory, although it lives in components/shell. Move it to reference/ with the country imagery data; it is a reference widget that Planner/Today and Explore render, not shell chrome. Keep the non-conditional scrim, the mandatory credit and the URL scheme check. Photos are hotlinked from Commons, so offline the band falls back to its gradient ground unless the service worker caches the images; decide that explicitly. It reads usePrefs from the shell for the accent, which is an allowed feature→platform dependency. **China-only part:** the member trip hero inside this band renders the country 'mark' chop, which only CN has (同行): components/TripView.tsx:256-260, lib/countries.ts:43 |
| Tap places on the country map to add or remove | Taps a city marker on the country map (or tabs onto the markers, moves with arrow keys/Home/End and presses Enter). The tap adds or removes the city and opens a small card with that month's typical temperatures, the main airport and an 'Add/Remove X to trip' button. The x button, Escape or a click outside closes the card. | Keep | offline read |  | `components/map/MapExplorer.tsx:282-303; components/map/CountryLevel.tsx:490-500,631-694; components/map/SelectedPlaceCard.tsx:138-156,253-275; components/map/useMarkerSelection.ts:75,132-190; lib/mainAirport.ts:104; components/DestinationStep.tsx:371-387` | The map is a shared reference widget that emits picks through a callback. Planner owns what happens to them. The card's climate line and main-airport line read reference data only, so they stay in the widget. |
| Switch between globe and flat world map | Taps 'Show a flat map' or 'Show the globe' under the world picker. The choice is saved as a preference. Reduced-motion users always get the flat map and no toggle. | Keep | offline edits |  | `components/map/WorldPane.tsx:66,80,113-124; components/shell/PrefsProvider.tsx:74-87 (cookie + PUT /api/me/prefs); lib/server/schemas.ts:355` | Both renderers stay in the shared reference map widget. The worldView preference moves to the user sync space in platform/identity, as a per-user setting rather than a trip or group one. |
| Spin the globe and move between countries by keyboard | Drags the globe to spin it and taps a country to open it, or tabs onto the countries and uses the arrow keys, Home/End and Enter. The globe spins to bring an off-screen country into view. | Keep | offline read |  | `components/map/GlobeLevel.tsx:293-340,475-625; components/map/worldLevelShared.tsx:200-271; components/map/WorldMap.tsx (flat renderer)` | Part of the shared world-picker widget used by Planner and Explore. The world topology must be cached in the installed PWA for it to work offline. |
| Step between world, country and region levels | Uses '← All countries' to go back to the globe, '← Back to <country>' to return from the globe, and '← All <country>' to leave a zoomed region. | Keep | — |  | `components/map/MapExplorer.tsx:132-133,392-419; components/map/WorldPane.tsx:95-103; components/map/stepUpButton.ts` | The level is held by the Planner step today (DestinationStep.tsx:74) and resets to the globe whenever the step remounts. As a widget, the host app should own the level so it survives navigation and can be saved in the draft. |
| Show the airports layer | Taps 'Airports' on the country map to draw the country's airports, and taps again to hide them. The button appears only when the map has geometry and the country has airports. | Keep | offline read |  | `components/map/MapExplorer.tsx:111,248,468-481,507-511; components/map/AirportLayer.tsx; components/map/CountryLevel.tsx:574; components/map/useCountryAssets.ts:270-285; app/api/map/airports/route.ts (GET)` | A temporary toggle on the open map, not a preference. Airports come from the worldwide reference airport set. Cache the per-country airport list for offline. |
| Pick from and filter the country's place list | Below the map, or instead of it when a country has no province geometry, taps city chips grouped by province to add or remove them, types in 'Filter N places', and taps 'Show all N in <province>' to expand a group of more than 12. | Keep | offline read |  | `components/map/CountryPlaceList.tsx:37-146; components/map/CountryLevel.tsx:700-707; components/map/CountryMap.tsx:297-304; lib/placeGrouping.ts` | Part of the shared map widget. It is the accessible way to pick any city and the only picker for countries without admin-1 geometry. Local names render in font-kai (:119); see the typography row. |
| Retry when map data fails to load | Sees 'Couldn't load the map data' or 'Couldn't load the world map' with a 'Try again' button, or a notice that the city list is unavailable and search still reaches every place. | Keep | offline read |  | `components/map/MapExplorer.tsx:355-368,484-489; components/map/useCountryAssets.ts:176-265,290; components/map/worldLevelShared.tsx:386-403; components/map/GlobeLevel.tsx:587; components/map/WorldMap.tsx:266` | Offline, the PWA should serve cached country assets rather than show this error. The notice's 'showing curated destinations only' copy (MapExplorer.tsx:486-487) goes with the curated list. |
| Pick a country on the flat world map | Hovers to see a country's name, then clicks a country on a flat world map (tiny countries appear as dots) to open it. | Keep | offline read |  | `components/map/WorldMap.tsx:140-334 (<title> at :295,:323); lib/isoTopology.ts:14,24-130; public/world-countries.json (747 KB, Natural Earth 50m, smallCountries dot layer)` | Moves into the reference map widget unchanged. It is the heaviest world asset at 747 KB, so for offline use cache it only once it has been opened, not on first install. |
| Move between countries with the keyboard | Uses arrow keys, Home and End to walk through countries on the globe or flat map, and Enter or Space to pick one. On the globe, a country out of view spins round to the front. | Keep | offline read |  | `components/map/worldLevelShared.tsx:172-291 (roving tabindex, stepFor, handleKeyDown); components/map/GlobeLevel.tsx:365-462 (onFocusOffscreen spin, parked-caret onParkedKeyDown)` | This is the accessibility layer and should move with the world widget unchanged. |
| Show airports on the map | Toggles an "Airports" button to draw diamond marks for the country's large and medium airports. | Keep | offline read |  | `components/map/MapExplorer.tsx:111,248,468-481; components/map/AirportLayer.tsx:14-116; app/api/map/airports/route.ts:9-15; components/map/useCountryAssets.ts:266-281` | This is local, temporary state, deliberately not a saved pref (MapExplorer.tsx:92-110). The marks are decorative and hidden from screen readers. The button only appears when the country has airport rows. |
| Go back to the country you had open | From the world picker, after a country has been opened once, taps "← Back to X" to return to that country's map without picking again. | Keep | offline read |  | `components/map/WorldPane.tsx:95-103; components/map/MapExplorer.tsx:117-133 (openedCountry latch); components/map/stepUpButton.ts` | This is the downward rung of the world ⇄ country ⇄ province navigation, and it moves with the widget. The openedCountry latch also stops any country file being fetched until a country is opened, which matters for offline cache size. |
| Move between places on the map with the keyboard | Tabs into the country map once, uses arrow keys, Home and End to move between city markers, and Enter or Space to add or remove a place and open its card. | Keep | offline read |  | `components/map/useMarkerSelection.ts:63,117-194 (roving tabindex, aria-haspopup=dialog); components/map/CountryLevel.tsx:495-500` | This is the marker accessibility layer and moves with the widget. Activation writes the Planner add or remove command, so it must be disabled in the same cases as the tap (read-only, viewers, Explore without Planner). read-only mode already removes the roles and tab stops. |
| China region system | Nothing directly. It silently assigns Chinese cities to one of China's 7 app regions for climate verdicts, the "North China" labels and the old China-only map. | Drop | — |  | `lib/provinces.ts:14-97 (PROVINCES with nameZh, regionForProvinceText with 'Central' fallback, REGION_META); lib/types.ts:25 (ChinaRegion); components/map/mapTypes.ts:61-97 (isChinaRegion, CLIMATE_COUNTRY, isChinaPlace); lib/server/catalog.ts:266-268; lib/regionScheme.ts:1,166-176; components/map/explorerPlaces.ts:6,171-180` | Delete it with REGION_MONTHS. Admin-1 names from Natural Earth and GeoNames become the only region vocabulary for every country. **China-only part:** All of it. |
| China baseline test fixtures | Nothing (test-only). These fixtures freeze China's pre-Phase-4 map markup and scan non-China plans for China strings. | Drop | — |  | `components/map/chinaBaseline.ts + chinaBaseline.test.tsx; components/map/countryFixture.ts:235; lib/chinaLeakScan.ts; lib/chinaRegion.test.ts; lib/countryGuidance.test.ts; data/climate-anchors.json; lib/climateModel.test.ts:556-602` | Replace them with the global-only CI scan and worldwide fixtures. climate-anchors.json and the climate-model calibration tests need a non-China anchor set before REGION_MONTHS is deleted. **China-only part:** All of it. |

#### CTY · Country facts

_Currency, languages, emergency numbers, tips_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Good to know tips | Reads country tips, 'X in <season>: …' notes, and a kids tip beside the plan. They are saved into the trip and published on the briefing. | Change | offline read |  | `lib/itinerary.ts:1,80,302-316; lib/countryProfile.ts:164-235; lib/countryTips.ts:191-195; components/PlanStep.tsx:238-254` | Stop snapshotting tips into plan.tips. Planner and Briefing read country tips live from reference (Wikidata facts). Season notes are curated-only, so drop them. Delete CN_GENERAL_TIPS/GENERAL_TIPS and chinaProfile, so China goes through factsProfile like everyone else. **China-only part:** chinaProfile countryProfile.ts:164-178; CN_GENERAL_TIPS itinerary.ts:1,80 |
| See the selected country's photo card | After picking a country, sees a "Selected" card with the country's name over a photo (with credit links to the source and licence) or over a colour gradient. The same hero banner heads the trip page. | Change | offline read |  | `components/map/worldLevelShared.tsx:356-374; components/shell/CountryHero.tsx:62-169; lib/countryImagery.ts:69,196-222; data/country-images.json (14 countries: BR,CN,DE,EG,FR,GB,GR,ID,MA,MX,SG,TH,TR,VN); scripts/ingest-country-images.mjs (manual, no workflow); components/TripView.tsx:245` | Only 14 of 246 countries have a photo; the rest get an accent gradient. Fix: run the image ingest for all countries through the same pipeline, ideally on a schedule. Images are hotlinked from Wikimedia Commons (Special:FilePath), so offline the banner falls back to the gradient unless the service worker caches the photo. CountryHero should become a reference/ui widget that the launcher, Planner and Briefing can all use. |
| Countries shown in their own colour | Sees each country filled in its own accent hue on the world map, in hero gradients, and as the app accent while inside that trip. | Change | offline read |  | `lib/accent.ts:17-66 (24-slot derived palette); lib/countries.ts:37-62 (24 hand-set hues, CN=30 'vermilion seal'); components/map/worldLevelShared.tsx:241-242; lib/prefs.ts:22,68-72,140-161 (accentHues field, resolveAccentOverride); components/shell/ThemeToggle.tsx:74,107,116,134 (the only prefs writers besides WorldPane); components/shell/PrefsProvider.tsx:64; components/shell/TripAccentProvider.tsx:37` | Split this: the hue table stays in reference/countries, and the theme tokens go to platform/shell. A per-country hue override (prefs.accentHues) is stored, cleaned and read, but no screen lets a user set it. ThemeToggle only switches between the country accent and one fixed hue. Either build a control for it or drop the field. On any page without a trip, the whole app takes China's accent, because PrefsProvider defaults to country="CN" (PrefsProvider.tsx:64) and TripAccentProvider falls back to "CN" (TripAccentProvider.tsx:37). In the launcher, where the group or trip switcher decides the accent, off-trip pages should get a neutral app accent instead. **China-only part:** Off-trip pages default to China's hue (PrefsProvider.tsx:64, TripAccentProvider.tsx:37). |
| Currency fact and tips | Reads "Prices are in PEN. Set your home currency on the Money tab for live conversions.", gets a "Some PEN cash as a backup" packing line, and Money uses the code as the trip's conversion currency. | Change | offline read |  | `lib/countryFacts.ts:69-77,98-133,304-307; lib/countryTips.ts:58-61,226-229; lib/countryProfile.ts:98,175,210,256-258; lib/tripShared.ts:51,227-228; data/country-facts.json (Wikidata CC0; 239/246 have currencyCode)` | currencyName is carried but never shown, deliberately (Peru's label is stale). China hardcodes currency "CNY" (countryProfile.ts:175) and the packing line "Some RMB cash (¥300–500)" (cn.ts:54) instead of reading its facts record. The tip copy names the "Money tab" and should say the Money app. Money, Packing and Planner should get these through registry contributions from reference, not by importing countryProfile. **China-only part:** Hard-coded CNY and the RMB cash line (countryProfile.ts:175; cn.ts:54). |
| Plug types and voltage | Reads "Sockets are type A, B and C at 220 V — bring a universal adapter." and gets a packing line such as "Universal power adapter (Peru uses type A/C plugs, 220V)". | Change | offline read |  | `lib/countryFacts.ts:78-81,262-279; lib/countryTips.ts:68-72,197-223; lib/countryProfile.ts:143-163; data/country-facts.json (plugs 207/246, voltage 221/246)` | The template fires only when both plug types and voltage are known. 15 countries are deliberately withheld (the BS 546 D/M ambiguity). China uses a hand-written copy of the same line (cn.ts:64), which the template reproduces exactly. That reproduction check dies with cn.ts, so it needs a different golden test. **China-only part:** Hand-written 'China uses type A/C/I plugs, 220V' line (cn.ts:64). |
| Emergency numbers | Reads for example "Emergency numbers: 105 police, 116 fire, 106 or 117 ambulance." | Change | offline read |  | `lib/countryFacts.ts:32-44,281-292; lib/countryTips.ts:91-133; data/country-facts.json (221/246)` | China travellers never see this, because China's hand-written profile bypasses the facts path (countryProfile.ts:228) even though the data has CN numbers. Once China goes through the shared path it gets them. Consider showing these in Today and Briefing through a registry contribution, since this is the fact a traveller most needs offline. **China-only part:** Not shown for CN (the hand profile bypasses facts). |
| Official languages and translation pack | Reads "Spanish, Quechua and Aymara are official languages." A country with exactly one language also gets an "Offline Japanese translation pack" packing line. | Change | offline read |  | `lib/countryFacts.ts:84-85,237-241; lib/countryTips.ts:162-168,240-244; data/country-facts.json (239/246); scripts/country-facts/languages.mjs` | No 'primary' language is invented. China does not get this tip today (hand profile); it will once it goes through the shared path. |
| Driving side and dialling code | Reads "Traffic drives on the left. The international dialling code is +81." | Change | offline read |  | `lib/countryFacts.ts:82,86-87; lib/countryTips.ts:178-185; data/country-facts.json (driving side 245/246, dialling code 237/246)` | Each clause fires on its own. China does not get it today (hand profile); it will once it goes through the shared path. |
| What we don't know yet (gap note) | Reads a muted note saying the tips come from open data, that there's no country-specific guidance on payments, connectivity, booking channels or holidays, and which facts are missing. | Change | offline read |  | `lib/countryTips.ts:254-317; lib/countryProfile.ts:108,177,196,211; components/PlanStep.tsx:57,252; components/trip/PlanTab.tsx:92-93,285; components/trip/BriefingView.tsx:148-149,201; lib/briefing.ts:233` | The note is never saved into a trip; it shrinks as coverage grows. It is empty for China ("researched by hand", countryProfile.ts:177). Once China goes through the shared path, China gets the note too. Planner and Briefing render it through the registry. **China-only part:** gapNote [] for CN. |
| Country stamp (chop mark) | On a China trip, sees a round red stamp reading 同行 on the wizard's boarding pass and in the trip header. | Change | offline read |  | `lib/countries.ts:27-28,43 (mark only on CN); components/PlanStep.tsx:110-113; components/TripView.tsx:251-258,405-407` | Owner decision: this becomes an English passport-style stamp for every country, generated from reference data (name or code, optionally trip dates). Planner, Briefing and the launcher show it as a UI-kit component. **China-only part:** mark '同行' exists only for CN. |
| Re-derive the season from month and country | Sees the season update automatically when they pick a month or the trip's country changes, south of the equator included. | Keep | offline read |  | `app/plan/page.tsx:299-303,439-442; lib/tripSeason.ts:15-28; app/api/trips/route.ts:51` | Keep the rule. The month is the fact and the server derives the season from getCountryBaseProfile(country).seasonOfMonth. For multi-country trips, resolve per stop or take it from dates. contracts.test.ts C8 scans page.tsx for this and must move with the code. |
| Show what our data doesn't cover | Reads a muted note under the tips naming the data source and the facts missing for that country. | Keep | offline read |  | `components/plan/GapNote.tsx:34-63; components/PlanStep.tsx:53-57,252; lib/countryProfile.ts:108,177,196,211` | A shared reference UI piece (Planner, Briefing, Explore). China currently gets none because it was researched by hand. Once CN goes through the pipeline it gets a gap note like every other country. **China-only part:** CN gapNote [] countryProfile.ts:177 |
| Hop glyph by country rail | Sees 🚄 beside city hops where the country has researched rail, otherwise 🧭. | Keep | — |  | `lib/meta.ts:124-171; components/PlanStep.tsx:66,427-448` | Keep the rule. Only CN has railKmh (230), so under global-only every country shows 🧭 unless a worldwide rail source is added. **China-only part:** railKmh only researched for CN |
| Country name | Sees the country's English name in map headings, the Back and All controls, the picker, cards and generated copy. | Keep | offline read |  | `lib/countryFacts.ts:450-454 (getCountryName); lib/countries.ts:37-62,108,199,366-417 (24 hand names, INGESTED_NAMES for 222, UNINGESTED_NAMES for AQ/BV/HM/UM); components/map/worldLevelShared.tsx:61-64` | The 24-row hand table (for example "China" instead of "People's Republic of China", "Türkiye") is plain data applied the same way to every country, so it complies with global-only. Merge the resolvers into one reference country table. |
| General tips and base packing list | Gets neutral tips (check passport and entry rules, tell your bank, download offline maps) plus a base packing list (documents and money, tech, health), then the fact lines added into it. Generated plans use neutral copy such as "Travel to {city}" and "Time to head home — safe travels!" and a neutral kids tip. | Keep | offline read |  | `lib/countryData/neutral.ts:139-238; lib/countryProfile.ts:112-213; lib/countryBaseProfile.ts:229-254; lib/itinerary.ts:185-313; lib/packing.ts:41-45` | This becomes the one global guidance baseline. Packing and Planner read it through registry contributions (or a reference API), not by importing the profile. Keep the split that keeps the 70 KB facts file out of client map bundles (countryBaseProfile.ts:43-64). |
| Seasons by hemisphere | Sees the correct season for the month in the country (for example March is autumn in Peru) on the month strip, in tooltips and on the wizard's season chips. | Keep | offline read |  | `lib/countryBaseProfile.ts:152-153,179-181; lib/countries.ts:272 (SOUTHERN); lib/months.ts:34-39` | Already global. SOUTHERN is checked against the facts latitude by a test. lib/months.ts seasonOfMonth is hard-coded northern and must not be used for trips (the wizard uses resolveTripSeason). |
| China default country in generators | Nothing. Whenever a trip has no country, generation, packing, tips, the headline and the route estimator all assume China. | Drop | — |  | `lib/countryBaseProfile.ts:76; lib/itinerary.ts:185; lib/packing.ts:41; components/PlanStep.tsx:54,76; lib/route.ts:122` | Delete DEFAULT_COUNTRY. Country is always known from the reference place ids of the picks, and a missing one is a validation error. **China-only part:** DEFAULT_COUNTRY = "CN" |
| See crowd levels and holiday periods by month | Sees "Crowds ●●●●○", coloured holiday bands on the month strip, and holiday notes such as Chinese New Year or Golden Week for the chosen month, in both the strip and the tooltip. | Drop | — |  | `lib/months.ts:45-117 (NATIONAL_CROWD, HOLIDAY_BANDS, bandsIn); lib/countryBaseProfile.ts:165-166,214-215,233-234; components/map/MonthTimeline.tsx:49-75,116-158; components/map/PlacePopup.tsx:50-52,121-136` | Only China has this data; every other country gets crowdByMonth null and holidays []. Drop it along with the China data. A crowd or holiday feature can come back later only if it is fed worldwide through the reference pipeline, not a hand-written table for one country. **China-only part:** All of it: China's national crowd curve plus its 4 holiday bands (CNY, Labour Day, school summer, National Day Golden Week). |
| China hand-written travel guidance | On a China trip, gets tips about Alipay and WeChat Pay, a VPN, 12306 rail booking, carrying a passport and the Amap app, a China packing document, and copy such as "High-speed rail or flight to {city}", "Arrive at the station 30–40 minutes early", a metro-lifts kids tip and "northern air is very dry". | Drop | — |  | `lib/countryData/cn.ts:21-112; lib/countryProfile.ts:3,165-179,228; lib/countryBaseProfile.ts:3-12,211-227,272` | China-only code is retired, so China gets the same fact-based profile as every other country. This loses the payments, VPN and booking advice. If it is wanted later, it must return as rows in a guidance table that every country can have, never as a CN branch. **China-only part:** All of it. |
| Fall back to China when no country is given | Nothing visible, except that a trip or screen with no country quietly gets China's copy, currency, accent or map defaults. | Drop | — |  | `lib/countryBaseProfile.ts:76 (DEFAULT_COUNTRY='CN'); lib/countryProfile.ts:66; lib/route.ts:122; lib/itinerary.ts:185; lib/packing.ts:41; components/PlanStep.tsx:54,76; lib/tripCountry.ts:38; lib/server/schemas.ts:42; app/plan/page.tsx:57,330; components/map/mapTypes.ts:84; lib/climateNote.ts:26; lib/regionScheme.ts:174; lib/server/catalog.ts:101,267; components/shell/PrefsProvider.tsx:64; components/shell/TripAccentProvider.tsx:37` | On the clean-slate schema a trip's country is required, so no default country is needed. Guard against regressions with the planned global-only CI scan, which flags '=== "CN"' and default country literals. **China-only part:** Every literal listed. |

#### CIT · Cities & place search

_GeoNames catalogue, enrichment, search_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Merge city details picked from map and search | Nothing. Picking the same city on the map and in search keeps the richer details (population, blurb, attraction count). | Change | — |  | `lib/catalogExtras.ts:35-73; app/plan/page.tsx:155-160` | With one reference id space, Planner stores only place to reference id and reads the details from reference. The client-side CatalogHit merge becomes unnecessary. |
| Resolve picks into full destinations (Build my plan) | Taps 'Build my plan'. Catalog picks are fetched with their activities, then the plan appears, or 'Couldn't load catalog destinations — try again'. | Change | online only |  | `app/plan/page.tsx:228-253; app/api/destinations/resolve/route.ts:4-17; lib/server/catalog.ts:429-443` | Resolve by reference id only. The curated-slug and Wikidata-Q namespaces go. The route silently truncates to 12 ids (MAX_IDS). An id the server cannot resolve is silently missing from the preview. Serve from cached reference so the preview also works offline. **China-only part:** curated slugs and Q-ids are China-only namespaces |
| Turn a catalog city's attractions into activities | Nothing. A catalog city gets up to 12 real attractions as activities (the first 2 must-see) and a 1-to-2-4 day range. | Change | — |  | `lib/server/catalog.ts:331-384 (untagged → history+nature :335-338, mustSee :342, tagline 'all-China catalog' :369)` | Attractions exist only for the 695 China catalog cities. Global-only needs either a worldwide attractions ingest through the same pipeline or retiring this path. This is the main open decision about how rich plans are outside China. **China-only part:** catalog attractions are China-only; 'from the all-China catalog' tagline :369 |
| Cities per country (fact family) | Sees and picks from each country's notable cities (name, region, population, elevation, timezone) on the map, in the list and in search. | Change | offline read |  | `public/cities/<CC>.json + index.json (GeoNames cities500 CC BY 4.0, top 750 per country, 58,789 cities across 246); lib/cityShard.ts:19-72,205-281; components/map/explorerPlaces.ts:27-39,95-110; app/api/map/cities/route.ts:21-36; lib/server/catalog.ts:95-109,308-320 (mapCities); data/catalog.json (695 Wikidata cities, all China); components/map/MapExplorer.tsx:484-489` | China also merges in the 695-city Wikidata catalog through /api/map/cities, which returns nothing for any other country (mapCities, catalog.ts:308; LEGACY_CATALOG_COUNTRY="CN" at catalog.ts:101). That catalog brings Chinese local names, descriptions, images and attraction counts. Retire it and use GeoNames for China too; the "showing curated destinations only" banner (MapExplorer.tsx:486) goes with it. Timezone (tz) is carried but not shown anywhere. The shards are static files that can be cached offline. **China-only part:** The Wikidata catalog (data/catalog.json) and /api/map/cities return data only for CN. |
| Search places inside the open country | Types in "Search a city, or type any place" above the map to find and add a city in the chosen country, using arrow keys and Enter or a click. | Change | offline read | `planner.edit` | `components/plan/PlaceSearch.tsx:195-206 (fetches the GeoNames shard in the browser),281-302 (/api/destinations),330-356 (shard matching),372-513; app/api/destinations/route.ts:22-36; lib/server/catalog.ts:204-240` | PlaceSearch already searches the open country's GeoNames shard in the browser (PlaceSearch.tsx:195-206,334-356). The server /api/destinations call adds only hits from the China-only Wikidata catalog (catalog.ts:95,216-226), and returns nothing for any other country. After retiring the catalog, drop the server call; search then runs entirely on the cached shard and works offline. Picking a result writes the Planner add-stop command. (Correction: the "from the all-China catalog" string at catalog.ts:369 is not a search-result label. It is the tagline fallback in catalogCityToDestination, used when resolving picks; see the resolve row.) **China-only part:** The /api/destinations catalog is all-China. |
| Local names in native script | Sees a place's name in its local script (for example 北京) next to the English name, in a brush-style Kai font in seal red, in tooltips, cards, list chips and search. | Change | offline read |  | `components/map/PlacePopup.tsx:84-86; components/map/SelectedPlaceCard.tsx:214-216; components/map/CountryPlaceList.tsx:118-120; components/plan/PlaceSearch.tsx:513; lib/cityShard.ts:233 (GeoNames rows set localName null); lib/countries.ts:18 (country localName, 24 rows, not rendered anywhere found); app/globals.css:90-92` | Only the China curated and catalog places have a local name, so once they are retired no place has one. Either drop the field, or source local names the same way for every country (for example GeoNames alternate names or Wikidata native labels). Either way, replace the Chinese Kai font and seal colour with neutral styling. **China-only part:** Local names exist only on China curated and catalog places; Kai font. |
| Data credits | Sees the required GeoNames credit and change notice wherever city data appears, and a photo credit (source and licence links) on country photos. | Change | offline read |  | `components/plan/GeoNamesCredit.tsx; used by app/plan/page.tsx:523, components/DestinationStep.tsx:368, components/TripView.tsx:178,373, components/home/TripsDashboard.tsx:280, components/shell/ShareBriefing.tsx:54, app/b/[code]/page.tsx:45; lib/contracts.test.ts (C7 placement scan); data/cities-report.md; components/shell/CountryHero.tsx:126-169` | The GeoNames credit is legally required (CC BY 4.0). It lives in the planner folder but is used by the shell, dashboard, briefing, trip view and wizard. Move it to reference/ui, and make every new mini-app that shows city data (Explore, Today, Briefing, Now cards) render it. Carry the C7 contract scan over to the new mini-apps. |
| Nightly refresh of cities, descriptions and country facts | Nothing directly; the city lists, descriptions and country facts stay current because a job runs daily and auto-deploys. | Change | — |  | `.github/workflows/refresh-cities.yml:92,138,148,190,298; scripts/ingest-cities.mjs:106-149; scripts/enrich-cities.mjs; scripts/ingest-country-facts.mjs; scripts/country-facts/gate.mjs:75,105,138,612-620; scripts/cities/gate.mjs:106` | Keep the pipeline (daily cron 53 8 * * *) but remove its China-specific gates. REQUIRED_FACT_COUNTRIES includes CN, and CN_CROSS_CHECK compares China's Wikidata facts to the hand-written cn.ts line (gate.mjs:75,105,612-620). REQUIRED_NAMES pins "People's Republic of China" (gate.mjs:138). The cities gate pins Jinan (cities/gate.mjs:106). ingest-cities dedupes against the China catalog (ingest-cities.mjs:106-132). Replace them with neutral golden rows across several countries; CN can be one sample among many. The data these jobs produce lives under reference/. **China-only part:** CN cross-check, required-name and Jinan pins; dedupe against the China catalog. |
| Look up picked places as full stops (resolve) | Nothing directly. When the wizard moves to the plan step, or a trip's map or plan is built, picked city ids are turned into stops with coordinates, region and a tagline. On failure the user sees "Couldn't load catalog destinations — try again." | Change | online only |  | `app/api/destinations/resolve/route.ts:1-18 (GET, max 12 ids, public); lib/server/catalog.ts:331-440 (catalogCityToDestination, geoNamesCityToDestination, resolveDestinations); lib/server/cityIndex.ts + data/cities-index.json (server-only); app/plan/page.tsx:236-252; components/trip/RouteMap.tsx:251; lib/server/planService.ts:12; app/api/trips/route.ts:5` | Keep only the GeoNames branch (geoNamesCityToDestination over the city index). Drop the curated DESTINATIONS branch, the Wikidata catalog branch and its "— from the all-China catalog" tagline (catalog.ts:369). In the new model, each stop row should store the name and coordinates when it is added, so reading a trip offline never needs this lookup. Plan generation can call reference/cities directly on the server. **China-only part:** Curated and catalog branches resolve China-only ids; catalog tagline says "from the all-China catalog". |
| Search places (combobox) | Types in 'Where are you going?'. From the first character it offers matching curated cards and an 'Add "X" as its own place' row. From 2 characters it adds the open country's GeoNames cities at once and the server's catalog hits after a 300 ms debounce. Arrow keys move (wrapping), Enter adds, Escape clears, and focus stays in the box. | Keep | offline read |  | `components/plan/PlaceSearch.tsx:95-96,280-305,345-365,399-402,408-455,464-527; lib/placeSearch.ts:57-60,88-164` | Becomes the shared search widget for Planner and Explore. Ranking: prefix 100 / substring 80 / local name 60 / knownFor 40, at most 10 catalog rows. With the curated leg and the Wikidata leg removed, it ranks one GeoNames leg for every country, and every result appears without a network round-trip. **China-only part:** curated leg (DESTINATIONS) and Wikidata leg are China-only |
| Load the country's city list on first focus | Nothing visible. Focusing the search box fetches that country's GeoNames city file so results appear instantly. | Keep | offline read |  | `components/plan/PlaceSearch.tsx:182,222-233,477-485; lib/cityShard.ts:35,263` | Keep. Cache the city files for opened and trip countries in the installed PWA so search works offline. Failures are silent by design, and the hand-typed row is still offered. |
| Look up a picked city's description | Nothing. The first time a GeoNames city is picked, a one-line description is fetched in the background and shown on its chip. | Keep | online only |  | `app/plan/page.tsx:98,155-184; lib/catalogExtras.ts:82-89; app/api/cities/enrich/route.ts:28-37; lib/server/cityEnrichment.ts:50-70,148,225` | Keep. Signed-in only, Wikidata SPARQL by P1566, 12 ids per call, 20k-entry cache, 15 s timeout, fire-and-forget. The User-Agent still names the old repo china-itinerary-planner (cityEnrichment.ts:58). Update it for travel-super-app. **China-only part:** UA string names china-itinerary-planner |
| Build a destination from a GeoNames city | Nothing. Any worldwide city gets 3 generic activities (old town, market and food street, museum and park) and 1-2 days. | Keep | — |  | `lib/server/catalog.ts:279-306,395-418` | This becomes the single global path. Its destinations carry no foods, no seasons and no must-sees. Region comes from the GeoNames admin-1 only. |
| City descriptions and pictures | Reads a one-line description of a city in its tooltip. When a signed-in user picks a city in the wizard, a short description is fetched live and shown on the picked-place chip. | Keep | offline read |  | `public/cities/enrich/<CC>.json (246) via scripts/enrich-cities.mjs + data/cities-enrich-targets.json; lib/cityShard.ts:80-86,187,224-248; components/map/PlacePopup.tsx:117-119; app/api/cities/enrich/route.ts:28-37 (signed-in only, 401 otherwise); lib/server/cityEnrichment.ts:50,225 (live Wikidata SPARQL); app/plan/page.tsx:155-184; components/plan/PlaceSearch.tsx:541-554` | Build-time descriptions and images cover roughly the top 20-30 cities per country. The image is carried but never drawn on the map (shardRowToMapCity drops it), so Explore could show it. The live lookup needs the network and fails silently for guests. |
| Search the Wikidata city catalog (server) | Nothing visible. Once the query has 2 or more characters, a 300 ms-debounced GET /api/destinations?q=&country= returns catalog cities for the open country. | Drop | online only |  | `app/api/destinations/route.ts:22-36; lib/server/catalog.ts:101,216-255; components/plan/PlaceSearch.tsx:280-305` | The catalog holds 695 cities, all stamped LEGACY_CATALOG_COUNTRY='CN', so the leg answers only for China. Under global-only every country goes through the GeoNames pipeline, so this second leg and its name+province dedupe (PlaceSearch.tsx:150-163) are removed. **China-only part:** entire leg (catalog.ts:101) |
| Hide places a curated card already covers | Nothing visible. Stops search and the map from offering 'Yangshuo' beside the Guilin & Yangshuo card, and removes duplicates between the two data legs. | Drop | — |  | `components/plan/PlaceSearch.tsx:118-163,331,345-365; lib/curatedNames.ts:23-73; lib/placeSearch.ts:122-132; components/map/explorerPlaces.ts:67,97-109 (mergeCountryCities, dropCatalogTwins)` | Exists only because the hand-written China list and two overlapping catalogs exist. Delete it with them, on both the search side and the map side. **China-only part:** whole mechanism (ACTIVITY_COVERED curatedNames.ts:23) |
| Assign region labels to catalog cities | Nothing. Chinese catalog cities get one of China's seven regions; others get their admin-1 name. | Drop | — |  | `lib/server/catalog.ts:266-269; lib/provinces.ts:66` | Drop the China branch and the regionForProvinceText keyword table. Every country uses Natural Earth / GeoNames admin-1. **China-only part:** regionFor CN branch; ChinaRegion keyword table |
| Load the city catalog (bundle or GitHub) | Nothing. The server loads data/catalog.json, or fetches it from GitHub raw when the deploy ships a stub. | Drop | — |  | `lib/server/catalog.ts:69-81,144-176` | Reference data ships through the global pipeline. The hard-coded URL still points at darrenCWJ/china-itinerary-planner, which breaks with the repo rename to travel-super-app. **China-only part:** China-only catalog; old repo URL :144-145 |
| Refresh the catalog on demand | Nothing (no UI). GET shows catalog status; POST starts the Wikidata ingest script locally. | Drop | — |  | `app/api/destinations/refresh/route.ts:27-84` | Local-only (501 on Vercel, :33). When accounts are on, any signed-in user can start it; when accounts are off, anyone can, because the session check runs only inside accountsEnabled() (:42-47). It rebuilds the China catalog and nothing calls it. The nightly pipeline replaces it. **China-only part:** rebuilds the China-only catalog |
| China destinations catalog ingest | Nothing directly. It produced the 695-city China catalog (Wikidata Q148 and China's World Heritage sites). Any signed-in user can re-run it through an unused refresh endpoint, and anyone can read its status. | Drop | — |  | `scripts/ingest-destinations.mjs:37,61,470,662; app/api/destinations/refresh/route.ts:27 (GET status, public),32 (POST spawns the script, signed-in); data/catalog.json; lib/server/catalog.ts:132-178 (bundled plus GitHub raw fallback at :145-146)` | Delete the script, the refresh route (it spawns a child process, only works locally and has no UI caller), data/catalog.json and the catalog loader. The raw fallback URL points at the old repo name (catalog.ts:146). **China-only part:** All of it. |

#### CLM · Climate

_Monthly normals and fit verdicts_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Marker colours show how good the month is | Sees every city marker coloured by how good the chosen month is, with a legend ("What the marker colours mean"). | Change | offline read |  | `components/map/mapTypes.ts:43-97,220-238,273-287,398; components/map/FitLegend.tsx:26-47; lib/climateModel.ts:224-225,266,304-421; components/map/climateIndex.ts; lib/climateShard.ts:86-140,230-281; components/map/useCountryAssets.ts:222-226,258; components/map/explorerPlaces.ts:171-180; public/climate/<CC>.json (246, CHELSA V2.1 1981-2010: lo, hi, precip, cloud, dew point per month)` | 245 countries get a verdict computed from CHELSA climate data. China alone uses the hand-written REGION_MONTHS table, keyed by its 7 regions (mapTypes.ts:84,234,376-385; lib/months.ts:132). Its catalog cities are assigned a region by keyword, falling back to "Central" (explorerPlaces.ts:171-180). China also never fetches its own CHELSA file (useCountryAssets.ts:222-226; RouteMap.tsx:370), although public/climate/CN.json has 412 rows. Send CN through the CHELSA path like everyone else. The model's settings were tuned against China's regions (climateModel.ts:224-225 plus data/climate-anchors.json), so deleting REGION_MONTHS needs a worldwide calibration fixture to replace it. fitForRegion, FIT_FILL_OPACITY and DerivedRegionFits are used only by tests. Rain, cloud and humidity feed the verdict but are never shown as numbers. **China-only part:** CLIMATE_COUNTRY="CN" uses REGION_MONTHS and skips the CN CHELSA shard; the model was tuned on China regions. |
| Read the climate footnote | Opens "About the climate colours" under the map. It says temperatures are 1981–2010 grid averages, mountain towns read colder than they are, and fog and monsoon timing are not modelled. | Change | offline read |  | `lib/climateNote.ts:14-37; components/map/MapExplorer.tsx:529; components/plan/GapNote.tsx` | The footnote is suppressed for China (CURATED_CLIMATE_COUNTRY="CN", climateNote.ts:26,34) and for any country whose climate file failed to load. After the change every country with climate rows gets it. GapNote currently lives in the planner folder and is also used by PlanStep, PlanTab and BriefingView, so it should move to the UI kit. **China-only part:** Suppressed for CN (climateNote.ts:26,34). |
| Climate normals build | Nothing directly; it produces the month verdicts and typical temperatures. | Keep | — |  | `.github/workflows/refresh-climate.yml:9-14,110,148-180,211 (workflow_dispatch only; 10.65 GB CHELSA rasters); scripts/ingest-climate.mjs; scripts/climate/*; data/climate-report.md` | Already worldwide (246 shards, 58,759 cities). The shards join to the city shards by GeoNames id, so this job must be re-run whenever the city shards change a lot. Retiring the China catalog does not affect it. |

#### APT · Airports

_Worldwide airports, main-airport pick_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| See a place's main airport | Reads "Main airport: TNA · 30 km" on a place's card. | Keep | offline read |  | `components/map/CountryLevel.tsx:519-522,689-693; lib/mainAirport.ts:33,104; lib/airports.ts:47,91,174-191 (large/medium only, within 150 km, size-discounted ranking)` | This is a pure function over the open country's airports. It knowingly misses airports across a border because it only sees that country's rows (see the mainAirportFor docblock). |
| Search airports by name or code | Types two or more characters and gets matching airports worldwide. | Keep | online only |  | `app/api/airports/search/route.ts:1-19 (GET, 5-min cache); lib/server/airports.ts:36-68 (server-only); lib/airports.ts:116-191; components/trip/AirportInput.tsx:152; data/airports.json (OurAirports, public domain, 4,133 airports in 234 countries: 1149 large, 2095 medium, 889 small)` | The artifact is server-only (835 KB) and deliberately not shipped to the browser. The only offline fallback is the trip countries' cached airport lists. |
| Airports data per country (fact family) | Sees a country's airports (IATA, ICAO, name, city served, size) on the map layer, in main-airport lines and in route flight legs. | Keep | offline read |  | `app/api/map/airports/route.ts:9-15 (1h cache); lib/server/airports.ts:54; lib/airports.ts:19-32; .github/workflows/refresh-airports.yml:24,51,61; scripts/ingest-airports.mjs (uses lib/csv.ts)` | This is already global and refreshed nightly (cron 23 8 * * *) with an auto-commit. The response is cacheable per country for offline reads. |
| Nightly airport refresh | Nothing directly; airport lists stay current. | Keep | — |  | `.github/workflows/refresh-airports.yml:24,51,61; scripts/ingest-airports.mjs; lib/csv.ts` | Already global (daily cron 23 8 * * *). lib/csv.ts is used only here and should move beside the script. |

#### LIC · Credits & licences

_GeoNames, Wikipedia and Commons attribution_

| Function | What the user does | Status | Offline | Permission | Command / today in | What changes |
|---|---|---|---|---|---|---|
| Map-data credit on the home page | Sees the GeoNames credit under the trip cards, because they show GeoNames city names. | Change | — |  | `components/home/TripsDashboard.tsx:262-281` | The launcher shows city names inside Now cards and trip labels, so each surface that renders reference names must carry the credit. The C7 scan has to be re-rooted to cover platform/ and features/. |
| Show the GeoNames / Wikipedia credit | Sees the licence credit under the search and at the bottom of every wizard step. | Keep | — |  | `components/plan/GeoNamesCredit.tsx:77-89; components/DestinationStep.tsx:368; app/plan/page.tsx:522-524` | Legally required (CC BY / CC BY-SA). It lives in reference UI and is used by the shell, briefing and dashboard, so it must not sit inside a feature folder. |
| See data and photo credits | Sees the GeoNames credit at the foot of the trip page, guest view, in-app briefing and public briefing, a photographer and licence line on the hero photo, and the rate provider's attribution in the rates panel. | Keep | offline read |  | `components/plan/GeoNamesCredit.tsx; components/TripView.tsx:177-179,372-374; components/shell/ShareBriefing.tsx; app/b/[code]/page.tsx:44-46; components/shell/CountryHero.tsx (Credit); components/trip/Rates.tsx:257` | These are licence obligations (CC BY 4.0 GeoNames, Wikimedia photo licences, rate-provider terms), and contract C7 in lib/contracts.test.ts enforces the GeoNames one. New: each mini-app that renders reference/ data mounts the reference/ credit widget, and the registry or shell makes sure every surface that shows the data carries its credit, including the Briefing and the launcher. |

## Part 2: decision register

Every durable decision found in the specs and project memory, classified against the approved rebuild. Later owner decisions (2026-09-24/25) are in §0–§1 of the design spec.

### Money

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Multi-currency by storage, not conversion: every expense keeps its own currency, and totals are always shown as plain per-currency sums that need no rates. | Owner-specified in the Money brainstorm. No rate is ever required to see what was spent. Today: lib/money.ts:107 convertedTotals is an optional extra over per-currency sums. Re-homed to features/money/domain. |  | docs/superpowers/specs/2026-08-15-trip-tracker-money-design.md §Decisions 2; memory china-itinerary-planner-project.md v5 (2026-08-15) |
| Still applies | Balances stay per currency. Debts are computed and settled in the currency they were incurred in, and are never converted. | Converting debts at a hand-entered rate invites arguments. Today: lib/money.ts:335 balancesByCurrency and :381 settleUp. In the rebuild, balances are computed, never stored, by one shared TS function that runs on both the phone and the server. |  | trip-tracker-money-design.md §Decisions 3; memory v5 (2026-08-15) |
| Still applies | Repayments are Settlement records (from, to, amount, currency, date), not paid flags. Partial and aggregated repayments are supported, and 'Mark repaid' pre-fills a settlement. | Supports partial repayment and netting across many expenses. Becomes money_settlement, with from/to as FKs to member ids. |  | trip-tracker-money-design.md §Decisions 4; memory v5 (2026-08-15) |
| Still applies | All amounts are integers in minor units, and money code does no float arithmetic. The UI accepts major units and converts them. | Prevents float drift. Today: lib/server/schemas.ts:253 MinorAmountSchema is an int from 1 to 100,000,000. The rebuild stores it as amount_minor bigint CHECK > 0 with currency char(3) CHECK. |  | trip-tracker-money-design.md §Decisions 8; memory v5 (2026-08-15) |
| Still applies | Zero- and three-decimal currencies honour their ISO minor-unit exponent (JPY 0, KWD 3). Conversion normalises through major units, so mixed-exponent trips stay correct. | A fixed exponent of 2 made JPY 100x wrong. Today: lib/money.ts:32 minorUnitDigits, :278 majorToMinor, :172 formatMinor. |  | docs/superpowers/plans/2026-08-17-pr4-currency-pivot-plan.md §1 ruling 6, §3 (2026-08-17) |
| Still applies | Amounts stay exactly as typed. Nothing rescales or rewrites a stored amount. When a meaning changes, the new meaning is recorded beside the data. | Owner ruling 1 of the PR4 plan (the persisted-semantics rule). In the rebuild this is enforced by frozen command shapes and migrations rather than read-time defaults. |  | pr4-currency-pivot-plan.md §1 ruling 1 (2026-08-17) |
| Still applies | Equal splits only (v1): an expense is split equally among a chosen subset of members, defaulting to everyone. Custom per-member amounts are deferred until real trips need them. | Scope control. Today: lib/money.ts:313 splitMinorUnits. The rebuild's money_expense_share rows (a deferred trigger checks they sum to the amount) make unequal splits possible later, but the owner has not revisited equal-only. |  | trip-tracker-money-design.md §Decisions 5 (2026-08-15) |
| Replaced | Conversion is optional: hand-entered per-trip rates (pivot units per 1 foreign unit) unlock converted grand totals. The converted view is for totals only. | No FX API was wanted, and rates are informational. The rebuild stores an FX snapshot on each expense, so history does not move when rates change. | Money: integer minor units + currency + FX snapshot stored on each expense | trip-tracker-money-design.md §Decisions 2; pr4-currency-pivot-plan.md ruling 2 (2026-08-15) |
| Replaced | Live rates are read-only and display-only (J-C2). A fetched rate never writes to a trip, never feeds converted totals, and never alters a stored amount. Hand-entered rates remain the only input to conversion arithmetic. | This keeps a provider outage from changing anyone's totals. It is in tension with the approved per-expense FX snapshot until the snapshot's source (hand-entered or fetched) is decided. | Money FX snapshot per expense. The snapshot's source is undecided and must be settled against this rule | pr4-currency-pivot-plan.md §6 J-C2, ruling 1.3 (2026-08-17) |
| Still applies | Exchange-rate sources are open.er-api.com, with fawazahmed0 via jsDelivr as the outage fallback. Frankfurter was rejected because it has 30 currencies and no TWD or VND. The fetch is server-side and cached hourly, with a stale fallback. The raw upstream body is never proxied, and the page shows the trip-currency and home-currency pair. | All-countries coverage, plus er-api's redistribution terms. Today: lib/rates.ts:284 and :288. Re-homed to app/api/money/rates, which the blueprint names as a true HTTP route. |  | pr4-currency-pivot-plan.md §1 rulings 3-5, Tasks 5-7 (2026-08-17) |
| Replaced | The conversion pivot is per trip, taken from CountryProfile.currency. A legacy absent pivot reads as CNY (lib/tripShared.ts:187 currencyPivot), and placeholder pivots are never stamped (J-C1). | CNY defaults are China-only and are retired under global-only. A trip-scoped pivot does not fit a group ledger whose expenses are tagged by trip. | Global-only (CNY default retired) + group ledger with a per-expense FX snapshot | pr4-currency-pivot-plan.md §6 Tasks 8-10; planner-redesign-design.md §5.5 (2026-08-17) |
| Replaced | Money is trip-scoped: expenses, settlements and currency settings are records in the trip snapshot, and every write bumps the trip version (Approach A). | This was the simplest route to polling sync in August. | Money becomes a group ledger in its own money_* tables. Expenses are tagged by trip, and balances carry across trips | trip-tracker-money-design.md §Decisions 1 (2026-08-15) |
| Still applies | Expenses, settlements and the journal are members-only and never appear on the public briefing. | Privacy of spending. In the rebuild each app's briefing section declares public:false, and a share-page canary test enforces it. |  | trip-tracker-money-design.md §Decisions 7 (2026-08-15) |
| Retired | The owner merged PR4 (zero-decimal fix) without the production data check. Pre-fix JPY/KRW amounts could read 100x too large. | Accepted risk on a one-day, one-account exposure window. | Clean-slate Postgres: no legacy rows exist | memory planner-redesign.md (PR4 section) (2026-08-22) |
| Replaced | Any trip member may edit or delete any expense, settlement or ticket. | This followed the any-member-edits trust model. | Per-app permission switches. Money's 'Edit anyone's expense' defaults to owner and organiser, and can be toggled per trip | memory understand/auth-identity.md §3.4 (checked at authz call sites); accounts-auth-design.md Out of scope (2026-08-15) |

### Identity

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Accounts use Better Auth email + password, chosen over a self-contained implementation and Auth.js. Google/OAuth, email verification and email-based reset are deferred. | Owner's choice. It works on every host with no email infrastructure. Re-homed to platform/identity. |  | docs/superpowers/specs/2026-08-15-accounts-auth-design.md §Decisions 1-2; memory identity-model.md (2026-08-15) |
| Retired | Accountless bearer-code model: trip join codes granted editing, member identity was a plain name in localStorage (cip-member-<tripId>), and this was chosen over magic links and OAuth. | The owner pivoted to accounts on 2026-08-15 because codes were not user friendly. | Accounts (2026-08-15), now platform member rows with roles | memory identity-model.md (superseded decision); china-itinerary-planner-project.md v2 (2026-08-10) |
| Still applies | Identity is resolved on the server. Mutating routes derive the actor from the session and never accept a client-asserted memberName. | Closes impersonation through the request body. The rebuild strengthens it: each queued command is checked on arrival as the same signed-in user who is still a member and still allowed (blueprint step 4). |  | accounts-auth-design.md §Decisions 6; identity-model.md (2026-08-15) |
| Replaced | Hybrid access with view-only codes. A join code alone gives a read-only, server-redacted Itinerary + Packing guest view built by construction (lib/redactTrip.ts:8 guestTripView). Account + code makes a full editing member. A bare link shows a private screen. | Specified by the owner as stricter than the old model. | Group roles owner/organiser/member/viewer, per-trip overrides, trip-only guests and read-only share links. The join-code trust model is retired | accounts-auth-design.md §Decisions 3; identity-model.md (2026-08-15) |
| Retired | Legacy plain-name members migrate by claiming their old name at join, and inherit its ticks, expenses and journal attribution. | Avoided data surgery on existing trips. | Clean slate: no legacy members | accounts-auth-design.md §Decisions 5 (2026-08-15) |
| Replaced | Any member can edit anything within a trip. There are no owner or editor tiers, and the creator is simply the first member. | Family-scale trust model, kept on purpose in the accounts spec's out-of-scope list. | Group roles, per-trip role overrides and per-app capability switches, edited by the owner or an organiser in one generic permission editor | accounts-auth-design.md §Decisions 3, Out of scope (2026-08-15) |
| Still applies | Journal entries can be edited or deleted only by their author. | Enforced today in app/api/trips/[id]/journal/[entryId]/route.ts:61,100 (403). The blueprint keeps it ('author edits own entries'). |  | accounts-auth-design.md; README API table (2026-08-15) |
| Still applies | Login is compulsory. Signed-out visitors are redirected to /login?next=. The wall is an optimistic session-cookie check in proxy.ts, and routes enforce access themselves (lib/wall.ts:36-37). | Makes sign-in the front door. Re-homed to platform/identity. Exemptions now come from each manifest's publicRoutes, plus the service worker, web manifest, icons and offline page. |  | docs/superpowers/specs/2026-08-16-app-shell-login-design.md §1-2 (2026-08-16) |
| Still applies | ACCESS_CODE was repurposed from a site gate to a signup invite code, checked in a Better Auth before-hook on /sign-up/email. Unset means open signups (lib/server/auth.ts:70-71). | Keeps the app invite-only without a site-wide unlock wall. The 'Family invite code' label may change when the app is named. |  | app-shell-login-design.md §3; memory v6 (2026-08-16) |
| Still applies | Password resets are admin-assisted through the ADMIN_USER_IDS admin plugin (lib/server/auth.ts:18). There is no email infrastructure by design. | Self-contained on every host. It must stay set in production, or a lost password cannot be recovered. |  | accounts-auth-design.md §Decisions 4; memory v8, owner-credential-recovery.md (2026-08-15) |
| Still applies | A deployment fails closed on a missing or weak BETTER_AUTH_SECRET. instrumentation.ts throws (:15-17), and the wall answers 'refuse' with a no-store 503 (lib/wall.ts:30, proxy.ts:44). The secret must be at least 24 characters and not blocklisted (lib/authSecret.ts:30,41). Set the secret before shipping. | Prerendered pages bypass instrumentation on Vercel, so the proxy is the one place every request passes. |  | memory china-itinerary-planner-project.md v7 (2026-08-17) |
| Still applies | Trusted origins are derived per deployment from VERCEL_URL, VERCEL_BRANCH_URL and VERCEL_PROJECT_PRODUCTION_URL, plus the manual TRUSTED_ORIGINS list. A team-wide wildcard was rejected, and the bare <project>-<team> alias stays untrusted, so sign in at the canonical URL. | Lets previews sign in without trusting every project on the team. Today: lib/server/trustedOrigins.ts:14-27. The canonical URL changes when the Vercel project is renamed. |  | docs/superpowers/specs/2026-09-06-backlog-clearance-design.md §1; owner-credential-recovery.md (2026-09-06) |
| Replaced | Better Auth is pinned to an exact version. Both hand-written auth schemas are regenerated on a bump, with an ALTER + backfill, and authSchemaParity.test.ts gates them. | CREATE TABLE IF NOT EXISTS never adds new columns, which was the issuer lock-out risk. | Drizzle v1 migration history run from CI (auth tables included). The version pin stays exact | memory identity-model.md; china-itinerary-planner-project.md v9 (2026-08-21) |
| Replaced | Sessions use Better Auth's default length. lib/server/auth.ts configures no expiry, so it is 7 days. | No decision was taken beyond the default. | Sessions raised to 60-90 days. A queued change is sent only as the person who made it, and a 401 pauses the outbox | superapp blueprint Risks; lib/server/auth.ts (no session config) (2026-08-15) |
| Retired | A trip wallet synced the device's trip list through a secret sync code. Its UI was retired on 2026-08-15, and the endpoints were kept 'temporarily'. | Superseded by server-derived trip lists. lib/walletSync.ts has no importer, but app/api/wallet/* is still live. | Wallets and walletSync are deleted as dead code | docs/superpowers/specs/2026-08-11-trip-wallet-sync-design.md; accounts-auth-design.md §Decisions 7 (2026-08-11) |
| Replaced | The per-device localStorage trip list left the UI. The home page shows the account's trips, derived on the server. | Trips follow the account. | The group/trip switcher on the launcher, fed by synced group and trip spaces | app-shell-login-design.md §Decisions 3, §5 (2026-08-16) |
| Still applies | Preferences are stored per user (theme, accent mode, per-country hue overrides, worldView). The theme is applied before first paint via a cookie and an inline script. Accent overrides are per user, not per trip. | No shared-state conflicts, and no flash of the wrong theme. The theme and accent move to platform/shell, and the map-specific fields (accentHues, worldView) move with the map widgets. |  | planner-redesign-design.md §4.3 (2026-08-17) |

### Security

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Never create an account, type a password or read a secret on the owner's behalf. Hand the owner the SQL or script instead, and do the env and redeploy steps only with their explicit OK. | Standing safety rule from the credential-recovery session. |  | memory owner-credential-recovery.md (How to apply) (2026-09-06) |
| Still applies | Upstream calls go only through server routes, so members' IPs never reach third parties. Inputs are validated before they are put into upstream URLs. | Keeps each provider in one place and protects member privacy. |  | pr4-currency-pivot-plan.md Network rule (2026-08-17) |
| Still applies | Modules that bundle server artifacts or secrets carry import 'server-only' (e.g. lib/server/cityEnrichment.ts:1), enforced by next build in CI (.github/workflows/ci.yml:43). Vitest aliases the package away. | A client import would silently ship 0.8-3.6 MB. In the rebuild every features/*/server.ts carries it. |  | backlog-clearance-design.md §3; memory v36 (2026-09-06) |
| Still applies | For security upgrades, read the GitHub release notes and repository advisories, not just npm audit. Next 16.3.6 was chosen over 16.3.3 for GHSA-vcvr-r3jv-pc5j, and the upgrade was made even though no reachable exploit path was found. | Repository advisories are not in the global advisory database. |  | memory v38 (2026-09-24) |
| Still applies | Guest and public payloads are built from an allowlist, with a compile-time canary that forces every payload field to be classified (lib/redactTrip.ts). | A forgotten field cannot leak. It generalises to the share-page canary: every Briefing and Today section must declare whether it may be public. |  | accounts-auth-design.md §Authorization; understand/auth-identity.md (2026-08-15) |
| Still applies | Every declared outbound User-Agent is '<repo>/<script> (+https://github.com/<owner>/<repo>)', never an email. The single fetch() in each file attaches it, and scripts/user-agent.test.ts pins it (e.g. scripts/country-facts/io.mjs:77). | Wikimedia's policy returns 403 for bare or generic UAs. The value is renamed to travel-super-app in phase 1's first PR. |  | memory v35; nightly-refresh-wikidata-breaks.md (2026-09-23) |

### Data model

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Retired | Two storage backends: SQLite locally (data/app.db, WAL), Postgres (Supabase) in production, chosen by DATABASE_URL, with a 503 on Vercel when it is missing. | Supported local, Raspberry Pi and Vercel hosting. Every table was written twice. | One Postgres only (Neon). SQLite is retired, and tests run on PGlite plus a real-Postgres CI job | memory v2; README Deploying (2026-08-10) |
| Replaced | Tables are created at cold start with CREATE TABLE IF NOT EXISTS, with no migration runner. Shapes evolve lazily at the read boundary (planIdMigration; country defaults to CN; an absent pivot means CNY). Bulk rewrites through the unguarded path are forbidden. | Avoided an ALTER migration on two backends, and protected member edits from fleet rewrites. | Drizzle v1 migrations applied once per deploy from CI with an advisory lock, on a clean slate | planner-redesign-design.md §5.4; understand/data-layer.md §2 (2026-08-17) |
| Replaced | A trip is one whole-blob TripData JSON plus sibling tables. getTrip aggregates everything into one TripPayload, and every child write bumps one trip-wide version. | Simple polling sync. | Per-feature <name>_* tables with FKs into platform tables only, sync spaces with a per-space version lock, and a version on every row | trip-tracker-money-design.md §Decisions 1; understand/data-layer.md §5 (2026-08-15) |
| Still applies | Tickets are stored outside TripData, so a plan rebuild can never touch them. | Bookings survive regeneration. In the rebuild, Tickets owns its own tables and has no FK to Planner. |  | docs/superpowers/specs/2026-08-10-itinerary-editing-tickets-design.md §Storage; trip-plan-ownership-model.md (2026-08-10) |
| Replaced | Members are identified by display-name strings, append-only. Feature rows (checked_by, paidBy, splitAmong, by, addedBy) reference those names inside JSON. | Safe only because nothing ever renames or deletes a member. | People are referenced by member id (FK to the platform member table), never by name. Someone who leaves is marked as left, never deleted | trip-tracker-money-design.md §Data model; understand/data-layer.md §4 (2026-08-15) |
| Retired | The unguessable trip id (40 bits) is itself the bearer capability. The review verdicts were accepted as 'do not re-litigate': 404 before 403, and insertJsonRow throwing on a duplicate UUID. | GET exposed all record ids anyway. | Space-based authorization on member ids and roles, with UUIDv7 ids | memory v5 (2026-08-15) |
| Replaced | Round-trips, not query work, are the database cost. Keep functions in the same region as the database (vercel.json regions ['bom1'] beside Supabase ap-south-1), and parallelise independent reads. | Measured: iad1 to Mumbai made cold get-session take 6-7 s, and 8 sequential reads cost about 10x. The principle carries over. The region changes. | Neon in aws-ap-southeast-1, with functions moved from bom1 to sin1 in the same environment change | memory storage-and-load-budget.md; v21; README Deploying (2026-09-03) |

### Ops

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Replaced | The production database is Supabase free tier over the transaction pooler (Mumbai). It was wiped on 2026-08-21 and found deprovisioned on 2026-09-23, after which a keep-alive was suggested. | It was free. Free-tier projects pause after about 7 idle days. | Neon Launch (Singapore), clean slate, nightly encrypted pg_dump to Cloudflare R2 under a separate account, a monthly restore test, and the backup job doubling as the uptime alarm | memory v1-hosting, v8 wipe, v35 outage (2026-08-10) |
| Still applies | Hosting is Vercel, and every push to main deploys. The install command comes from vercel.json (npm install --no-audit --no-fund), and the poisoned dashboard override was cleared. README's 'pulls the main tarball' claim is stale. | A tarball-over-branch install had broken branch builds for three days. |  | memory planner-redesign.md (Vercel install); vercel.json (2026-08-21) |
| Retired | The repo was made public so the Vercel install command could pull the main tarball. | That install command is gone, so the reason no longer holds. Repo visibility is now just the owner's choice. | vercel.json installCommand (2026-08-21) | memory hosting (2026-08-10) (2026-08-10) |
| Still applies | Nightly refresh jobs commit data to main and deploy themselves. They fail closed and wait for a human to adjudicate. Crons run off-peak at 08:23 and 08:53 UTC (refresh-airports.yml:24, refresh-cities.yml:92), in the maintainer's afternoon, under the shared concurrency group refresh-data. | GitHub's peak-hour cron is best-effort, and a red run expects a human. |  | memory v12; nightly-refresh-wikidata-breaks.md (2026-08-28) |
| Still applies | Refresh cities runs as three jobs: cities and facts in parallel, then commit, which needs both and runs the full npm test. It is strict: a red facts job withholds the cities. Permissions are contents: read except on commit, the artifact path is the LCA of its files, and if-no-files-found is error. | Fixed a 429 caused by back-to-back SPARQL. Note that a full suite in the nightly means any feature's flaky test will block data commits. |  | memory v11 (split); tests-ci map §4 (2026-08-28) |
| Still applies | Artifacts are byte-stable: generatedAt is preserved when the payload is unchanged, and the change guard is git status --porcelain. A commit every night is itself the defect signal. | Avoids committing and redeploying about 10 MB of noise nightly. |  | global-expansion-design.md §3.3; worldwide-city-catalog-design.md §3.1 (2026-08-23) |
| Still applies | Retry-After handling differs on purpose. Country-facts refuses anything above 300 s because its codes query is fatal (scripts/country-facts/io.mjs:168), while the other scripts clamp at 30 s (scripts/enrich/io.mjs:44). The per-country field-loss grace is 1 (scripts/country-facts/gate.mjs:307). | Different failure semantics. Do not 'make them consistent'. |  | memory v11 (2026-08-28) |
| Still applies | Refresh climate is workflow_dispatch only (refresh-climate.yml:9) and is run outside 08:00-09:00 UTC. The CHELSA raster cache (CIP_CHELSA_CACHE) lives outside the OneDrive repo. | About 10.65 GB of rasters. WMO normals change once a decade. |  | memory v24-v25, v30 (2026-09-04) |
| Still applies | Workflows pin Node 24 because the ingest relies on native type stripping. The dependency floor is Node 22, so repo code uses no Node-24-only APIs. | There is no engines field. RegExp.escape was removed for this reason. |  | memory worldwide-airports-phase1.md; v40 (2026-08-23) |
| Still applies | Production env vars are Sensitive, which makes them write-only (vercel env add defaults to Sensitive). Env changes need a redeploy. Use the Vercel CLI, not the MCP or API, for deploy facts and logs. | The MCP returns 403 or 404 on this project. The CLI works. |  | memory v8, v12; owner-credential-recovery.md (2026-08-21) |
| Replaced | Functions are pinned to bom1, beside the Supabase Mumbai pooler (vercel.json). | Measured the 220 ms RTT from iad1. | Functions move from bom1 to sin1 beside Neon Singapore (Neon has no Mumbai region) | memory v21; README (2026-09-03) |
| Replaced | Preview and Production share one DATABASE_URL and one BETTER_AUTH_URL. Push ordering is therefore a correctness property: fix commits go out in the same push as the defect. | Previews write to the production database. | Phase 1 Neon environment change. Whether previews get their own Neon branch is not decided yet | memory v11 (push ordering); owner-credential-recovery.md (2026-09-06) |
| Replaced | Owner credential recovery: the Supabase SQL editor, or .vercel/reset-password.mjs (gitignored), or a temporary admin via /signup + ADMIN_USER_IDS. The invite code can be rotated. | No self-service reset exists. | Clean-slate Neon + Drizzle. The DB-dependent paths are dead, and recovery tooling must be rebuilt for the new schema | memory owner-credential-recovery.md (2026-09-06) |
| Still applies | .npmrc sets legacy-peer-deps=true for the better-auth peer conflict. | Vercel deploys were failing. Re-check it when the dependency set changes. |  | memory v6; .npmrc (2026-08-16) |
| Replaced | Install with npm ci --ignore-scripts, then npm rebuild esbuild, because npm otherwise runs node-gyp for better-sqlite3 and this machine has no Visual Studio toolchain. | The cause is better-sqlite3, which goes away with SQLite. | Postgres only. Re-verify plain npm ci once better-sqlite3 is removed | memory v1, planner-redesign.md traps (2026-08-10) |

### Reference data

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Reference artifacts are per-country static shards under public/ (not Postgres), fetched lazily per country. Server code reads only bundled data/*.json imports, because public/ is unreadable from a Vercel function. The static imports stay (about 27 ms cold, measured). | Avoids seeding a database, and a visitor downloads about 150 KB gzip. Re-homed to reference/, refreshed by nightly jobs. |  | docs/superpowers/specs/2026-08-25-worldwide-city-catalog-design.md §3.2; backlog-clearance-design.md §4 (2026-08-25) |
| Still applies | Each country's province file is held to a 150,000 B gzip budget, with a 700 KB raw tripwire (CA is at 93%). JSON.parse, not I/O, is the bottleneck, so the 22 MB total is left alone. | It is the binding size constraint. Measured parse cost is 6.1 s CPU against 199 ms of disk. |  | docs/superpowers/specs/2026-08-29-phase4-country-region-levels-design.md §8.3; memory storage-and-load-budget.md (2026-08-30) |
| Still applies | Artifacts carry no content hash, so a schema-breaking rebuild needs a cache bust first. Topology assets are cached for 24 h (next.config.ts:45-46), and the login redirect is no-store (proxy.ts:61). | A cached 307 once parked a day-long bounce in front of the assets. |  | memory globe-world-level-phase2.md; map-subsystem-constraints.md (2026-08-24) |
| Still applies | Never scrape. Dianping, Mafengwo and Xiaohongshu are robots.txt-blocked and legally risky under China's Anti-Unfair Competition Law, and unofficial 12306 endpoints are never used. | Legal and ToS risk (docs/PLAN.md:98-100, docs/RESEARCH.md). It generalises to: no scraping of ToS- or robots-blocked sources for any country. |  | memory v1; docs/PLAN.md §Data pipeline (2026-08-10) |
| Replaced | Curated-data-first strategy, with an upgrade path to Wikidata/Wikivoyage/Open-Meteo, then Klook/Trip.com affiliates and AMap via Alibaba Cloud Marketplace. | The original China-first plan. | Owner's 2026-08-27 open-data-first instruction, now global-only. The affiliate and AMap path is not in the approved plan | memory v1 (data strategy); docs/PLAN.md history (2026-08-10) |
| Still applies | Owner instruction: ingest at build time from open data, only what the data supports, with honest gaps rather than invented advice, explicitly not hand-curated-first. 'As clear as China' was resolved as the same concreteness with different, sourced facts. | Binding sourcing instruction. Global-only strengthens it. |  | country-guidance-design.md §What we are building (The bar) (2026-08-27) |
| Still applies | Cities are chosen by notability, not population. From GeoNames cities500, score altNameCount + 2·log10(max(1,pop)) (scripts/cities/build.mjs:45), rank within each country, and keep the top 750 (:28). The score decides inclusion only, display sorts by population, and admin-1 codes resolve to names. GeoNames ids are namespaced G+geonameid. | At pop >= 15k, all 14 sampled destinations (Zermatt, Petra and others) are missing. The score recovers 13 of the 14. |  | memory city-selection-notability-not-population.md; worldwide-city-catalog-design.md §2, §9 (2026-08-25) |
| Still applies | Enrichment: the top 30 cities per country get Wikidata + Wikipedia summaries at build time, and anything else is fetched lazily on first selection (lib/server/cityEnrichment.ts, server-only and session-gated). | Fits a GitHub Action, and every country gets something. |  | worldwide-city-catalog-design.md §4, §9 (2026-08-25) |
| Still applies | Country facts come from Wikidata as structured scalars only, rendered through fixed, reviewed TypeScript templates, so no upstream string reaches a user. Withhold rules: voltage 100-260 V, a single ISO-shaped currency, the 13-row plug table, emergency numbers with roles. CURATED_FACTS hand-verified rows must fire or go stale (scripts/country-facts/curated.mjs:37). | Prose extraction leaked wikitext, and sampling a value would publish 'Belize runs at 550 V'. Country data like this is allowed in reference/. |  | country-guidance-design.md §Why facts, §Four landmines, §CURATED_FACTS (2026-08-27) |
| Still applies | The honest-gap rule: a missing fact is absent, never empty, never a placeholder, never a hedge. A per-field gapNote is rendered and never snapshotted. China's gapNote was [] because China was researched by hand. | A user must be able to tell 'nothing to say' from 'forgot Peru'. The China exemption goes with global-only. |  | country-guidance-design.md §The honest-gap rule (2026-08-27) |
| Still applies | Wrong upstream values are refused in the repo (REFUSED_LANGUAGE_ITEMS by Q-id, curated.mjs:195), rather than accepted or fixed by editing Wikidata. | Owner's call on the Mauritania French edit. |  | memory nightly-refresh-wikidata-breaks.md; v34 (2026-09-23) |
| Still applies | A per-country language-change gate. Any change to a published official-language list aborts before the write: an addition, removal, relabel, withdrawal or new field. The run aborts rather than quarantining. Acceptance is CIP_ACCEPT_LANGUAGE_CHANGES (scripts/country-facts/languages.mjs:43): set for one command, exact, validated first, never set by the workflow. Baseline defects are a separate follow-up. | Three owner forks. The 426 pin had missed a relabel. Still open: env var versus a --accept-language-changes flag, and the baseline review (CY, KP, IQ, GI/FK, CN, KG, AG, MW, sign languages). |  | docs/superpowers/specs/2026-09-24-language-change-gate-design.md Status, §2-3; memory v39 (2026-09-24) |
| Still applies | The SOUTHERN hemisphere list is derived from the artifact's latitudes and reconciled in both directions, with KE as the one named equator exception. | A one-way check left 25 countries wrong. |  | country-guidance-design.md Non-goals (SUPERSEDED note) (2026-08-27) |
| Still applies | Airports come from OurAirports (public domain), filtered to scheduled_service = yes AND an IATA code (4,134 airports, IATA unique and used as the primary key). A daily refresh commits only on change and preserves generatedAt. | 'An airport you can buy a ticket to.' |  | docs/superpowers/specs/2026-08-23-global-expansion-design.md §3.1-3.3; worldwide-airports-phase1.md (2026-08-22) |
| Still applies | Airports are never selectable trip stops, and the types enforce it. They are not a substitute for destinations. | Otherwise a Peru itinerary becomes a list of airports. |  | memory globe-drilldown-blocked-on-data.md; phase4-country-region-levels-design.md §10.1 (2026-08-23) |
| Still applies | The card says 'Main airport', not 'Nearest', because ranking includes a size bonus. The drawable set (large and medium) governs both the card and the layer. The airport layer toggle is ephemeral state, not a preference (D11). | Keeps the card and the map in agreement, and the text honest. |  | phase4-country-region-levels-design.md §3 D11-D12, §10.2; memory v13 (2026-08-31) |
| Still applies | Worldwide climate comes from CHELSA V2.1 1981-2010 normals (CC0), built from offline rasters with zero API calls; Open-Meteo's free tier is unusable. Each city gets 60 ints [lo,hi,precip,clt,td], with elevation correction at read time. The card shows raw lo/hi while the verdict applies the model (P6-7), and an honesty note sits under the map. The Lima coastal-desert case is a test.fails tripwire by decision, and a missing row reads as unknown. | Owner scoped climate in (D4). The fit-model failures were fixed before the fit colours shipped. |  | phase4-country-region-levels-design.md §9; memory v23-v29 (2026-08-29) |
| Still applies | Exact city-id parity is the climate build's invariant. The committed test tolerates bounded cumulative drift, MAX_CATALOG_DRIFT = 10,000 (lib/climateShard.test.ts:464), and tripping it is the cue to dispatch Refresh climate. | Exact parity would turn the nightly refresh red on the first id churn. Merged as built. |  | memory v25-v26 (2026-09-04) |
| Replaced | China's curated climate stays authoritative. REGION_MONTHS takes precedence over derived data, and China's output had to stay byte-identical (§9.5). | Only 72.9% agreement: the model misses Harbin's ice-festival months and Wuhan's 'furnace city' summers. | Global only: China uses the worldwide CHELSA model like every country. The 72.9% agreement cost is accepted | phase4-country-region-levels-design.md §9.5; memory v12 (2026-08-29) |
| Still applies | Natural Earth v5.1.2 admin-1 is the province source. ISO 3166 is the single territory rule, applied through named overrides (D5; Jan Mayen into SJ). The province file is also the outline via merge() (D6). Cities are assigned to provinces by build-time point-in-polygon with an a1c fallback (D8). Single-unit countries get no L3 (D10). | Provinces and outlines agree by construction, and no rule is decided by key precedence. |  | phase4-country-region-levels-design.md §3, §4.1, §7 (2026-08-29) |
| Retired | China keeps its curated 58 KB DataV topology (D7), with public/china-provinces.json as the build input for CN.json. China's seven regions stay as a grouping layer (ChinaRegion, REGION_MONTHS at lib/months.ts:132), guarded by isChinaPlace. | China's hand-drawn regions and climate table were kept. | Global only: China's regions are rebuilt from Natural Earth like every other country. ChinaRegion, REGION_MONTHS and china-provinces.json as a separate source are deleted | phase4-country-region-levels-design.md §3 D7, §6.3-6.4; memory v17 (2026-08-29) |
| Still applies | 'Standardise China to the rest.' ChinaLevel was deleted and China renders CountryLevel like the other 245 countries. | Owner: 'We want to upgrade the app to global and not keep china as unique.' Global-only completes it. |  | memory v17 (2026-09-03) |
| Retired | Judgement call J14: the L2 and L3 map levels were China-only. | No worldwide data existed at the time. | Reversed in Phase 4 (L2/L3 for all 246 countries); now global-only | memory map-subsystem-constraints.md (2026-08-23) |
| Still applies | Wikivoyage was refused on measured grounds (viral CC BY-SA, thin coverage). The owner then overrode that and chose to try extraction. It is designed but not built: nothing ships without a one-time hand review of about 1,321 sentences, quoted verbatim in a separate attributed artifact and never snapshotted. The share-alike risk is unresolved. | Still the owner's call and unscheduled. If built it lives in reference/ as uniform per-country data. The blueprint also suggests community tips as another route to local knowledge. |  | docs/superpowers/specs/2026-08-27-wikivoyage-extraction-design.md Status, §6.4, §9; docs/PLAN.md (2026-08-27) |
| Still applies | Phase 5 flight data is deliberately left unspecified. There is no free daily schedule or fare feed, so it gets a brainstorm first, and the first decision is scope. | Real airport-pair estimates already deliver most of the value. |  | global-expansion-design.md §7; docs/PLAN.md (2026-08-23) |

### Journal & photos

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Replaced | Hybrid photo storage: uploads go to local disk where the filesystem is writable (the planned Raspberry Pi self-host), and Vercel offers paste-a-link photos through a features.photoUploads flag (lib/server/photoStore.ts:54). | The owner planned to self-host on a Pi, and Vercel's filesystem is read-only. | Platform blob table + Cloudflare R2 for photos and files (hosting is Vercel + Neon + R2) | trip-tracker-money-design.md §Decisions 6; memory v5 (2026-08-15) |
| Still applies | Photo uploads accept JPEG, PNG or WebP up to 8 MB (lib/server/photoStore.ts:9). Ids are unguessable, the extension comes from the validated type and never the filename, the bytes must start with the declared type's magic bytes (sniffPhotoType :35), and a mismatch gets the existing 400. User input is never used as a prototype-chain key. | Owner's prescription in PR #38. Still open with the owner: whether a PNG named .jpg should be refused or stored as the type it really is. |  | memory v41; trip-tracker-money-design.md §Photo storage (2026-09-24) |
| Replaced | Deleting a journal entry deletes its uploaded files best-effort. Orphan files are acceptable, and an entry whose file is missing renders a placeholder. | Files lived outside the database. | Synced rows are soft-deleted (deleted_at), and photos are blob rows in R2 with a DB record | trip-tracker-money-design.md §Photo storage (2026-08-15) |

### Sync

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Replaced | Live sync polls /api/trips/:id every 4 s while the page is visible, with a monotonic version guard (lib/useTripPayload.ts:144 POLL_MS). | The simplest multi-user sync. | IndexedDB outbox, POST /api/sync/push and GET /api/sync/pull since a cursor, per group, trip or user space | memory v2; itinerary-editing-tickets-design.md §Sync (2026-08-10) |
| Replaced | Plan edits are single-item ops (POST /plan, pure lib/planOps.ts), applied atomically on the server. The client is optimistic and the server is authoritative. | Concurrent member edits cannot clobber whole plans. The edit-per-op idea carries into Planner commands. | Named, frozen commands (name@version), one transaction per command, with the results applied/duplicate/rejected/retry/paused | itinerary-editing-tickets-design.md §Sync; trip-plan-ownership-model.md (2026-08-10) |
| Replaced | Whole-object writes are version-guarded: read, apply, write if unchanged, retry 3 times, then 409. This covers plan, gateways, currency and rebuild. | Stops a concurrent edit being silently reverted. | A per-space version lock taken inside each command's transaction | backlog-clearance-design.md §2 (2026-09-06) |
| Replaced | Offline and mobile were to be specified separately later, with only 'offline Kit' (tickets and packing readable with no signal) in scope. A session expiring offline was left undefined. | Mobile was deferred behind the desktop redesign. | Full offline edits in every app, an installed PWA on iPhone, and a 'paused' result on 401 with re-sign-in as the same person | planner-redesign-design.md §7, §11 (2026-08-17) |
| Still applies | Mobile contract C6: the trip payload stays serialisable (no Date, Map, Set or functions), so it can be cached and rehydrated. | Required for any offline cache. It carries over to synced rows and command inputs. |  | planner-redesign-design.md §7 C6; lib/contracts.test.ts C6 (2026-08-17) |
| Replaced | Mobile contract C4: one trip-payload accessor (lib/useTripPayload.ts), and components never fetch trip data directly. | Leaves a place to insert a cache later. | Screens read the local IndexedDB replica, and all writes go through commands | planner-redesign-design.md §7 C4; contracts.test.ts:183 (2026-08-17) |

### Planner

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | The plan becomes yours. The generator writes the plan once at creation, and members then own and edit it. Changing settings is an explicit, destructive Rebuild behind a warning. Tickets and packing checks always survive a rebuild, and schedule checks are cleared (app/api/trips/[id]/route.ts:104-107). | A generated plan is only a starting draft. Re-homed to features/planner as an explicit rebuild command. |  | memory trip-plan-ownership-model.md; itinerary-editing-tickets-design.md §Decisions 4 (2026-08-10) |
| Still applies | Schedule checks are keyed by item id (item:<id>), not by index, and legacy index keys were migrated lazily on read. | Needed for durable ticks across edits. Id-keyed references carry over, and the lazy migration goes with the clean slate. |  | itinerary-editing-tickets-design.md §Check keys (2026-08-10) |
| Still applies | The wizard asks for details (dates, days, travellers) before destinations, with a live feasibility counter. | The largest accuracy win: users stop picking blind. |  | planner-redesign-design.md §3.2.1-3 (2026-08-17) |
| Still applies | One merged search input across places, with keyboard navigation and Enter to add. It originally ranked curated results first. | A browsable grid of every city on Earth cannot render. It becomes the reference/ place-search widget, and the curated-first ranking goes with the curated list. |  | planner-redesign-design.md §3.2.2 (2026-08-17) |
| Still applies | Days are time-blocked with nullable startMinutes and durationMinutes. Untimed items render in slot lanes and are never given invented times. Reflow output is never persisted, only the block the user touched emits setTiming, and free-text time is never parsed or overwritten. | No fabricated data, and no rewriting of another member's blocks. |  | planner-redesign-design.md §5.3; docs/superpowers/specs/2026-08-18-day-builder-invariants.md (2026-08-17) |
| Still applies | A places shelf with an explicit target day. Tap-to-target is the primary mechanism, drag-and-drop is layered on for desktop, and there is a keyboard move up/down path. Builder state lives in a hook with no layout knowledge (C3). | Tap is the only mechanism that works reliably on a phone. C3 keeps the mobile view a second view over the same state, not a rewrite. |  | planner-redesign-design.md §3.2.4-5, §7 C3 (2026-08-17) |
| Still applies | Off-map places are allowed: lat/lon are nullable, suggestedDays defaults to [1,2], and a transfer with no coordinates is untimed rather than fabricated. | Thin catalog coverage must not block planning. |  | planner-redesign-design.md §3.2.7, §5.6 (2026-08-17) |
| Still applies | No AI for now. suggestRoute stays a pure function as the future AI seam, with the same input and output shape (lib/route.ts:308-314). | Owner chose 'no AI for now'. |  | docs/superpowers/specs/2026-08-10-map-timeline-explorer-design.md §Decisions; memory v3 (2026-08-10) |
| Still applies | Gateways: arrival and departure airports are optional and nullable (absent, null and IATA are three states). The server stamps defaults at create. They are saved through their own route, never through the rebuild. suggestRoute is anchored only when the user set a gateway (D3), and unknown IATA codes are refused (lib/server/gatewayGuard.ts). | PATCH regenerates the plan and would wipe ticks. |  | phase4-country-region-levels-design.md §3 D3, §10.3; memory v22 (2026-08-29) |
| Still applies | Trip-level season is derived on the server through the country profile and is hemisphere-aware. Per-destination season is deferred. Contract C8 bans the northern-only seasonOfMonth from app/ and components/. | The northern-only derivation was wrong for Peru and Australia. |  | planner-redesign-design.md §5.2; contracts.test.ts C8 (2026-08-17) |
| Still applies | Generated tips, packing and travel-item copy are snapshotted into the trip at creation, with country context applied at generation time. The gap note is computed at render and never snapshotted. | Advice a traveller acted on stays stable. Statements about the data shrink as the data improves. |  | planner-redesign-design.md §11; country-guidance-design.md honest-gap rule (2026-08-17) |
| Retired | China's hand-written editorial profile (lib/countryData/cn.ts) is 'never dissolved'. It holds GENERAL_TIPS (Alipay, VPN, 12306, Amap), the China packing document (RMB, Alipay, VPN, Harbin cold), RAIL_KMH 230 HSR speed and 12306/Trip.com booking copy. | It was the permanent editorial layer for the one hand-researched country. | Global only: every country goes through the same pipeline. Local knowledge may return later as a feature (e.g. community tips), not as country code | docs/superpowers/specs/2026-08-27-country-guidance-design.md §What we are building, Risks 1 (2026-08-27) |
| Still applies | Rail speed is never inferred from open data, because Wikidata's high-speed-rail signal marks Peru and Panama as HSR countries. railKmh null means no rail estimate (lib/countryBaseProfile.ts:104). No invented holidays or crowd curves: holidays [], crowdByMonth null, Nager.Date refused. | This is the invented-advice failure. A flat crowd curve would itself be an unsourced claim. |  | country-guidance-design.md §What no source supplies, Non-goals (2026-08-27) |
| Still applies | The fabricated season stamp was removed. Catalog and GeoNames destinations no longer carry bestSeasons spring/autumn (lib/server/catalog.ts:377,411 now []), and [] reads as no claim. | A card recommended spring in Sydney. |  | phase4-country-region-levels-design.md §9.6; memory v27 (2026-09-05) |
| Retired | The 16 curated Chinese destinations in lib/data/{east,north,south,west}.ts are the app's deepest content: 1,255 lines with index.ts at f3c5788 (memory says 1,292). They carry knownFor, foods and seasonal notes, and feed the cards view. | This was the curated-first strategy. | Global only: Planner drops the hand-written China list and month table | memory v1; README Features (2026-08-10) |
| Retired | The China catalog is data/catalog.json: 695 Chinese cities and attractions from PRC-specific Wikidata classes via scripts/ingest-destinations.mjs, keeping their Q-id namespace. GeoNames twins within 5 km are dropped in favour of the richer Q record. | This was the all-China catalog. It is China-only by construction. | Global only: every country through GeoNames + Wikidata. It is not named in the blueprint's retirement list, so the owner should confirm | memory v2; worldwide-city-catalog-design.md §3.3 (2026-08-10) |
| Replaced | The day tracker computes 'today' from the device clock, assuming the whole party is in UTC+8 (lib/tracker.ts:48). | This is China-specific, and it was recorded as a known limitation. | Global only: Today must use trip-local time | planner-redesign-design.md §11 (2026-08-17) |
| Still applies | 'Already been' tracking: visited places drop out of selection and can be restored (localStorage cip-visited-v1). Wizard drafts stay local until a trip is created. | Part of the owner's original wishlist. Probably moves to the user sync space; not yet decided. |  | memory v1; app-shell-login-design.md §Decisions 3 (2026-08-10) |
| Still applies | Project B planning enrichments (notes on days and activities, a pre-trip to-do list, distances and travel times) were chosen in brainstorm and deferred. | Owner picked them but put Tracker & Money first. Still backlog. The to-do list may belong to Packing or the later toolbox. |  | trip-tracker-money-design.md §Goal; memory v5 (2026-08-15) |
| Replaced | Content parity with China outside China was open and unscheduled. The one designed step was the Wikivoyage extraction. | Other countries had facts but no curated cards. | Global only: China's curated content is retired, so there is no China baseline to match | docs/PLAN.md §What is open (2026-09-06) |

### Briefing

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Derive, then render. A pure buildBriefing (lib/briefing.ts:201) maps the trip to one view-model, and redaction is applied on the server by construction, never in rendering. | A forgotten prop check would leak a booking reference. Re-homed to features/briefing. Other apps contribute sections through the registry. |  | docs/superpowers/specs/2026-08-12-trip-briefing-design.md §Decisions 1 (2026-08-12) |
| Still applies | Redaction table: the public link drops member names, tick attribution, progress counts and Ticket.addedBy. Confirmation, price and notes appear only with the bookings toggle. | A booking's shape is useful. Its reference number is sensitive. |  | trip-briefing-design.md §Redaction table (2026-08-12) |
| Still applies | Share links are opt-in, one live code per trip. The code is 12 characters, about 60 bits (lib/server/ids.ts:29). Revoke means delete, re-enabling mints a fresh code, and the link cascades with the trip. | It is the only secret guarding a public URL. Becomes the platform share_link table. |  | trip-briefing-design.md §Storage (2026-08-12) |
| Still applies | The public link renders live trip data. A frozen snapshot was considered and deferred. | Avoids staleness and a refresh action. Revisit if drift bites. |  | trip-briefing-design.md §Decisions 6 (2026-08-12) |
| Still applies | Only honest charts: every chart derives from the stored plan, and no numbers are invented. | A document people are asked to trust. |  | trip-briefing-design.md §Decisions 3 (2026-08-12) |
| Still applies | /b/<code> is exempt from the login wall (lib/wall.ts:36). The 12-character code is a stronger secret than a shared access code. | Recipients have no account. The rebuild adds: share pages are online-only, never cached by the service worker, and render only sections an app marks public. |  | trip-briefing-design.md §Access gate (2026-08-12) |
| Replaced | Briefing moved out of the nav into a header Share action ('an output you generate, not a room you occupy'). | Part of collapsing the nav to four items. | Briefing is its own mini-app tile, backed by a platform share link | planner-redesign-design.md §2.1 (2026-08-17) |

### Tickets & packing & today

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Tickets are structured entries only, with no file uploads. They show in their own list and as inline chips on the matching days, matched by date. | No blob storage. Re-homed to features/tickets, with the day chips as a registry contribution. |  | itinerary-editing-tickets-design.md §Decisions 1-3 (2026-08-10) |
| Still applies | Ticket-form placeholders come from the trip: first and last stop, gateway IATA, and the trip currency via formatMinor (lib/tickets.ts:78). Title hints depend on the kind and are neutral (TICKET_TITLE_EXAMPLES :98). 'Hotpot dinner' became 'Group dinner'. | Owner's choice over neutral wording. |  | docs/superpowers/specs/2026-09-07-unscheduled-items-design.md §1 (2026-09-07) |
| Replaced | Four-item trip navigation (Plan / Today / Money / Kit, lib/nav.ts:33 TRIP_NAV) with Kit grouping tickets and packing. Any fifth item must justify itself, and contract C1 is one nav source with short labels (C7). | Four labels fit a mobile bottom bar. | Launcher home: 'Now' strip, app grid of mini-apps (Tickets and Packing separate), group/trip switcher. C1 is rewritten once in phase 1 | planner-redesign-design.md §2.1-2.2, §7 C1/C7 (2026-08-17) |
| Still applies | The Today tab shows a countdown before departure, a live dashboard during the trip (day X of Y, now/next, ticks, spend snapshot) and a recap afterwards. | Owner's Wanderlog-style tracker request. Re-homed to features/today, with its content coming through registry contributions. |  | trip-tracker-money-design.md §Tracker tab; README (2026-08-15) |
| Still applies | Shared ticking of packing items and activities by any member, with attribution ('done by Bob'), synced to everyone. | Core group feature. Attribution moves from names to member ids. |  | memory v2; README (2026-08-10) |
| Still applies | Mobile contracts C2 (only the shell pins anything to the bottom edge) and C5 (safe-area insets and a 44 px minimum target in the token set). | Cheap now, expensive later. They carry into platform/shell and platform/ui. |  | planner-redesign-design.md §7 C2, C5 (2026-08-17) |

### Licensing

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | GeoNames CC BY 4.0: a visible credit on every surface that renders GeoNames city names (components/plan/GeoNamesCredit.tsx), enforced by contract C7 (lib/contracts.test.ts:612), including a byte comparison of data/cities-report.md. It is a licence obligation, and nothing merges without it. | The app's first attribution-required source. In the rebuild, C7's scan roots must include features/ and reference/. |  | worldwide-city-catalog-design.md §7; memory v10 (2026-08-25) |
| Still applies | Wikipedia summaries (CC BY-SA 4.0, attribution and share-alike) are credited in the same component (GeoNamesCredit.tsx:16-38). | Enrichment blurbs are mostly Wikipedia intro extracts. |  | GeoNamesCredit.tsx docblock; docs/PLAN.md data table (2026-08-28) |
| Still applies | CC0 and public-domain sources (Wikidata, CHELSA, OurAirports, Natural Earth) are deliberately NOT credited in the UI. Their licence is recorded in the artifact envelope and report. A task that adds an attribution-bearing source owns the credit clause, the C7 widening and the report update in one commit. | Naming a CC0 source would make the legal notice look discretionary. |  | country-guidance-design.md §Attribution (2026-08-27) |
| Still applies | Wikimedia Commons hero images carry a credit line, always sit under a scrim, and fall back to an accent gradient. | Required by the licence. Contrast holds on any photo. |  | planner-redesign-design.md §4.4 (2026-08-17) |
| Still applies | The rates page carries the required 'Rates By Exchange Rate API' attribution link, pinned by a test. | Required by er-api's terms. |  | pr4-currency-pivot-plan.md Task 7 (2026-08-17) |
| Still applies | Fonts are vendored under app/fonts with OFL.txt (Bricolage Grotesque, IBM Plex Sans and Mono) through next/font/local. The owner chose to keep all 19 subset files. | Removes the Google Fonts /l/font build flake (vercel/next.js#99114), and place names need latin-ext and vietnamese. |  | memory v42 (2026-09-24) |
| Still applies | Line endings: .gitattributes pins LF for generated artifacts. The fix is pinning eol, never stripping CRs in the licence test. | A licence guard that goes red spuriously is one people learn to ignore. The rebuild's migration rules also forbid CRLF. |  | memory v11; .gitattributes header; crlf-breaks-committed-report-contracts.md (2026-08-28) |

### Map & ui

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | The globe is the default world picker (110m polygons plus the point layer carried over from 50m, 235 countries reachable). The flat map is a worldView preference, and prefers-reduced-motion forces it. Back-face countries get no DOM node, and keyboard focus drives rotation. | 110m is required for frame budget. opacity:0 would leave phantom focusable nodes. Re-homed to reference/ widgets. |  | memory globe-world-level-phase2.md; map-subsystem-constraints.md (2026-08-24) |
| Still applies | The destinations step opens on the globe. | Picking a country out of 246 is what the globe is for. |  | memory v28 (PR #29) (2026-09-06) |
| Still applies | At L2 the map comes with a grouped, filterable place list, and the list is the accessibility spine (D2): the WCAG 2.5.8 'Equivalent' path. Markers get at least 44 CSS px targets (--tap-min, inline-flex min-h pattern). Dot overlap is left alone. | Owner decision D2. Hit circles are exact by construction. |  | phase4-country-region-levels-design.md §3 D2; memory v17-v18 (2026-08-29) |
| Still applies | Anything meant to sit under the map goes through CountryLevel's belowMap slot, never as a sibling after the level. | The legend rendered under the 750-chip list and eleven reviews missed it. |  | memory browser-glance-before-merge.md; v27 (2026-09-05) |
| Still applies | Nothing country-scoped is fetched until a country is opened (the one-way openedCountry latch). PlaceSearch fetches only after first focus or keystroke. | Owner's choice. It saved about 95 KB gzip of China prefetch on every globe mount. |  | unscheduled-items-design.md §4; memory v32 (2026-09-07) |
| Still applies | Maps render SVG through d3-geo + topojson with no new dependencies. Colour comes from lib/accent and tokens, with no hex literals in map components (FIT_COLORS is the one exception). | Keeps the accessibility tree and jsdom tests. |  | planner-redesign-design.md §6; global-expansion-design.md §8 (2026-08-17) |

### Design & brand

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Train-ticket / boarding-pass aesthetic: rail blue, ink navy and vermilion seal, with perforated edges and mono route codes, set in Bricolage Grotesque and IBM Plex. Keep this identity when extending the UI. | Owner's standing direction, reconfirmed on 2026-09-24. Three-letter app codes keep the boarding-pass look on the launcher. |  | memory v1; app-shell-login-design.md §6; super-app-architecture.md (2026-08-10) |
| Retired | Chinese-character marks: seal stamps (已选/去过/启程), the 同行 chop via Country.mark (lib/countries.ts:43), the 游 brand mark and the .font-kai stack. The 2026-09-07 decision kept 游 and .font-kai as brand. | They were China-rooted identity. | English passport stamps (identity: boarding-pass look plus vermilion ink kept) | memory v1; planner-redesign-design.md §4.5; unscheduled-items-design.md §1 (2026-09-07) |
| Still applies | 21st.dev is interaction inspiration only. No shadcn or Radix install, and patterns are rebuilt in plain Tailwind. | Keeps the ticket aesthetic. |  | map-timeline-explorer-design.md §Decisions; memory v3 (2026-08-10) |
| Still applies | Semantic token system (--ink, --surf, --paper, --seal, --on-accent) with light as the default plus dark and system. Dark mode is reachable, and there is no opaque bg-white in app/ or components/. | A theme change is a token swap. Re-homed to platform/ui. |  | planner-redesign-design.md §4.1; memory planner-redesign.md (PR3, Task 39) (2026-08-21) |
| Still applies | Per-country accent is derived by golden angle over the ISO list, with lightness and chroma pinned per role (--accent-ink and --accent-fill). The user picker offers hue only. Resolution order is user, then curated, then derived (lib/accent.ts:85-100). | A hash put CN, TH and VN on the same pink, and hue-only means every possible pick stays legible. |  | planner-redesign-design.md §4.2-4.3 (2026-08-17) |
| Replaced | China has a curated accent hue (accentHue 30 at lib/countries.ts:43). | A deliberately chosen hue for the home country. | Global-only: a country-specific value may exist only as reference/ data. Either keep it as reference data or derive CN like everyone else (owner's call) | planner-redesign-design.md §4.2 (2026-08-17) |
| Still applies | Polish gate: the make-interfaces-feel-better checklist, tabular-nums, balanced headings, scoped transitions, and plain sentence-case copy. | Named review step for new surfaces. |  | app-shell-login-design.md §6 (2026-08-16) |
| Replaced | The app is named 'China Itinerary Planner 游' (README title, live URL). | Its origin as a China planner. | Display name chosen in the shell and launcher phase. The repo is already renamed travel-super-app | README.md:1; unscheduled-items-design.md §1 (2026-08-10) |

### Testing

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Tests must be able to fail. Run a new test against the unfixed code, mutation-check it, assert the fixture can reach the condition (e.g. geoArea < 1 for ring winding), and prefer an absolute pinned number to a relational assertion. | Repeated hollow tests on this repo. |  | memory review-catches-plan-bugs.md; planner-redesign.md (2026-08-23) |
| Still applies | Do not raise test timeouts. Move the work into setup instead (the jsdom warmup in vitest.setup.ts). Sample per-process CPU before blaming a branch, and check the 'Test Files' count and any dropped workers. | Raising the timeout was tried and bought nothing. |  | memory concurrent-load-test-flakes.md (2026-08-24) |
| Replaced | Postgres store paths shipped on inspection only, and a testcontainers harness was wanted. | There was no PG harness. Four features shipped that way. | PGlite + a real-Postgres CI job (Testcontainers), with randomised replay tests | memory v4-v5 (2026-08-12) |
| Replaced | E2E runs on Playwright, port 3100, workers: 1 (playwright.config.ts:87), against a throwaway SQLite DB. Projects are chosen by file-name regexes. | One next dev server answers only one spec at a time. | Per-feature Playwright file-name conventions (x.spec.ts, x.signed-out.spec.ts, x.mobile.spec.ts), on Postgres only | memory v17, v40 (2026-09-03) |
| Replaced | Whole-repo contract tests C1-C8 in lib/contracts.test.ts: one nav, bottom edge, builder boundary, one trip accessor, serialisable payload, GeoNames credit, hemisphere. | Source scans turn written rules into CI failures. | Platform contracts plus one contract file per feature, and the new guards: boundary, schema reference, pull conformance, command snapshot, share-page canary, migration rules, diff scope, global-only scan | planner-redesign-design.md §9; tests-ci map §2 (2026-08-20) |
| Replaced | CI runs tsc, vitest, next build and Playwright on every push and PR, with no path filters. | The first time the repo gated merges on its own suite. | Path-filtered CI: touched features plus every platform contract; platform changes run everything | memory v9, v30 (2026-08-24) |
| Replaced | lib/chinaLeakScan.ts proves CN-only strings never appear in another country's generated plan, after first proving it detects them in a real China plan. | Guarded against China leaking into other countries' plans. | The global-only scan CI guard: no country special cases in features/ or platform/ | country-guidance-design.md Risks 11 (2026-08-27) |
| Still applies | Every artifact commits a measurement report (data/*-report.md). A number in a spec with no report behind it counts as unmeasured, and a plan's 'Expected: N' is a claim to verify. | The one roadmap section with no report was wrong on every figure. |  | memory v12; review-catches-plan-bugs.md (2026-08-29) |
| Replaced | Tests are placed by vitest project: node runs lib/**/*.test.ts and scripts/**, jsdom runs components/**/*.test.tsx. Route handlers are tested from lib/server, and shared harnesses live in root-level test/. | A mismatched file runs in no project. | features/<name>/tests, with vitest includes covering features/, platform/ and reference/ | global-expansion-design.md §8; tests-ci map §1 (2026-08-23) |

### Workflow

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Rebase-merge only. main stays linear with zero merge commits, because every branch commit lands individually and a red one breaks bisect. | Standing repo invariant. |  | memory v11, v19, v43 (2026-08-16) |
| Still applies | Merge one PR at a time. Rebase each onto the new main, because GitHub rewrites SHAs even for an up-to-date branch. Force-with-lease, wait for every check, run gh pr merge --rebase --match-head-commit, confirm main's tree equals the tested head's tree, and delete the remote branch by hand (delete_branch_on_merge is false). | This is how all six PRs merged in one pass. |  | memory v43 (2026-09-24) |
| Still applies | Merging is the owner's call. The agent merges only on explicit instruction. | Standing practice (e.g. 'merge pr 34', 'merge it and proceed next'). |  | memory v22, v35-v43 (2026-08-16) |
| Still applies | Before calling work done, hand the whole-branch diff plus the spec to Fable for a final review. Say where earlier reviewers looked, accept a clean report, and verify its claims. | Standing owner instruction. Fable repeatedly caught what nine or more reviews missed. |  | memory always-final-check-with-fable.md (2026-08-23) |
| Still applies | Before any UI merge, glance at the rendered result in a real browser through the e2e harness's saved session. Check order, not just presence, and never sign in by hand. | Standing instruction after jsdom and eleven reviews missed a layout bug. The glance harness must follow the new Playwright conventions. |  | memory browser-glance-before-merge.md (2026-09-05) |
| Still applies | Agents write, adversarial refuters try to knock the findings down, and survivors are re-reviewed before anyone acts. Verify agent self-reports by running the gates yourself, and do not re-run a review after committing its findings document. | Owner's standing process. About 1 in 6 confirmed findings did not hold. |  | memory planner-redesign.md (standing process) (2026-08-17) |
| Still applies | Code blocks in a plan are unverified starting points. The final pass reads the SPEC, not the plan, and is run on a different model. A reviewer's caveats are carried into the artifacts. | Most defects on this repo lived in the plan's own prescribed code. |  | memory review-catches-plan-bugs.md (2026-08-23) |
| Still applies | Any command a doc tells the operator to run is run in PowerShell (the owner's default shell) before merge. | Five reviewers passed a POSIX-only command. |  | memory review-catches-plan-bugs.md (PR #39 addendum) (2026-09-24) |
| Still applies | Each piece of work gets one dated spec with a status line, then a plan, then execution. Plan files stay untouched during execution (no ticked boxes). The in-repo record carries corrections, because gitignored ledgers evaporate. | The design record lives in the repo. Re-homed to features/<name>/docs and docs/platform. |  | docs/PLAN.md header; memory v14, review-catches-plan-bugs.md (2026-08-10) |
| Still applies | Small PRs from one plan. Never stack a PR on an unmerged branch, and ship behaviour changes before pure moves. | A stacked branch on a fast-moving rebase-merged main is a rebase waiting to happen. It becomes one feature per PR, one worktree per feature. |  | unscheduled-items-design.md §5 (2026-09-07) |
| Still applies | The 800-line file guidance is enforced by splitting all oversized source files (owner chose all five), with tests split by describe. A pure-move PR gets one mechanical comment sweep at the end. | Owner's choice. |  | unscheduled-items-design.md §2; memory v31-v33 (2026-09-07) |
| Still applies | Commit messages are permanent under rebase-merge. Corrections go in a later commit or the PR body, never a force-push of main. Ask when a commit landed using committer date (%cd) or the API's created_at, never %ad, and git fetch before judging whether a rebase is needed. | Author dates are days early under rebase-merge, and a stale local main misleads. |  | memory v12, v15, review-catches-plan-bugs.md (PR #34) (2026-09-23) |
| Retired | The Phase 4 branch was held back from origin until the phase completed ('will push together when phase 4 completes'). | Phase-specific instruction. | Phase 4 merged on 2026-09-06; one feature per PR from now on | memory v14 (2026-08-30) |
| Still applies | Run next and Playwright from .claude/worktrees/<name>, never from the %TEMP% scratchpad, and never touch the Windows App Control policy. | App Control blocks Next's native SWC under %TEMP%. |  | memory v43 (2026-09-24) |

### Rebuild baseline

| Status | Decision | Why | Replaced / retired by | Source |
|---|---|---|---|---|
| Still applies | Sync is a custom, server-authoritative command outbox that follows Replicache's published protocol as a spec, not as a dependency. A Dexie outbox sits beside a disposable replica. PowerSync (about $68/month) is the fallback. Zero, Electric and the CRDT frameworks are ruled out. | Option A, approved: no always-on sync service, and a database that sleeps when idle. |  | memory super-app-architecture.md; superapp-blueprint (Three ways to build it) (2026-09-24) |
| Still applies | Commands go to fixed routes, never to Server Actions. A shipped name@version keeps a working handler forever, guarded by a snapshot test. A rejection is recorded in a separate transaction after the rollback, so a retry gets the same answer. | Server Action ids change on every deploy, and phones can stay offline through many deploys. |  | superapp-blueprint (Opening an app with no signal; Risks) (2026-09-24) |
| Still applies | Drizzle v1 RC pinned exactly, with Kysely 0.29 as the fallback. Migrations run from CI with an advisory lock. Tests run on PGlite plus Testcontainers. | Migration history, one DDL, and real-Postgres coverage. |  | memory super-app-architecture.md (2026-09-24) |
| Still applies | Every synced row has a UUIDv7 id, space_id, version and deleted_at. Deletes are soft, with partial unique indexes WHERE deleted_at IS NULL. group_id is on every row, with a composite (group_id, trip_id) FK. A constraint trigger keeps trip-only guests on their own trip, and a deferred trigger makes split shares sum to the amount. | The database itself enforces integrity, so one feature's bug cannot corrupt another's data. |  | superapp-blueprint (One database, strict rules) (2026-09-24) |
| Still applies | Hosting: Neon Launch in aws-ap-southeast-1, with functions moved from bom1 to sin1. Cloudflare R2 holds photos and a nightly encrypted pg_dump under a separate account, and a restore is tested monthly. The service worker is hand-written, and iPhone users are prompted to install to the Home Screen. | Owner confirmed the move to Singapore. Safari can wipe site storage that is not installed. |  | memory super-app-architecture.md; blueprint Risks (2026-09-24) |
| Still applies | Build order: (1) platform foundations, retiring the old store first because pgStore's ensureSchema would create clashing auth tables, with trip pages showing 'being rebuilt'; (2) offline sync and install; (3) shell and launcher, where the name is chosen; (4) a reference/ platform PR, then Explore, Planner, Money, Tickets, Packing, Journal, Briefing, Today; (5) Polls, then Dates. Dates sets trip dates through the platform trip API. | Each step has a gate. The first two new apps are built only from the template. |  | memory super-app-architecture.md; blueprint Build order (2026-09-24) |
| Still applies | The registry discovers each feature's manifest.ts, server.ts and client.tsx through import.meta.glob. No central list is edited, which ties the build to Turbopack. | Two features built in parallel never conflict on a shared file. |  | superapp-blueprint (Anatomy of one mini-app) (2026-09-24) |
| Still applies | Agent files: a tracked root CLAUDE.md (@AGENTS.md plus a repo map), and .claude/rules/platform/*.md always loaded (200 lines or fewer). Each feature has its own CLAUDE.md, not AGENTS.md, because Claude Code 2.1.215 does not read subfolder AGENTS.md. A path-scoped platform-internals rule has no braces in paths:. Add a new-mini-app skill and boundary and integrity reviewer agents. Split the project memory per feature, and add .claude to .vercelignore. | Owner approved the Claude-file layout. |  | memory super-app-architecture.md; blueprint (Claude and agent files) (2026-09-24) |
| Still applies | Naming: the repo is renamed darrenCWJ/travel-super-app. The 23 files naming the old repo (package.json, README, the catalog raw URL, every User-Agent) are updated in phase 1's first PR. The Vercel project is renamed together with the Neon env change, when BETTER_AUTH_URL moves. The local folder is renamed later along with this memory directory. The display name is decided in the shell phase. | Owner decisions from 2026-09-24. |  | memory super-app-architecture.md (2026-09-24) |
| Still applies | A global-only scan in CI fails any country special case (=== 'CN', a hard-coded default country or currency) in features/ or platform/. Country data may live only in reference/ tables. | Owner: 'no more china only stuff'. This reverses the older 'curated China stays authoritative' call. |  | memory super-app-architecture.md; blueprint Guardrails (2026-09-24) |

